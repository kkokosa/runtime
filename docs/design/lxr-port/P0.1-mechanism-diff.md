# P0.1 — Mechanism-level diff: `lxr-pldi-2022` vs `lxr` branch head

**Purpose.** This is the direct input to **P0.3** (the mechanism parity ledger). For each LXR
mechanism it records where the mechanism lives on each of the two parity oracles, what changed
between them, whether the change is **semantic** or **cosmetic**, and which revision a .NET/CoreCLR
port should follow — with a reason.

**Revisions under comparison**

| | mmtk-core | mmtk-openjdk | date |
|---|---|---|---|
| **PLDI** (`lxr-pldi-2022`) | `4d4e516c` *class pointer validity checks* | `abbdd1d` | 2022-04-08 |
| **HEAD** (`lxr` branch) | `9625c174` *Add an option to disable RC decs* | `0682434` | 2026-05-06 |

`lxr-x/simplified` is out of scope by the parity contract and is not considered anywhere below.

**Evidence convention.** Every claim is tagged:

- `[verified]` — read directly out of both checked-out revisions (file listing, line count, or
  quoted source), or observed at runtime.
- `[inferred]` — a reasonable reading of the code that was *not* separately confirmed by running it.

---

## 0. The structural fact that shapes this whole document

> **`mmtk-core/src/plan/lxr/` does not exist at the PLDI revision.** `[verified]`

At `4d4e516c` there is no LXR plan. LXR is a **compile-time feature configuration of the Immix
plan**. `src/plan/` at PLDI contains `barriers.rs`, `generational/`, `global.rs`, `immix/`,
`markcompact/`, `marksweep/`, `mod.rs`, `mutator_context.rs`, `nogc/`, `pageprotect/`,
`plan_constraints.rs`, `semispace/`, `tracelocal.rs`, `transitive_closure.rs` — and nothing else.
`[verified]`

At `9625c174` there is a first-class `src/plan/lxr/` tree of 13 files. `[verified]`

**Consequence for P0.3:** a path-to-path `git diff` between the two revisions is meaningless for
every path the P0.1 task statement listed under `src/plan/lxr/`. The comparison below is therefore
organised **by mechanism**, and each section states the *location on each revision* before it states
the delta. This is not a stylistic preference; it is the only form the comparison can take.

### Corrections to the paths named in the P0.1 task statement

| Task statement said | Reality | Evidence |
|---|---|---|
| `plan/lxr/{barrier,mutator,global,mod,mature_evac,args}.rs` | HEAD-only paths, except `args.rs` which is `src/args.rs` at the **crate root on both revisions** | `[verified]` |
| `plan/lxr/gc_work/{rc,tracing,nursery_sweeping,mature_sweeping,mature_evac}.rs` | HEAD-only paths | `[verified]` |
| `policy/immix/block_allocation.rs` | PLDI-only path. At HEAD it is `plan/lxr/block_allocation.rs`, behind an `ImmixHooks` trait | `[verified]` |
| `policy/immix/{block,line,immixspace,defrag}.rs` | Present on **both**; the only genuinely path-stable Immix set | `[verified]` |
| `plan/concurrent/mod.rs` | HEAD-only. PLDI's equivalent is `src/util/cm.rs` | `[verified]` |
| `mmtk-openjdk/openjdk/barriers/mmtkFieldBarrier.cpp` | HEAD-only. PLDI's equivalent is `mmtkFieldLoggingBarrier.cpp/.hpp` | `[verified]` |
| `policy/immix/chunk.rs` | PLDI-only (508 lines). At HEAD, replaced by `util/heap/chunk_map.rs` | `[verified]` |

---

## 1. Summary table

| # | Mechanism | Delta | Recommended oracle | One-line reason |
|---|---|---|---|---|
| 2 | Plan identity / configuration | **semantic** | **HEAD** | A runtime-selected plan is the only shape that maps onto a CoreCLR standalone GC |
| 3 | Coalescing reference counting | **cosmetic + parameterisation** | **PLDI** | The algorithm is unchanged; PLDI states it in one self-contained file |
| 4 | The field barrier (RC + SATB, fused) | **cosmetic (rename + slot abstraction)** | **HEAD** | Same algorithm, but `VMSlot` is exactly the abstraction a CoreCLR port needs |
| 5 | SATB concurrent marking | **semantic (relocation + generalisation)** | **PLDI** for LXR semantics; **HEAD** for the scheduler contract | HEAD generalised SATB for a *different* plan; LXR's own use of it is unchanged |
| 6 | Nursery sweeping | **structural** | **HEAD** | Same job, extracted to a named file; HEAD is simply easier to port |
| 7 | Mature sweeping / cycle collection | **structural** | **HEAD** | ditto |
| 8 | Mature evacuation & defrag | **semantic (tuning)** | **HEAD** | HEAD has four more years of remset/defrag tuning; PLDI's is the paper's |
| 9 | Immix block / line metadata | **semantic (shrinkage)** | **PLDI** | PLDI's `block.rs` carries LXR-specific state that HEAD moved behind a trait |
| 10 | Block allocation | **structural (relocation)** | **HEAD** | The `ImmixHooks` split is the correct layering for a port |
| 11 | Object forwarding & side metadata | **cosmetic** | **HEAD** | Near-identical; HEAD tracks upstream MMTk |
| 12 | Work bucket / scheduler | **semantic (new buckets)** | **HEAD** | Concurrent buckets are a hard requirement and PLDI lacks them |
| 13 | Object model / binding | **semantic (upstream churn)** | neither directly | OpenJDK-specific; CoreCLR needs its own |
| 14 | Runtime args | **semantic (inverted defaults)** | **HEAD** | HEAD's opt-*out* defaults encode "full LXR is the default" |

**Coupled sets** (must share one oracle — see §15).

---

## 2. Plan identity and configuration

### Location

| | PLDI | HEAD |
|---|---|---|
| plan | `src/plan/immix/{global,mod,mutator,gc_work}.rs` (1025/154/90/297 lines) | `src/plan/lxr/{global,mod,mutator}.rs` + `gc_work/` (1100/124/75 lines + 8 files) |
| selection | `Cargo.toml` feature `lxr` | `PlanSelector::LXR` in `src/util/options.rs:54` |

`[verified]`

### What changed

**PLDI.** LXR is a stack of cargo features. `Cargo.toml` defines **41 `lxr*` features**. `[verified]`
The composite is:

```toml
lxr_basic = ["ix_ref_count"]
lxr_cm    = ["lxr_basic", "lxr_concurrent_marking"]
lxr_lazy  = ["lxr_basic", "lxr_lazy_decrements"]
lxr_evac  = ["lxr_basic", "lxr_nursery_evacuation",
             "lxr_mature_evacuation", "lxr_count_bytes_for_mature_evac"]
lxr       = ["lxr_basic", "lxr_cm", "lxr_lazy", "lxr_evac"]
```

`src/args.rs` then turns each feature into a `const bool` — `REF_COUNT`, `CONCURRENT_MARKING`,
`LAZY_DECREMENTS`, `RC_NURSERY_EVACUATION`, `RC_MATURE_EVACUATION`, `BLOCK_ONLY`, `DEFRAG` — so the
whole plan is monomorphised at compile time. `[verified]`

**HEAD.** `PlanSelector::LXR` is a runtime enum variant; `src/plan/global.rs:63` and `:103`
dispatch to `create_lxr_mutator` and `LXR::new`. `[verified]` The head `Cargo.toml` says outright:
*"We can select plan at runtime. So no need to use any of these features"*. `[verified]`

### Semantic or cosmetic

**Semantic**, and it is the single most consequential difference for a port. It changes what a
"plan" *is*: at PLDI, choosing LXR is a build; at HEAD it is a startup option.

### Which revision should a .NET port follow, and why

**HEAD.** A CoreCLR standalone GC is a DLL loaded at process start and selected by configuration
(`GCName`); it cannot be a compile-time monomorphisation of a different collector. HEAD's shape is
already the shape the port must have. The PLDI feature list remains useful as an *ablation
catalogue* — it enumerates precisely which mechanisms can be independently disabled, which is
valuable for staged bring-up — but it should not drive the architecture.

---

## 3. Coalescing reference counting

### Location

`src/util/rc.rs` on both revisions — but **PLDI 1057 lines vs HEAD 245 lines**. `[verified]`

### What changed

This 4× shrink is *not* a loss of function. At HEAD the RC work packets moved out of `util/rc.rs`
into `src/plan/lxr/gc_work/rc.rs` (956 lines). `[verified]` What remains in `util/rc.rs` is the bit
manipulation layer.

The core operations are the same set on both: `inc`, `dec`, `set`, `count`, `is_dead`, `rc_stick`,
`promote`, `mark_straddle_object` / `unmark_straddle_object`, `is_straddle_line`,
`address_is_in_straddle_line`, `rc_table_range`, `assert_zero_ref_count`. `[verified]`

Both use the same side-metadata specs: `RC_TABLE`, `RC_STRADDLE_LINES`, and (PLDI) `RC_LOCK_BITS`.
`[verified]`

**Stuck counts.** Both implement the paper's saturating count: `rc_stick(o)` is
`count(o) == MAX_REF_COUNT`, and `inc` refuses to increment past it. `[verified]`

**The one real difference is the count width.**

| | PLDI | HEAD |
|---|---|---|
| `LOG_REF_COUNT_BITS` | `1` (a hard constant) | a `const` block selecting 1/2/3 from features `lxr_rc_bits_{2,4,8}`, **defaulting to 1** |
| `REF_COUNT_BITS` | `usize` | `u8` |

`[verified]` So the *default* is identical — 2 bits per object, counts 0–3, 3 = stuck — which is what
the paper describes. HEAD merely made the width tunable.

**`LOG_MIN_OBJECT_SIZE`**: PLDI hard-codes `4`; HEAD takes it from
`crate::util::constants::LOG_MIN_OBJECT_SIZE`. `[verified]` Same value on 64-bit OpenJDK; a port must
re-derive it from CoreCLR's own minimum object size, which is **not** 16 bytes.

### Semantic or cosmetic

**Cosmetic, plus parameterisation.** No change to the RC algorithm at the default configuration.

### Which revision should a .NET port follow, and why

**PLDI** for the *specification* of RC: at PLDI the whole mechanism — bit layout, straddle-line
handling, and the inc/dec processing packets — is in one 1057-line file that can be read end to end
against the paper. Port HEAD's *split* (metadata layer vs work packets) once the semantics are
established, because that split is better engineering; but the semantics to implement are PLDI's,
and they are unchanged at HEAD.

**Porting note.** `LOG_MIN_OBJECT_SIZE` and the RC-table granularity are the first two constants that
must be re-derived for CoreCLR. Every straddle-line computation depends on them.

---

## 4. The field barrier (coalescing RC + SATB, fused)

> This section corrects an error I made and caught. The initial reading was that HEAD splits LXR's
> barrier into an RC barrier and an SATB barrier. **It does not.** LXR's barrier is fused on *both*
> revisions. What HEAD adds is a *separate* SATB-only barrier belonging to a *different* plan.

### Location

| | PLDI | HEAD |
|---|---|---|
| Rust | `src/plan/barriers.rs` → `FieldLoggingBarrier` (655 lines total) | `src/plan/lxr/barrier.rs` → `LXRFieldBarrierSemantics` (249 lines) |
| C++ | `openjdk/barriers/mmtkFieldLoggingBarrier.{cpp,hpp}` (12.7 KB + 13.6 KB) | `openjdk/barriers/mmtkFieldBarrier.{cpp,hpp}` (20.9 KB + 3.5 KB) |
| selector | `BarrierSelector::FieldLoggingBarrier` | `BarrierSelector::FieldBarrier` |

`[verified]`

### The fusion evidence

**PLDI `FieldLoggingBarrier`** carries both buffers and imports the SATB packet type:

```rust
use crate::util::cm::ProcessModBufSATB;   // barriers.rs:10
...
incs: Vec<Address>,                       // :197
decs: Vec<ObjectReference>,               // :198
```

and in `slow()` (`:313`) pushes `old` onto `decs` and `edge` onto `incs` in the same call. `[verified]`

**HEAD `LXRFieldBarrierSemantics`** is structurally the same:

```rust
use super::gc_work::tracing::ProcessModBufSATB;  // barrier.rs:10
...
incs: VectorQueue<VM::VMSlot>,                   // :35
decs: VectorQueue<ObjectReference>,              // :36
```

with `flush_incs()` (`:145`) and `flush_decs_and_satb()` (`:159`) — the latter feeding the *same*
`decs` buffer to both `ProcessModBufSATB` and `ProcessDecs`. `[verified]`

**That last point is the mechanism.** LXR's decrement buffer *is* its SATB buffer. The old target
overwritten by a field write is simultaneously (a) an object that lost a reference, so it needs a
decrement, and (b) an object that was reachable at the snapshot, so the concurrent trace must see it.
One barrier, one buffer, two consumers. This is the core insight the port must preserve, and it is
identical on both revisions.

### What HEAD's `SATBBarrierSemantics` actually is

`src/plan/concurrent/barrier.rs:18` defines `SATBBarrierSemantics` with a **single** field
`satb: VectorQueue<ObjectReference>` (`:25`) and no `incs`/`decs`. `[verified]` It belongs to HEAD's
new **ConcurrentImmix** plan (`src/plan/concurrent/immix/`), not to LXR. `[verified]`
Similarly `mmtkSATBBarrier.cpp` and `mmtkUnlogBitBarrier.cpp` are new **additional** barrier
implementations, not a decomposition of the field barrier. `[verified]`

So: HEAD split the barrier **family**; it did not split **LXR's** barrier.

### The genuine LXR-visible delta

1. **Rename** `FieldLoggingBarrier` → `FieldBarrier` (and the C++ file with it).
2. **`Address` → `VM::VMSlot`.** PLDI's `incs` buffer holds raw `Address`; HEAD's holds an
   abstract slot type. Every barrier entry point changes accordingly
   (`get_edge_logging_state(edge: Address)` → `get_slot_logging_state(slot: VM::VMSlot)`,
   `log_edge_and_get_old_target` → `log_slot_and_get_old_target`). `[verified]`
3. **`Vec` → `VectorQueue`.** A capacity-aware buffer replacing manual `len() >= CAPACITY` checks.
   `[verified]`
4. **PLDI has explicit per-edge locking** — `attempt_to_lock_edge_bailout_if_logged`, `unlock_edge`,
   `log_and_unlock_edge`, backed by the `RC_LOCK_BITS` side-metadata spec and
   `LOG_BYTES_PER_RC_LOCK_BIT`. HEAD has `attempt_to_log_field` and **no** `RC_LOCK_BITS`.
   `[verified]` HEAD appears to have replaced the lock-bit protocol with a CAS on the log bit itself.
   `[inferred]` — I did not confirm this by execution.
5. **PLDI's `slow()` returns `Result<ObjectReference, ()>` from the log helpers; HEAD returns
   `Result<Option<ObjectReference>, ()>`** — an explicit null-target case rather than a sentinel.
   `[verified]`

### Semantic or cosmetic

**Cosmetic for the algorithm; semantic for the interface.** Item 4 is the one item that could be
semantic, and it is flagged `[inferred]`: P0.3 should resolve whether HEAD dropped a lock protocol
that PLDI needed, or whether PLDI's lock was superseded by a correct single-CAS.

### Observed behaviour — a caution about the PLDI implementation

The PLDI build's crashes (`P0.1-benchmarks.md` §7) fault **inside this mechanism**:
`FieldLoggingBarrier::write_barrier`, alternating with `ImmixConcurrentTraceObjects::do_work` (§5) —
the producer and the consumer of the shared decs/SATB buffer described above.

This is not evidence that the barrier *algorithm* is wrong; PLDI is a research snapshot whose own CI
never exercised LXR (§16.5). But it does mean the PLDI barrier implementation **cannot be treated as
a known-good executable reference**. Implement the algorithm as described here, and validate against
**HEAD**, which runs the full benchmark set cleanly. It also raises the priority of resolving item 4
above: a dropped lock protocol is exactly the kind of change that would produce a rare race between
these two sites.

### Which revision should a .NET port follow, and why

**HEAD**, for the interface. `VM::VMSlot` is exactly the abstraction a CoreCLR port requires: CoreCLR
slots are not uniformly "an address holding a full pointer" (there are interior pointers, byrefs, and
— if the port ever wants them — compressed forms). Porting PLDI's raw-`Address` barrier would bake in
an assumption that has to be undone later.

But **read PLDI's `mmtkFieldLoggingBarrier.hpp` for the fast path.** It is 13.6 KB of header versus
HEAD's 3.5 KB, because at PLDI the C1/C2/interpreter fast-path emission lives in the header.
`[verified]` That fast path — the inline unlogged-bit test that runs on every reference store — is
the single most performance-critical piece of the entire port and the part that must be re-expressed
as JIT-emitted code in RyuJIT rather than as a runtime call.

---

## 5. SATB concurrent marking

### Location

| | PLDI | HEAD |
|---|---|---|
| implementation | `src/util/cm.rs` (`ProcessModBufSATB`) | `src/plan/lxr/gc_work/tracing.rs` (815 lines) for LXR; `src/plan/concurrent/concurrent_marking_work.rs` (309 lines) for the generic plan |
| enable flag | `args.rs: CONCURRENT_MARKING = cfg!(feature = "ix_concurrent_marking")` | runtime |
| interface | none — inline in the Immix plan | `src/plan/concurrent/mod.rs` (35 lines) + `src/plan/concurrent/global.rs` (10 lines) |

`[verified]`

Files mentioning concurrent marking:

- PLDI: `policy/largeobjectspace.rs`, `args.rs`, `util/rc.rs`, `lib.rs`, `plan/immix/global.rs`,
  `plan/barriers.rs` `[verified]`
- HEAD: `lib.rs`, `plan/concurrent/{immix/mutator,immix/global,barrier,mod}.rs`,
  `plan/lxr/global.rs`, `vm/collection.rs` `[verified]`

### What changed

HEAD **extracted a reusable concurrent-marking framework** and made a second plan (ConcurrentImmix)
use it. LXR at HEAD keeps its own `ProcessModBufSATB` in `plan/lxr/gc_work/tracing.rs`, *distinct
from* the one in `plan/concurrent/concurrent_marking_work.rs`. `[verified]` — the two barrier files
import different `ProcessModBufSATB` types (`super::gc_work::tracing::ProcessModBufSATB` vs
`super::concurrent_marking_work::ProcessModBufSATB`).

The scheduler side did change materially — see §12.

`vm/collection.rs` appearing in HEAD's list is the VM-facing hook for concurrent work, which PLDI
does not have. `[verified]`

### Semantic or cosmetic

**Semantic at the framework level, largely unchanged at the LXR level.** LXR still owns its own SATB
tracing; what is new is that the *scheduler* now has first-class concurrent buckets to run it in.

### Which revision should a .NET port follow, and why

**Split, deliberately, and this is the one place I recommend a split within a broadly HEAD-leaning
port:**

- **LXR's SATB semantics: PLDI.** It is the paper's, it is unchanged at HEAD, and it is readable in
  one place.
- **The scheduler contract that runs it: HEAD** (§12). PLDI has no concurrent bucket; it cannot
  express "run this work while mutators are running" without the `Concurrent` /
  `ConcurrentResumable` / `FinishConcurrentWork` triple.

These are **not** the same mechanism, so this is not a coupled-set violation. §15 records the
coupling explicitly.

**Porting note.** CoreCLR already has a suspend-EE / restart-EE protocol and a GC-safe-point
mechanism; the `vm/collection.rs` hook is where that maps. This is the largest single piece of
generic runtime capability the project will need.

---

## 6. Nursery sweeping

### Location

| | PLDI | HEAD |
|---|---|---|
| | inline in `policy/immix/immixspace.rs` (1246 lines) and `plan/immix/global.rs` (1025 lines) | `src/plan/lxr/gc_work/nursery_sweeping.rs` (65 lines) |

`[verified]` — no PLDI file of that name exists.

### What changed

Extraction to a named 65-line file. The mechanism — after an RC pause, blocks whose lines are all
dead are returned; blocks with surviving lines become reusable — is the same. `[inferred]`: I did not
diff the logic line by line, because there is no PLDI counterpart file to diff against.

### Semantic or cosmetic

**Structural.** Flagged for P0.3 as *not fully verified*.

### Which revision should a .NET port follow, and why

**HEAD.** A named, 65-line unit is portable; a fragment interleaved into a 1246-line `immixspace.rs`
is not. If P0.3 finds a semantic delta, revisit — but the default should be HEAD's factoring.

---

## 7. Mature sweeping / cycle collection

### Location

| | PLDI | HEAD |
|---|---|---|
| | inline in `plan/immix/global.rs` + `util/cm.rs` | `src/plan/lxr/gc_work/mature_sweeping.rs` (155 lines) |

`[verified]`

### What changed

Same story as §6: extraction. This is the mechanism that reclaims cycles — the part RC alone cannot
do, and the reason LXR needs the SATB trace at all.

Relevant PLDI knobs: `LAZY_MU_REUSE_BLOCK_SWEEPING = true`,
`NO_LAZY_SWEEP_WHEN_STW_CANNOT_RELEASE_ENOUGH_MEMORY = false`, `CYCLE_TRIGGER_THRESHOLD = 1024`.
`[verified]` HEAD keeps `NO_LAZY_SWEEP_WHEN_STW_CANNOT_RELEASE_ENOUGH_MEMORY = false` and
`CYCLE_TRIGGER_THRESHOLD = 1024` but **drops `LAZY_MU_REUSE_BLOCK_SWEEPING`** and
**drops `HOLE_COUNTING`**. `[verified]`

HEAD adds a dedicated `STWRCDecsAndSweep` work bucket (§12). `[verified]`

### Semantic or cosmetic

**Structural, with two dropped knobs.** The dropped knobs were `true`/feature-gated at PLDI; whether
their behaviour became unconditional or was removed is `[inferred]` and is a P0.3 question.

### Which revision should a .NET port follow, and why

**HEAD**, coupled to §6 and §12 (see §15).

---

## 8. Mature evacuation and defrag

### Location

| | PLDI | HEAD |
|---|---|---|
| driver | `policy/immix/defrag.rs` (205 lines) | `policy/immix/defrag.rs` (228 lines) |
| LXR evac | inline in `plan/immix/` | `plan/lxr/mature_evac.rs` (286) + `plan/lxr/gc_work/mature_evac.rs` (213) |

`[verified]`

### What changed

- PLDI gates mature evacuation on `lxr_mature_evacuation`, which itself requires
  `lxr_nursery_evacuation` — i.e. **mature evac cannot exist without nursery evac**. `[verified]`
  HEAD inverts this to `RC_MATURE_EVACUATION = !cfg!(feature = "lxr_no_mature_evac")`. `[verified]`
- PLDI has `COUNT_BYTES_FOR_MATURE_EVAC`, `RC_DONT_EVACUATE_NURSERY_IN_RECYCLED_LINES`,
  `lxr_delayed_nursery_evacuation`, `lxr_evacuate_nursery_in_recycled_lines`,
  `max_mature_defrag_percent: 15`. `[verified]` HEAD retains none of the first four as named args.
  `[verified]`
- HEAD adds `INC_MAX_COPY_DEPTH = false` and a prefetch family (`PREFETCH`, `PREFETCH_HEADER`,
  `PREFETCH_MARK`, `PREFETCH_RC = false`, `PREFETCH_STEP = 8`). `[verified]`
- `defrag.rs` grew 205 → 228 lines. `[verified]`

### Semantic or cosmetic

**Semantic (tuning).** The evacuation *policy* — which blocks are selected, and the remembered-set
discipline that makes mature evacuation safe under RC — is where four years of divergence
concentrates.

### Which revision should a .NET port follow, and why

**HEAD**, with a caveat. HEAD's policy is better tuned and its defaults are the "everything on"
configuration. But **the paper's numbers were produced by PLDI's policy**, so any attempt to
reproduce the paper's evacuation results must use PLDI. Record this as a *known divergence* rather
than pretending the two agree.

**This is the mechanism most affected by the fact that PLDI could not be benchmarked (§16).** The
choice above rests on code reading.

---

## 9. Immix block and line metadata

### Location

`src/policy/immix/{block,line,immixspace,defrag,mod}.rs` — the only path-stable set. `[verified]`

| file | PLDI | HEAD | delta |
|---|---|---|---|
| `block.rs` | 944 | 868 | −76 |
| `line.rs` | 379 | 340 | −39 |
| `immixspace.rs` | 1246 | 1676 | **+430** |
| `defrag.rs` | 205 | 228 | +23 |
| `chunk.rs` | 508 | **absent** | moved to `util/heap/chunk_map.rs` |
| `mod.rs` | 44 | 26 | −18 |

`[verified]`

### What changed

`block.rs` and `line.rs` shrank because LXR-specific state moved out to `plan/lxr/`; `immixspace.rs`
grew because it absorbed the `ImmixHooks` abstraction (`immixspace.rs:46`) that makes that possible.
`[verified]`

Both revisions keep the same block/line size knobs: PLDI has `log_block_size: 15`,
`log_line_size: 8` (32 KB blocks, 256 B lines) — **observed at runtime**, and a feature family
`lxr_block_{16k..1m}`. `[verified]`

### Semantic or cosmetic

**Semantic (shrinkage by relocation).**

### Which revision should a .NET port follow, and why

**PLDI, for reading; HEAD, for structure.** PLDI's `block.rs` is where the LXR block state machine
(`Unallocated`/`Unmarked`/`Marked`/`Reusing`, the hole counter, the RC-table range accessors) is
visible in one place. Port HEAD's layering, but derive the state machine from PLDI's `block.rs`,
which is *coupled* to §3 (RC) — see §15.

---

## 10. Block allocation

### Location

| | PLDI | HEAD |
|---|---|---|
| | `policy/immix/block_allocation.rs`, **330 lines** | `plan/lxr/block_allocation.rs`, **102 lines** |

`[verified]` — moved by mmtk-core commit `e6e7d2eb` *"move BlockAllocation to plan/lxr behind
ImmixHooks trait"*. `[verified]`

### What changed

A 330-line policy-level allocator became a 102-line plan-level one, with the policy-side
responsibilities exposed through `ImmixHooks` (`policy/immix/immixspace.rs:46`). `[verified]`

PLDI's `args.rs` shows the associated knobs: `LOCK_FREE_BLOCK_ALLOCATION` (feature
`ix_lock_free_block_allocation`, observed **true** at runtime), `lock_free_blocks: 64`,
`ignore_reusing_blocks: true`, `disable_mutator_line_reusing: false`. `[verified]`

### Semantic or cosmetic

**Structural (relocation), with a 3× reduction that suggests real simplification.** Whether
behaviour changed is `[inferred]`.

### Which revision should a .NET port follow, and why

**HEAD.** The `ImmixHooks` split is precisely the layering a port wants: a reusable Immix space plus
an LXR-specific allocation policy. Building the port on PLDI's fused version would have to be undone.

---

## 11. Object forwarding and side metadata

### Location

`src/util/object_forwarding.rs`: PLDI 259 lines, HEAD 274. `src/util/metadata/side_metadata/`:
PLDI 8 files, HEAD 9. `[verified]`

### What changed

Very little. Forwarding-pointer install/read via the header-word protocol is the same shape on both;
the +15 lines and +1 file are upstream MMTk evolution.

The one LXR-relevant point: **PLDI has a `RC_LOCK_BITS` side-metadata spec that HEAD does not**
(§4 item 4). `[verified]`

### Semantic or cosmetic

**Cosmetic**, except for `RC_LOCK_BITS`, which is accounted under §4.

### Which revision should a .NET port follow, and why

**HEAD**, but this mechanism will be **rewritten wholesale** for CoreCLR regardless. MMTk's side
metadata assumes a contiguous mmap-able address-space policy that CoreCLR's regions/segments layout
does not share. Treat both revisions as informative, not normative.

---

## 12. Work bucket / scheduler

### Location

`src/scheduler/work_bucket.rs`: **PLDI 308 lines, HEAD 458 lines**. `[verified]`

### What changed — this is the clearest semantic delta in the whole comparison

PLDI's `WorkBucketStage`:

```
Unconstrained, FinishConcurrentWork, Initial, Prepare, Closure, RefClosure,
CalculateForwarding, RefForwarding, Compact, Release, Final
```

HEAD's adds, among others:

```
Concurrent, ConcurrentResumable,          // new concurrent execution buckets
TPinningClosure, PinningRootsTrace,       // pinning support
SoftRefClosure, WeakRefClosure,
FinalRefClosure, PhantomRefClosure,       // RefClosure split four ways
VMRefClosure, VMRefForwarding,            // VM-delegated weak handling
SecondRoots, FinalizableForwarding,
ClearVOBits,                              // (feature-gated)
STWRCDecsAndSweep                         // LXR-specific
```

`[verified]`

Three of these matter enormously for a .NET port:

1. **`Concurrent` / `ConcurrentResumable`.** HEAD's own comment: *"Users can disable this bucket and
   cache some concurrent work during STW, and only enable this bucket and allow concurrent execution
   once a STW is done."* `[verified]` PLDI has only `FinishConcurrentWork` — it can *drain*
   concurrent work at a pause but has no first-class bucket to *run* it in.
2. **`TPinningClosure` / `PinningRootsTrace`.** Transitively-pinning and non-transitively-pinning
   roots. **CoreCLR requires this**: pinned handles, `fixed` statements, and interior pointers from
   unmanaged frames all demand that some objects not move while their referents may. PLDI has **no**
   pinning support in the bucket set at all.
3. **`STWRCDecsAndSweep`.** LXR's own stop-the-world decrement-and-sweep phase promoted to a named
   bucket. `[verified]`

### Semantic or cosmetic

**Strongly semantic.**

### Which revision should a .NET port follow, and why

**HEAD, without qualification.** Points 1 and 2 are not optional for CoreCLR. A port built on PLDI's
bucket set would have to invent both, and inventing a pinning protocol is exactly the kind of
"GC-side workaround" the parity contract forbids.

---

## 13. Object model and the binding

### Location

`mmtk-openjdk/mmtk/src/object_model.rs`: PLDI 172 lines, HEAD 147. `[verified]`

`openjdk/barriers/`:

| PLDI | HEAD |
|---|---|
| `mmtkFieldLoggingBarrier.{cpp,hpp}` | `mmtkFieldBarrier.{cpp,hpp}` |
| `mmtkObjectBarrier.{cpp,hpp}` | `mmtkObjectBarrier.{cpp,hpp}` |
| `mmtkNoBarrier.hpp` | `mmtkNoBarrier.hpp` |
| — | `mmtkSATBBarrier.{cpp,hpp}` |
| — | `mmtkUnlogBitBarrier.{cpp,hpp}` |

`[verified]`

`BarrierSelector`: PLDI `{NoBarrier, ObjectBarrier, FieldLoggingBarrier}`; HEAD
`{NoBarrier, ObjectBarrier, FieldBarrier, SATBBarrier}`. `[verified]`

Note PLDI implements `const PartialEq for BarrierSelector` — a const-trait impl, which is *why*
PLDI requires a 2021-era nightly toolchain. `[verified]` HEAD uses a plain `const fn`. This is a
concrete, mechanical reason the two revisions cannot share a toolchain.

### Notable

Both revisions **disable compressed oops unconditionally**: `thirdPartyHeapArguments.cpp:46` does
`FLAG_SET_DEFAULT(UseCompressedOops, false)` and `mmtkHeap.cpp:78` asserts
`!UseCompressedOops`. `[verified]` So neither reference revision has ever run LXR with a compressed
reference representation.

### Semantic or cosmetic

**Semantic**, but OpenJDK-specific.

### Which revision should a .NET port follow, and why

**Neither directly.** CoreCLR needs its own object model shim. The *finding that matters* is the
compressed-oops one: **LXR has never been validated against compressed references**, so if the
CoreCLR port ever wants a compressed-pointer mode, it is unexplored territory in the reference and
must be treated as new work, not as a port. This belongs in the P0.3 ledger as a risk row with no
oracle.

---

## 14. Runtime args (`src/args.rs`)

### Location

`src/args.rs` at the crate root on **both** revisions: PLDI 287 lines, HEAD 193. `[verified]`

### What changed — defaults inverted

| knob | PLDI | HEAD |
|---|---|---|
| lazy decrements | `cfg!(feature = "lxr_lazy_decrements")` — **opt in** | `!cfg!(feature = "lxr_no_lazy")` — **opt out** |
| nursery evacuation | `cfg!(feature = "lxr_nursery_evacuation")` | `!cfg!(feature = "lxr_no_nursery_evac")` |
| mature evacuation | `cfg!(feature = "lxr_mature_evacuation")` | `!cfg!(feature = "lxr_no_mature_evac")` |

`[verified]`

Other deltas:

- `CM_LARGE_ARRAY_OPTIMIZATION`: PLDI **`true`**, HEAD **`false`**. `[verified]` A hard-coded
  behavioural flip — worth a P0.3 row of its own.
- HEAD gains a `RuntimeArgs` struct with `init()` / `get()` (`args.rs:72–80`) — a runtime-mutable
  arg block that PLDI does not have. `[verified]`
- HEAD gains `validate_features(active_barrier, options)` (`args.rs:191`). `[verified]`
- Dropped at HEAD: `HEAP_HEALTH_GUIDED_GC`, `HOLE_COUNTING`, `LXR_RC_ONLY`,
  `NO_RC_PAUSES_DURING_CONCURRENT_MARKING`, `ENABLE_INITIAL_ALLOC_LIMIT`,
  `RC_DONT_EVACUATE_NURSERY_IN_RECYCLED_LINES`, `COUNT_BYTES_FOR_MATURE_EVAC`,
  `ENABLE_NON_TEMPORAL_MEMSET`, `LOG_PER_GC_STATE`, `LOG_STAGES`, `HARNESS_PRETTY_PRINT`,
  `BARRIER_MEASUREMENT`. `[verified]`
- Added at HEAD: the prefetch family, `INC_MAX_COPY_DEPTH`. `[verified]`

### Semantic or cosmetic

**Semantic.** The inversion encodes a real decision: at PLDI, plain RC is the base case and the
paper's mechanisms are additions; at HEAD, full LXR is the base case and the features carve pieces
*out*.

### Which revision should a .NET port follow, and why

**HEAD.** Full LXR is the target; opt-out defaults match that. But **keep PLDI's opt-in names as the
staged bring-up plan** — `lxr_basic` → `+lxr_lazy` → `+lxr_cm` → `+lxr_evac` is a ready-made
four-milestone sequence for the port, and each stage is independently runnable in the reference.

Also: **`HARNESS_PRETTY_PRINT`, `LOG_PER_GC_STATE` and `LOG_STAGES` exist only at PLDI**, and
`log_gc` is not symmetric across the revisions. This is why the benchmark harness for this step had
to observe pauses from the *mutator* side rather than parse GC logs (§16).

---

## 15. Coupled mechanism sets

Per the parity contract, mechanisms inside one set must share an oracle.

| Set | Members | Shared oracle | Why coupled |
|---|---|---|---|
| **A. RC core** | §3 RC, §9 block/line metadata, the RC-table and straddle-line specs in §11 | **PLDI** | The RC bit layout, the block's RC-table range accessors, and straddle-line marking are one data structure viewed three ways. Mixing widths or table granularities across revisions is silently corrupting. |
| **B. Barrier + SATB buffer** | §4 field barrier, §5 LXR SATB semantics | **PLDI semantics, HEAD interface** | The decs buffer *is* the SATB buffer. The buffer's producer and both consumers must agree. The `Address`→`VMSlot` change is interface-only and does not cross the semantic boundary. |
| **C. Concurrency substrate** | §5 scheduler contract, §12 work buckets, §6 nursery sweeping, §7 mature sweeping | **HEAD** | The sweeps are scheduled *into* the buckets; `STWRCDecsAndSweep` exists only at HEAD and is where mature sweeping runs. |
| **D. Evacuation** | §8 mature evac + defrag, §10 block allocation | **HEAD** | Defrag selects blocks that the allocator must then honour; the `ImmixHooks` split changed both sides together. |
| **E. Configuration** | §2 plan identity, §14 args | **HEAD** | Runtime plan selection and runtime args are the same change. |

**Set B is the one that needs care.** It takes semantics from PLDI and interface from HEAD. That is
defensible only because §4 established the algorithm is *unchanged* — if P0.3 finds the
`RC_LOCK_BITS` removal was semantic, set B must collapse to a single oracle.

---

## 16. Honest limits of this document

These are the things a reader must not assume I verified.

1. **The PLDI revision could only be partially benchmarked.** It runs 5 of 8 DaCapo 2006 benchmarks
   at one iteration and crashes under sustained execution; see `P0.1-benchmarks.md` §7.
   Recommendations in §5 (PLDI SATB semantics) and §8 (evacuation policy) therefore rest
   substantially on **code reading**, not on observed behaviour. HEAD runs 8 of 8 cleanly, so it is
   the only oracle that can be *observed* across the whole set.

2. **No line-by-line diff was taken of the extracted files** (§6, §7, §10). There is no PLDI file to
   diff against; the claim "same mechanism, relocated" is `[inferred]` from names, sizes and call
   sites. P0.3 should confirm each by reading PLDI's `immixspace.rs`/`global.rs` against HEAD's
   extracted files.

3. **The `RC_LOCK_BITS` removal (§4 item 4) is not explained.** I verified the spec exists at PLDI
   and not at HEAD; I did not establish what replaced it.

4. **`CM_LARGE_ARRAY_OPTIMIZATION` flipping `true`→`false` is unexplained** (§14).

5. **Neither revision's LXR path is covered by the reference's own CI.** PLDI's
   `ci-test-normal.sh` tests SemiSpace, Immix and GenImmix only; HEAD's `normal-heap.yml` lists nine
   plans, none of them LXR. `[verified]` So "the reference works" is not something CI attests to for
   LXR on either revision — a materially important fact for calibrating how much the reference can be
   trusted as an oracle.

6. **Version skew.** Neither task-named mmtk-core revision is the one its binding pins:

   | | binding | binding's pinned mmtk-core | task oracle | relationship |
   |---|---|---|---|---|
   | PLDI | `abbdd1d` (2022-04-12) | `df8d30a3` (**2022-03-14**) | `4d4e516c` (2022-04-08) | the oracle is **newer** than the pin |
   | HEAD | `0682434` (2026-04-22) | `304ce69d` (2026-04-22) | `9625c174` (2026-05-06) | the oracle is 9 commits **ahead** of the pin |

   `[verified]` Both bindings nonetheless compile cleanly against their task-named oracle
   (`cargo check` exit 0 in both cases), so this document compares the oracles, not the pins.

---

## 17. What P0.3 should do with this

1. Turn §1's summary table into ledger rows, carrying the oracle choice and reason forward verbatim.
2. Treat §15's coupled sets as ledger constraints, not annotations.
3. Resolve the four `[inferred]` items in §16 (2)–(4) before any of the affected mechanisms is
   implemented.
4. Add a no-oracle risk row for compressed references (§13).
5. Re-derive, for CoreCLR: `LOG_MIN_OBJECT_SIZE`, the RC-table granularity, the block and line sizes,
   and the header-word layout used by object forwarding. Every one of them is an OpenJDK constant in
   both references.
