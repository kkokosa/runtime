<!-- lxr-gc-roadmap:v1 -->
# LXR GC implementation roadmap

This roadmap tracks a greenfield port of LXR, described in Zhao, Blackburn, and McKinley's
[Low-Latency, High-Throughput Garbage Collection](https://arxiv.org/abs/2210.17175), to CoreCLR as a
standalone GC plus a set of generic runtime capabilities. The reference implementation is checked out at
`C:\github\lxr-reference`: [`mmtk-core`](https://github.com/wenyuzhao/mmtk-core) and
[`mmtk-openjdk`](https://github.com/wenyuzhao/mmtk-openjdk).

Two revisions are in scope as parity oracles. The oracle basis is the **binding-pinned pair** — the configuration
you get by checking out the binding and building it with no override: PLDI is binding `abbdd1d` (tag
`lxr-pldi-2022`) resolving core `df8d30a3`, and head is binding `0682434` (branch `lxr`) resolving core
`304ce69d`. This is deliberately *not* the core tag `4d4e516c` nor the core branch head `9625c174`: the binding
does not reference either, so building them requires a manual override, whereas the pinned pair is what a reader
of the paper reproduces without intervention. Since a multi-year port will be rebuilt by many future sessions,
reproducibility-without-intervention is the deciding property. The oracle is chosen **per mechanism** between the
two pairs and recorded in the P0.3 ledger with its reason.

The core tag and branch head were also built and measured, and are **behaviorally indistinguishable** from their
pinned counterparts: P0.1 found agreement in all 24 antlr/`-n 5` cells and identical pass sets across the DaCapo
matrix on both revisions. The choice above is therefore provenance, not behavior, and **every P0.1 number is
valid under either reading**. Those runs are retained as labelled secondary evidence and never share a table with
oracle numbers.

Beware the author date when reasoning about these revisions: `df8d30a3` was authored 2022-03-14 but committed
2022-04-10 after a rebase, so it is 9 commits **ahead** of `4d4e516c` despite appearing older; `9625c174` is 9
commits ahead of `304ce69d`. The PLDI nine are inert specifically under `--features lxr` — verified mechanically,
not just empirically: `014368ef` changes `LAZY_MU_REUSE_BLOCK_SWEEPING` from `true` to `cfg!(feature =
"lxr_lazy")`, which still evaluates true because `lxr` implies `lxr_lazy`, and `518e9bc2`/`c2657a63` gate
`max_copy` on `!cfg!(feature = "lxr")` so the else branch is byte-identical. "stw LXR" names the stop-the-world
variants, a different configuration. They nevertheless contain `rc_bits`, which makes `LOG_REF_COUNT_BITS`
feature-selectable (`lxr_rc_bits_{2,4,8}`) while preserving the default — so **RC width is a designed tunable,
default 2 bits with `MAX_REF_COUNT == 3`, and a port must parameterize it rather than hardcode it.** The head
nine are structural and not inert: the `plan/lxr` relocation trio, the concurrent-plan rework, the upstream
concurrent-marking interface switch, and bucket states, at 32 files and +1357/-1575.

`lxr-x/simplified` is explicitly **excluded**: it is a reduced fork (+1568/-2216 across 19 files versus
`lxr`) and measuring parity against it is a defect.

Full parity with the reference is a requirement, not an aspiration. Runtime changes are permitted and
preferred over GC-side approximation, but only as generic capability extensions for which LXR is the first
consumer rather than the definition.

## Operating rules

1. A step becomes `done` only in a separate verification pass after implementation, and only when the cited reference mechanism is present with the same ordering and transition provenance. Statuses are `planned`, `in_progress`, `done`, and `failed`; `in_progress` is set automatically when the Implement button spawns a child session, and is what prevents a second session being spawned for the same step.
2. Similar observable behavior is not acceptance evidence. Approximations, heuristics, and GC-side workarounds do not substitute for a reference mechanism; if a mechanism cannot be reproduced, the step fails rather than silently degrading.
3. Every mechanism declares which oracle it follows (`pldi-2022` = binding `abbdd1d` + core `df8d30a3`, or `lxr-head` = binding `0682434` + core `304ce69d`) and why; coupled mechanisms must share one oracle, because mixing revisions inside a coupled set produces a hybrid that is self-consistent nowhere. Where a claim was read from the core tag `4d4e516c` or branch head `9625c174` instead, the row says so — those builds are behaviorally indistinguishable from the pinned pairs but are not the declared basis.
4. Sweeping never parses objects. Nursery and mature reclamation read only the RC table, line/straddle metadata, and mark bits, walking at 8-byte granularity; any design that requires object-by-object heap parsing during a sweep is rejected.
5. Runtime changes follow R1-R5: generalize the mechanism not the client; zero measured cost when unused, by init-time selection rather than branching; additive and version-gated; independently defensible without LXR; performance evidence per change.
6. Every performance checkpoint compares the same binaries, machine state, workload input, duration, warmup, and run count across LXR, Workstation GC, and Server GC; pin DATAS, heap count/limit, concurrency, tiering, PGO, and ReadyToRun, then interleave collectors across invocations.
7. Report failures and excluded runs. Never select only successful LXR runs when a workload is flaky.
8. No code, status, or measurement is inherited from the abandoned `C:\github\runtimelab` prototype.
9. `src/plan/lxr/...` paths in the References fields below are **head-only**. At the PLDI oracle there is no LXR plan at all: LXR is a compile-time feature configuration of the Immix plan (`lxr = ["lxr_basic", "lxr_cm", "lxr_lazy", "lxr_evac"]` over `ix_ref_count`/`ix_concurrent_marking`), with `args.rs` at the crate root, block allocation under `policy/immix/`, concurrent marking in `util/cm.rs`, and the barrier in `mmtkFieldLoggingBarrier.cpp`. A missing PLDI path therefore never means a missing mechanism, and cross-revision comparison is mechanism-to-mechanism, never path-to-path. The compile-time-feature to runtime-`MMTK_PLAN` shift between the two oracles is itself a portability-relevant difference, since .NET selects a standalone GC at init time.
10. Never trust a reference build's exit code as evidence of what it built. `CompileThirdPartyHeap.gmk` expands `GC_FEATURES=--features $(MMTK_PLAN)` into `CARGO_BUILD_FEATURES = --features $(GC_FEATURES)`, so setting `MMTK_PLAN` emits `cargo build --features --features lxr`, which is silently ignored and yields **NoGC while appearing to be LXR**. Pass `GC_FEATURES=lxr,immix` with `MMTK_PLAN` unset — `immix` is required because the binding's `lib.rs` has no `lxr` case and the default `PlanSelector` is NoGC. Confirm the emitted `cargo build` line and the runtime banner (`barrier: FieldLoggingBarrier`, `ix_ref_count`, `lxr_*` features) before believing any measurement. Two further traps: the reference's own CI runs DaCapo at `-Xms500M -Xmx500M`, and the MarkCompact minheap table describes MarkCompact rather than LXR, so undersized heaps mimic total breakage; and under WSL `ulimit -c 0` does not stop the `core_pattern` capture pipe turning each SIGSEGV into a ~155 s stall that looks like a hang (`echo core > /proc/sys/kernel/core_pattern`).
11. Classify every reference claim by how it was obtained: `[obs-oracle]` observed on a shipped pair, `[obs-override]` observed on a build that never shipped, `[read-only]` read but never executed. Order revisions with `git merge-base`, never by date — `df8d30a3` was rebased and its author date reverses the true topology. Scan **all** log directories before asserting a clean run; a release build with `cfg(debug_assertions)` checks compiled out carries the same corrupt state silently, so absence of a panic in release is not absence of a violation. A fix-shaped commit message does not imply the fix applies to your configuration: expand the feature gates before attributing a behavior change to a commit, and confirm with a differential run rather than inference.
12. Read the artifact, never a proxy for it. This failure mode has now recurred three times across P0.1 and P0.2: a TLS failure against the public PyPI CDN was generalized into "`pip install` is blocked" without ever running the command, which also produced "no PDF rasterizer exists" from the absence of standalone binaries when the `pymupdf` package renders in-process and made the step's own rendered-page check look impossible; a positional column index silently read `SATB%` in place of `!Lazy%` on rows with a different cell count; and Table 6's column membership was argued from prose when the page could simply be rendered and read. Never record a capability as unavailable without running it to completion and pasting the failure, never index a table column by position where row shapes vary, and prefer rendering a PDF page over trusting extracted text for anything structural. A claim about an artifact must cite the artifact — including one's own prior reports, which are artifacts too. P0.3 saw a correction issued against a child session's entirely accurate statement because the coordinator recalled which commit it had audited rather than re-reading its own transcript; the child settled it by fingerprinting the two trees (`redirectBlock=0` at `080ae02120b`, `1` at `5712425e3ed`). Check your own record before contradicting someone about what is in it, or the discipline is only ever pointed outward.
13. Absence of a check is not evidence of an invariant. The two oracles do not carry the same assertions: at PLDI the RC invariant is asserted in the write barrier slow path (`src/plan/barriers.rs:315-319`, condition at `:316`, gated `cfg(any(feature = "sanity", debug_assertions))`), while at `304ce69d` no barrier-side check exists and the only `zero rc count` assertion lives in `src/util/sanity/sanity_checker.rs:311-316` (condition at `:312`) behind `cfg(feature = "sanity")` — a non-default feature the build recipe never enabled. Both of those spans were first cited here one line off, at `:317` and `:313`, because grepping for the message literal lands on the format-string line rather than the condition; cite multi-line assertion macros as a span and name the condition line. So "HEAD never trips it" recorded an uncompiled check as a passing one, and validate-against-lxr-head must first re-instate the invariant at the validating revision before a clean run means anything. Before citing a clean run as validation, confirm the check exists and was compiled into the binary that produced it.
14. Cite reference locations at the declared oracle revision, never from the checkout. `C:\github\lxr-reference/mmtk-core` is checked out at the named `9625c174`, not at the declared oracle `304ce69d`, and the two differ in tree shape: `304ce69d` has a flat 9-file `src/plan/lxr/` with `cm.rs`, `gc_work.rs` and `remset.rs`, while `9625c174` has 13 files with a `gc_work/` subdirectory plus `block_allocation.rs`. A path read from the working tree can therefore name a file that does not exist at the oracle. All four revisions exist as objects in that clone, so cite with `git show <rev>:<path>` in `<rev>:<path>` form. A bare `:NNN` shorthand is only as good as the last full path before it: P0.3 wrote `plan/lxr/global.rs:707`/`:708` without the `src/` prefix, so the extractor bound `:708` to the preceding full path instead — which resolved, to a `cfg(feature = "vo_bit")` line. Shorthand that silently rebinds still resolves, so keep a full path with every citation that starts a new file.
15. A citation is not evidence unless the cited code is compiled in the configuration under discussion. Locating text at a line proves only that the text is there. P0.3 cited `plan/immix/global.rs:532-534` and `plan/lxr/global.rs:377-379` as both oracles ignoring `System.gc()`; both are `cfg(feature = "nogc_no_zeroing")`, a feature in neither oracle's default set and never enabled by the build recipe, so neither line is compiled. The live paths are `memory_manager.rs:686` to `mmtk.rs:436` to `util/heap/gc_trigger.rs:143` at HEAD, which sets the flag and requests the collection, and at PLDI the trait default `plan/global.rs:590-598`, whose body is entirely commented out. Dead code that reads correctly is the sharpest form of this trap, and a mechanical citation checker cannot catch it because the text resolves. Establish that a cited line is reachable in the built configuration before treating it as behavior, and state the configuration alongside the citation.
16. A verification tool must audit the artifact it ships with, and must be run against committed content. `scripts/verify-ledger.sh:18` defaulted its document root to an absolute path inside one worktree, so running the committed script from a clean `git archive` extract silently audited a different, mid-edit checkout — reporting a failure the committed tree did not have, and equally able to report a pass it had not earned. P9.1 is specified to lean on this gate, so an auditor running it from a fresh clone would either fail on an absent path or attest to a tree they are not looking at. Derive default inputs from `${BASH_SOURCE[0]}` so a script always checks the tree it ships in, let an explicit argument override, and derive expected counts from the document instead of repeating a literal at several assertion sites. Two corollaries: run the gate on extracted committed content rather than a working tree before treating its exit code as evidence, and remember that an `i/lf` script checked out `w/crlf` cannot be executed by bash at all, so a pass obtained from a converted copy attests to the copy rather than to the artifact.
17. A control that has not been shown to fire is indistinguishable from an absent one. P0.3 had to reject three controls that reported success while testing nothing: a reach counter whose abort threshold was an order of magnitude too high; a gate control that deleted row `E03`, which this ledger does not contain, so its `sed` matched nothing; and a gate control that perturbed its own input tree in place and then re-ran the gate on the tree it had just broken. This is the tooling-side twin of rule 13 — absence of a failure signal is evidence only once that signal has been demonstrated. Every check that gates a claim ships with a demonstration that it fails when the thing it checks is broken: perturb a throwaway copy rather than the input, assert the perturbation actually applied before trusting either verdict, and require the pair (clean pass, perturbed fail) rather than the pass alone. A control must also fire *reliably*, and P0.4 supplied the counter-example: its barrier-scale control asserted that a ~2% injected slowdown was resolved, then reached that target at n=8, at n=15, and not at all across three runs of the same binary on the same host, with the injected effect once measuring `0.9906` against a null replicate of `0.9912`. Direct probing outside the harness returned ratios of `1.0221`, `0.9649` and `0.9922` — a 2% signal under a ±3% spread, with the sign flipping — so the flake was the machine, not the harness. Gating a verdict on a signal below the measurement floor makes the outcome track ambient load, which is as useless as a control that never fires and worse in one respect: it teaches the next reader that the route to green is to rerun or loosen the threshold, which is choosing the acceptance criterion from the result. Split such a control rather than tuning it — assert on a magnitude the environment cannot miss (`0.8122 [0.8023, 0.8220]` for a ~16.7% injection), pair it with a null replicate so an over-confident estimator cannot pass by manufacturing an effect (`1.0085 [0.9978, 1.0195]`), and keep publishing the marginal measurement unasserted so the information survives without the flake. Name the blind band this leaves: between the asserted coarse magnitude and the unasserted fine one, a resolution regression fails nothing, so consider a deliberately generous bound on the fine arm chosen the same way the coarse one was.
18. Verify a hash, then push that hash. Worktrees share branch refs, so `git push origin <branch>` resolves the ref at push time rather than at verification time. P0.3 was audited at `0ab50bf3d04` and PR #3 was opened on `814a1967985`, because the child session committed in the seconds between the `git archive` and the push — putting a commit into a pull request that the verification statement in its body did not cover. The cost of a stale read is not the wasted time, it is the false attestation. This is also the only failure this project has hit that no single participant could see: every earlier instance was a claim about the wrong artifact inside one process, whereas here one session moved a shared ref between another session's two reads. Prefer the form that is correct by construction over the one that is correct by inspection — `git push origin <sha>:refs/heads/<branch>` makes the verified object and the pushed object the same by definition, and if the ref has moved since, it pushes less than the branch or is rejected outright, never more and never something unread. Re-reading the remote head afterwards is the weaker fallback because it detects the problem instead of preventing it. Name the verified hash in the PR body so a later reader can tell what the claim covers, and prefer attestations that can be re-checked over ones that must be trusted: a hash in the body can be compared against the head by anyone at any time, whereas a ref-relative phrase such as "this branch's head" reads as true at every future moment and silently covers whatever is there then. That was the form this rule's own pull request took on the first attempt, written an hour after the rule. The same applies in reverse when auditing a child: read its tip before grepping, since a correction may already be committed above the commit in hand.

19. A squash merge does not preserve the commit you verified, so the verification has to be repeated against what actually landed. PR #3 was verified and attested at `814a1967985`, but merging squashed it into `8a63e85178b`, a commit with a single parent whose ancestry does not contain the verified hash at all. `git merge-base --is-ancestor 814a1967985 origin/main` answers no. The tree happened to be byte-identical, but that is a fact to be checked rather than assumed, and once the branch is deleted the attested object may become unreachable entirely — leaving a pull request body naming a hash that no longer exists in the repository, which is an attestation a later reader cannot check against anything. This is the mirror image of rule 18: there the ref moved under a fixed verification, here the verification stayed fixed while the object it named was replaced. Treat the merge commit, not the branch tip, as the artifact a step's closure evidence refers to; re-run the gate against an extract of the merge commit and record that hash in the roadmap. A step is not done because its pull request is merged, it is done because the merged artifact was shown to pass.

20. Re-read the state you are gated on before reporting that you are still blocked on it. P0.3 sat unmarked for two hours after PR #3 merged because the gate was "waiting on the merge" and nothing prompted a re-read; the answer to "why is this not done" was that it was done and had not been looked at. This is rule 12 in its laziest form — the artifact was one command away and the stale proxy was my own last report. A blocked item carries an obligation to re-check its unblocking condition, not merely to wait for someone to announce it.

21. A claim about code you cannot read is a guess, and a moral drawn from a fabricated failure is worse than no moral. The P0.4 child reported that a benchmark file emitted without the `checkpoints` wrapper "parses without error and contributes nothing to the board — the quietest possible failure", and drew from it the lesson that one must read the artifact rather than a description of it. The lesson is right; the evidence for it was invented. The canvas is not committed on `main` and was absent from that worktree, so the claim was reasoning about unreadable code, and it is false: `store.mjs` merged such a file through `document.checkpoints ?? [document]`, so the flattened record *did* load and *did* chart. Writing that exact file and reading it back through the running canvas settled it in one command. The fix shipped alongside the claim was still correct, because the wrapper is the documented shape — but a correct fix resting on a mechanism that does not exist teaches the next reader to defend against the wrong thing, and an invented failure mode is not made harmless by an accurate remedy. The check paid for itself twice: that same tolerant fallback turned a file with a mistyped key into a checkpoint with no `date`, whereupon the sort comparator threw on `left.date.localeCompare` and **one malformed agent-produced file blanked the entire board**, phases and steps included — a real defect, in my own code, found only by testing someone else's wrong claim instead of accepting it. Files arriving from agents will be malformed sometimes, so per-file isolation, a required `schemaVersion`, and a named reason for every ignored file are now part of the contract rather than politeness. Verifying a claim you expect to confirm is how you find the bug that neither of you was looking for. Two refinements came back from the child session that are sharper than the correction itself, and both are the durable part. First, **the invented failure was milder than the truth**: "silently dropped" is the safer of the two behaviours, so the argument for the fix ran backwards — the real defect was that an unvalidated record was *charted as a real measurement*, and a fabricated failure mode understating severity is more dangerous than one overstating it, because it sets the remedy's urgency too low. Second, **a test of your own producer can only ever confirm your own producer.** The unit test asserting the flattened shape is rejected passed the entire time the claim was false, and could not have caught it: it exercised the emitter, while the claim was about a consumer outside the repository. Contract claims need the real consumer in the loop — rendering the file is what settled it. Where a consumer cannot be run, the honest form is "the documented shape is X, so emit X", which needs no behavioural claim at all.

22. Correct a false claim in place rather than rewriting it away, and re-verify at the corrected hash. The claim in rule 21 was already committed, pushed and cited in PR #4's body when it was found to be false. Deleting the sentence would have left a document that was correct and a history that silently disagreed with the numbers a reader had already seen; instead the section now states what it claimed, that the claim was false, and what the artifact actually does. The mechanical obligations that follow are easy to skip once the prose is fixed: the tip moved to `f5f1c6ed8b3`, so the gate was re-run from a fresh extract of *that* commit (`PASS (87 checks)`), the branch was pushed by hash rather than by ref, and the pull request body — which named the old hash in an attestation sentence — was rewritten to name the new one. An attestation naming a superseded hash is worse than none, because it reads as verified while covering a tree that no longer exists at that ref. Fixing the artifact and leaving the attestation stale converts a caught error into an uncaught one.

23. A search that cannot match is indistinguishable from an absence, and reports as one. Rule 15 says a hit may be meaningless because the text is not compiled; this is its inverse and it is the more dangerous half, because a miss produces no artifact to inspect and closes the question instead of opening it. Three instances in this effort, all different mechanisms and all reading as clean negatives: the P0.3 `zero rc count` spans were recorded a line off because grepping a message literal lands on the format-string line rather than the condition, so the probe hit the wrong thing while appearing to hit the right one; a HEAD comparison was believed to have run when its assertion sat behind a non-default `sanity` feature and was compiled out, so the search for failures returned nothing because nothing could fail; and a grep of the pushed commit messages for a claim known to be there returned empty because pipe-separated alternatives were passed to PowerShell's `-SimpleMatch`, which searches for the literal string `a|b|c` and can never match. That last one nearly became an accusation that a correct report was itself unverified — a null result laundering into a positive claim about someone else's work. Before believing a negative, prove the probe can fire: run it against a case known to match, or invert it and confirm the hit you expect. This is what control `[E]` does for the verification gate, asserting that a section emptied of checks is detected rather than silently passing, and it is the same obligation applied to the searches used to build the argument in the first place.

24. A coordinator that commits onto a child's branch has invalidated the child's picture of its own tree, and owes it the notice it would demand in return. The P0.4 child apologised for landing `5160725dcdd` while a fresh-extract verification of `d78a43684e3` was in flight — correctly, since "harmless this time" is a property of the delta and not of the practice. Within the hour the coordinator did the same thing to it in the opposite direction, committing the §10 correction `f5f1c6ed8b3` onto `kkokosa-p0-4-benchmark-harness` from a different worktree, after having raised the very complaint. The child's next report named `5160725dcdd` as its tip and was wrong about its own branch, and no amount of care on its side could have prevented that. The rule that follows is symmetric with rule 20: whoever moves a ref tells whoever is reading it, before the next exchange rather than after. Two details worth keeping because they cut in opposite directions. The child attributed a branch *rename* to the same interference; the reflog shows it renamed `kkokosa-crispy-robot` to `kkokosa-p0-4-benchmark-harness` itself at 09:56:24, nearly three hours before its own first commit — so it apologised for the right failure while misattributing half its cause, which is rule 23 again, a confident account of state nobody had re-read. And it doubted the coordinator's "our trees agree" and checked rather than deferring, which is the behaviour worth having: it was wrong that time, and being willing to be wrong that way is what makes its agreement worth anything.

25. A gate that audits an artifact against itself cannot catch the artifact lying about the code, and every capability a document claims is the cheapest possible assertion. `P0.4-harness.md` states at line 87 that P0.4 "implements the mechanism and a `calibrate` mode that bisects for a scenario's minimum viable heap", and repeats at line 881 that "the `calibrate` mechanism ships". Neither is true: `RunnerOptions.cs:71` accepts `matrix`, `controls`, `conformance` and `hosts`, and no `Calibrate` symbol exists anywhere in the harness. That document passed a gate run five ways at three different hashes, from clean `git archive` extracts, with the check count deliberately investigated when it moved between 71 and 87 — and none of it could ever have caught this, because every check compared the document to itself or to the results beside it. Grepping the source for the verb the document claims to ship is one line and would have failed instantly; the gate contains twenty-three lines mentioning `scenario` and **zero** mentioning `heap` or `calibrat`. The general form is that self-consistency is not correspondence, and the checks worth adding are the ones that cross the boundary between the document and the thing it describes. The same blindness hid a second defect of a different kind: `MatrixRunner.Run` calls `MatrixPlanner.Expand` with seven arguments, omitting the optional eighth `heapLimitForScenario`, so `MatrixCell.HeapLimitMb` is unconditionally `null`, the guard at `MatrixRunner.cs:242` never fires, and `System.GC.HeapHardLimit` is never set — while `:249` still passes `--heap-factor` through to the worker and `RunResult.cs:257` still publishes it. Running the paper's `{1.3, 2.0, 6.0}` would therefore have emitted three identically-configured unpinned runs under three different heap labels. It published nothing false only because nothing exercised it: all 528 `heapFactor` fields across the 474 preserved P0.4 result files are `null`, against 528 non-null `scenario` fields by the same method, so the trap was armed and never sprung. A latent fabrication mechanism is not a smaller bug than a realised one; it is the same bug waiting for its first user, and its first user was the step that had to trust it.

26. Prefer a probe that shows its evidence to one that returns a verdict, because a verdict hides its own misfires. Checking that claim about `heapFactor` began with `"heapFactor"\s*:\s*(?!null)`, intended to find non-null values and expected to return nothing. It returned five hundred and twenty-eight matches — every one of them a line reading `"heapFactor": null`, the exact text the lookahead was written to exclude. The cause is ordinary regex backtracking: `\s*` matches zero characters, the lookahead is then evaluated against ` null` rather than `null`, and it succeeds. Had that probe been written to emit a count or a boolean, it would have reported non-null heap factors in the preserved evidence — a fabricated defect in a merged artifact, produced by a search that ran cleanly and was read too quickly. It was caught for one reason only: `Select-String` prints the matched line, and the matched lines visibly said `null`. This is the counterpart to rule 23 rather than a repeat of it — 23 is a probe that cannot fire reading as an absence, this is a probe that fires wrongly reading as a finding — and the same discipline answers both: state what the probe should return before running it, make it print what it matched, and replace a clever exclusion with two counts and a subtraction whenever the question is "are there any". The rewritten check counted all occurrences and null occurrences separately and ran the identical method against a field known to be populated, which is what turned an assertion into a measurement. The habit paid again twice in one session: a preservation filter whose `-and`/`-ne` precedence was wrong copied 143 files where roughly 1,600 were expected, caught only because the count was compared against a prior inventory rather than accepted; and a `Select-String -Pattern 'FAIL'` over a passing test run reported one hit, which was the summary line `0 failed`, because PowerShell's matching is case-insensitive by default. Neither would have surfaced from a green tick.

27. A successful write is not a successful publish; confirm your own content reached the reader. The P0.5 roadmap entry was written with `Evidence`, `Corrections carried` and `Open` fields, the file saved correctly, the markdown was valid, the board reloaded, and the canvas reported `problems: 0` — while all three fields were discarded in silence. `parseRoadmap` called `parseField` once per known field name and simply never looked for anything else, so an unrecognised `- **Name:** value` line had no reader and produced no complaint. This was found only by querying the parsed state for text that had just been authored, which is a cheap habit and the only thing that distinguishes "written" from "published". It is the same failure the project already legislated against for agent-supplied benchmark files, where every ignored file must carry a named reason, and it had been sitting in the sibling code path the whole time — a contract enforced on other people's inputs and not on one's own. The parser now reads every field generically, promotes the known ones, and carries unknown ones through to the modal as labelled blocks, so a future step inventing a field gets it displayed rather than deleted; the fix was verified in both directions through a real click on a real card, one step showing its evidence and its two extra blocks and another correctly hiding an absent evidence block. The general rule is that a pipeline with a silent drop in the middle looks identical to one with nothing to carry, and the author is the only person positioned to notice the difference.

28. A gate that verifies data against data does not verify prose against data, and the sentence is where the claim actually lives. `P0.5-baselines.md:543` states that all four throughput ratios in the s2/s3 comparison drift by less than the 8.35% floor; `raw/arm-ratio-stability-s2-vs-s3.csv` — committed in the same directory, in the same commit — puts `long-lived-cache` at **8.744%**. It is three of four. The gate passed thirty checks over that sentence, and could not have done otherwise: it re-derives the arm-ratio CSV with `compare-arm-ratios.py`, compares committed rows to recomputed rows, and asserts the row *count* with `wc -l`, so the data was checked exhaustively and the paragraph describing it was never read. This is rule 25's twin — 25 is the artifact lying about the code, this is the artifact lying about its own data — and it is more insidious, because the false sentence and the refuting number ship in the same commit and neither is wrong in isolation. It propagated: the child asserted it to the coordinator, the coordinator repeated it verbatim in the PR body posted under the user's account and again in this roadmap, and it survived three readings by two parties because everyone who saw the number was reading a different artifact from everyone who saw the sentence. The conclusion it decorated was never in danger — `long-lived-cache`'s ratio is still 3.3× more stable than its absolute, and all four rows carry `ratioMoreStableThanAbsolute = true` — which is the trap: a false sentence attached to a true finding attracts no scrutiny at all. Two practices follow. Every quantified prose claim — "all N", "N of M", any comparison against a threshold — is a check the gate can run against the CSV, and should. And a table nobody can regenerate is one nobody can audit: §6.4 ships no generator, so recomputing it required reverse-engineering that its cells partition by `runId` and not by the `mode` field, and partitioning the obvious way returns 53 cells at 1.792% against a published 59 at 1.60% — a discrepancy that reads exactly like a defect and is not one. Publish the generator alongside the table, or the next reader's correct method is indistinguishable from their mistake.

29. A measurement rule that names one step will be orphaned by the step that follows it; project-wide constraints belong in the rules, not in a carry-forward. P0.5 concluded that LXR must be compared against a baseline arm measured in the same session, and expressed it as "P0.6 onward must" — in a document with no P0.6, whose next step was P1.1, a runtime-enablement step that measures nothing. The coordinator copied that phrasing into this roadmap and into the pull request body without checking that the addressee existed, which is rule 23's shape applied to a cross-reference rather than to code: a confident statement about a thing nobody had looked up. Two rules follow, and both are now recorded here rather than in a step. **First, every LXR benchmark carries its own baseline arm from the same session.** Between two sessions on this host the same throughput cell moved by a median of 12.15% and a maximum of 28.49%, while the within-session `srv`/`wks` ratio drifted 7.28% median against 19.06% for the corresponding absolutes — quoting the 28% alone against those medians compares a worst case with two typical cases, which is the kind of mismatch this project exists to catch. A baseline table read months later and compared against a fresh LXR number is not evidence. **Second, the shared heap limit moves when LXR joins.** The arithmetic is derived, stated and gated in `P0.5-baselines.md` §5.1, and is deliberately *not* restated here beyond the load-bearing fact that `srv` binds every calibrated row, so every absolute limit P0.5 publishes is a function of the Server arm's minimum alone. Adding a third collector makes the shared minimum `max(wks, srv, lxr)`, so for any scenario where LXR needs more than `srv` the cell's absolute limit rises and both existing arms land somewhere else on their own heap-size curves. That is not drift and the ratio rule does not repair it; it means P0.5's absolute tables cannot serve as a comparison arm across the LXR boundary for any cell whose limit moves, and the first step to measure LXR must recalibrate the shared minimum with LXR included and re-measure all three arms at the new limits — including when LXR's minimum turns out *lower* than Server's everywhere, since that negative result also has to be established by a run rather than assumed. The pointer rather than a copy is deliberate and was earned: this rule previously carried its own transcription of the Workstation and Server ranges, the document carried the same numbers in §5.1, **and both were false in the same way** — Workstation's minimum was given as a flat 4 MiB against a Server range of 35–43, which is true of exactly the four rows where Workstation sits at 4 and false of the ten-row matrix, where Workstation ranges over 4, 11, 19, 35 and 99 against Server's 35 to 139. Correcting the copy on this board left the copy in the document still false. A fact worth stating twice is a fact that will be wrong in one place and right in the other, and the reader has no way to tell which. The nominal label already describes the two existing arms differently — at nominal 1.3x, `srv` sits at 1.30–1.31x of its own minimum while `wks` sits at 1.83–14.00x, and at 6.0x `wks` reaches 64.5x — and since the paper's own throughput result inverts with heap generosity at 0.97, 0.96 and 1.01, this is a confound in precisely the dimension the port is judged on.

30. Two measurements that agree are corroboration only if they could have disagreed; methods sharing a degenerate failure mode agree perfectly and read as confirmation. Verifying the child's claim that its scripts are committed LF-only, a PowerShell probe counted carriage returns and reported non-zero for all fourteen. The obvious explanation was PowerShell's own CRLF conversion, so the check was re-run through bash on the raw blob — and returned *exactly the same fourteen numbers*, which was taken as evidence that the first result stood and the scripts were CRLF. Both were wrong. The bash script had itself been written with CRLF line endings, so `$'\r'` was mangled into a pattern that matched every line, and `grep -c` returned the line count; the PowerShell form had materialized the blob as text, introducing the very CRs it then counted, which also equals the line count. Two independent routes to the same wrong answer, agreeing to the digit. The tell was there to be read and was not: every file's "CR count" equalled its line count exactly, a coincidence across fourteen files of differing size that no real measurement produces. What settled it was refusing to compare the two suspect methods against each other and instead comparing each against something with a known answer — `od -c` on the raw bytes showing `\n`, git's own `git ls-files --eol` reporting `i/lf w/lf` for all fourteen, `tr -cd '\r' | wc -c` returning zero, and a three-line all-LF control file that the buggy pattern duly reported as containing three CRs. The claim under test had been true from the start; the verification invented the defect. So when two methods agree, ask what they share before treating it as independent confirmation, check the suspicious number against a quantity it should *not* equal, and run the method itself against an input whose answer is already known — the same control-input discipline the gate-of-the-gate applies to gates, turned on one's own probes. Agreement is the weakest evidence available when both parties can be wrong the same way.

31. Name the command that verifies an artifact, because the conventional one may pass without running anything. The P0.4 harness deliberately carries no `PackageReference` — so that it restores with no feed and runs on CoreRun, where there is no package resolution — which makes `Lxr.Harness.Tests` an `OutputType=Exe` assertion runner with no test adapter. `dotnet test` against it **exits 0 having run nothing**: five lines of restore output, no warning, no `0 tests`, no indication of any kind that no test executed. Running the assembly directly prints `58 passed, 0 failed`. Both were reproduced against the repository SDK rather than reasoned about, and the two transcripts are indistinguishable to anyone reading an exit code. This is rule 23's shape — a probe that cannot fire, reading as a pass — but it earns its own entry because it is not a mistake anyone made, it is a permanent property of a correct design decision, and it sits directly under the sentence "58 harness unit tests pass" in a pull request body published under the user's account. A reviewer doing exactly the right thing, reaching for the standard command to check a stated result, would have been told the result was confirmed. The rule generalises past this harness: whenever the natural verification command is not the correct one, the artifact must say so at the point it makes the claim, because a claim and its verification method are one statement and publishing the first without the second invites a false confirmation. The trap also inverts and both directions must be checked — `dotnet test` here fails on an SDK that cannot target the harness's framework, which looks like a broken test suite and is not, so the same paragraph must name the SDK the command needs.

32. A quantified claim inherits the population that was in view when it was written, silently, and a claim that is true of a subset is not a weaker error than one that is false — it is a harder one, because every number in it checks out. Three instances landed in a single step, in three different sections, written by two different parties. §6.4's CV table partitions its cells by `runId`; partitioning the obvious way, by the `mode` field, returns 53 cells at 1.792% against a published 59 at 1.60%, and the coordinator nearly reported that as a defect. §6.5's medians were re-derived by the implementer's own checker over the *disagreeing* subset and came back 17.94% and 18.16% against a published 14.48% and 16.19% — two more false claims, nearly reported, both published figures correct and the derivation wrong. And §5.1 shipped "Workstation never binds anywhere in this matrix. Its minimum is 4 MiB where Server needs 35–43", whose first sentence holds at 30 of 30 and whose second is false of the matrix: `wks` minima take five distinct values — 4, 11, 19, 35, 99 — and `srv` spans 35 to 139. Over the four rows where `wks` is exactly 4, `srv` is exactly {35, 43}. The sentence is a true statement about four rows wearing the grammar of a statement about ten. The coordinator committed the same error in the same exchange from the other direction, calling `large-object-pressure` "the one row that fails exact multiplication" when all ten fail it — a comparison tolerance had quietly defined a population of one and the conclusion was reported as though it described the matrix, so a universal was published as a singleton. What catches this is never the number, which is correct; it is a second sentence that cannot coexist with the first, and §5.1 supplied one twenty-one lines later by putting `wks` at 99. Two defences, both cheap. Every quantified claim names its population in the sentence — "across all 16 cells", "in 30 of 30 rows", "over the four scenarios where" — so that the subset reading and the whole-population reading stop being equally grammatical. And a checker that derives an expected value must derive it over a population it names and print that population beside the result, because a checker silently choosing the wrong subset agrees with wrong prose enthusiastically and reports a pass. Note what all four instances have in common: each decorated a conclusion that was true and survived the correction intact, which is precisely why nobody looked. A fourth variant arrived later in the same step and is worth separating, because it is the partition rather than the population that slips: a correction offered to this coordinator decomposed eighteen changed harness files as "5 new, 12 modified, 1 `.csproj`", which totals correctly while moving the project file out of the modified bucket it belongs to — the true split is 5 added and 13 modified, the `.csproj` being one of the thirteen. The conclusion resting on it was right and useful, that one modified file is the test program itself so 58 assertions guard 16 product sources rather than 18. **A correct total is not evidence of a correct partition**, and a decomposition that sums to the right answer is the easiest kind to accept without checking — especially when it arrives as someone else's correction of you, which is the one direction in which scrutiny is hardest to sustain.

33. A count stated as a literal beside a count that is computed will diverge, and the artifact will keep printing both. `verify-gate-of-the-gate.sh` closes by printing `RESULT: PASS (%d of %d cases behaved as required)` from live counters and then, on the very next line, the fixed string "The gate passed a clean extract, refused an empty one, and failed five single perturbations." At the commit where that sentence was written it was true. Two commits later the script ran ten cases: two clean extracts, one refusal, and **seven** perturbations — and it reported "10 of 10" and "five" in adjacent lines of the same output, one derived and one asserted, with nothing to reconcile them. The location is what makes it worth a rule: this is the control script, the artifact whose entire purpose is to prove the gate detects claims that data does not support, and it was making one. A hardcoded number is a second place for a fact to live, and the second place is never updated by the change that invalidates it — the same reason the prose checkers in this step compute every expected value at run time rather than storing it, applied to output rather than to input. Either derive the summary from the same counters that produced the verdict, or state no numbers in it at all; prose that restates a computed result in words is not documentation, it is an unversioned copy. The follow-up made the case better than the original did. Asked to derive the number or drop it, the author instead **incremented it** — "five" became "six" when a ninth case was added — and it remained wrong by exactly the same two, because the count had already missed that one of the cases is a refusal rather than a perturbation and that the script passes two clean extracts rather than one. Incrementing a literal preserves whatever error the literal already carried; only recomputing removes it, and the reason to recompute is precisely that nobody rereads the sentence they are adjusting. The stale number had been correct once, at the commit where it was written, which is the condition that makes this class survive review: every reader who checks it against the moment it was authored finds it sound.

34. A correction and the text it corrects live under opposite rules, and a fix written in the wrong one of the two is invisible to the gate. Rule 22 requires a false claim to stay visible with its refutation adjacent, so the document keeps the original sentence inside a blockquote. Rule 28 requires every quantified claim to be re-derived from the shipped data, and a checker can only anchor on text that asserts something — which the quoted false sentence, by design, does not. The two collide at the exact point they are both doing their job: the corrected figures were first written *inside* the rule-22 quote, next to the wrong ones, where they read perfectly to a human and where the anchors deliberately cannot see them. A correction placed there is unchecked, and unchecked is the state the original defect shipped in. So the discipline is positional: false text stays in the quote, the true replacement goes in live prose outside it, and the anchor is written against the live sentence. This generalises past blockquotes to anywhere a document quotes itself — evidence sections that reproduce a claim verbatim to discuss it are the other common case, and they cut the opposite way, since a whole-file replace aimed at the live claim will hit the quoted copy instead. Both directions were observed in one step: a perturbation case broke because the section documenting it quoted the string it perturbs, and a correction was nearly shipped ungated because it was written beside the sentence it replaced. When a document discusses its own claims, the position of a sentence is part of its meaning, and no checker can recover that from the text alone.

35. Two parties fixing the same defect in parallel will produce different fixes, and the better artifact is not reliably the later one, the longer one, or one's own. The coordinator, told by the implementing session that it had made no changes, wrote and pushed a correction to §5.1; the session had already written its own, unpushed. Both corrected the same false sentence, both derived the replacement from the CSV rather than transcribing it, and both replaced the control's hardcoded count with a computed one. **Neither dominated.** The session's gated the divisibility mechanism, which the coordinator's corrected in prose and left ungated — the weaker half of a commit whose entire argument is that an ungated correction is how the original defect shipped. Its `heap-arm-spread` derived four figures against two, so the four-row subpopulation that the false sentence had actually described became a checked number rather than a claim about a claim. And its helper deduplicated by scenario with the reason in its docstring, the achieved CSV carrying one row per (scenario, factor), so that counting rows would report fifteen distinct values and read as a document defect — rule 32 applied to the checker rather than only to the prose. The coordinator's carried one thing the session's could not, having been written first: binding counted as *equalling the shared minimum* rather than strictly exceeding the other arm, because `SharedMinimumMb` is a `Math.Max` and on a tie both arms bind, so a strict `>` reports a **true** document as false the first time the arms agree. There are no ties in this matrix, so both definitions return 30 and the divergence is invisible — until a third collector arrives and makes ties ordinary. The resolution was to take the better half of each and say in the commit message which half came from where, rather than to pick a winner and lose the rest. Two things follow. Reconcile by reading both diffs against each other before choosing, because "mine, plus whatever theirs has that mine lacks" is a different and better commit than either; and when a competing fix arrives, the parts of it that are stronger than yours are the parts you will be least inclined to look for. The cause was the coordinator's own: a report that nothing had changed was read as a fact about the present rather than a description of a moment, which is the same error as acting on a hash last read some time ago — a rule this coordinator wrote and had twice enforced against the session.

36. A perturbation battery tests only the perturbations someone thought of, so "I watched this check fail" is a weaker guarantee than it sounds. `heap-arm-spread` expected the workstation value set as the literal substring `4, 11, 19, 35, 99`. It was written, anchored, perturbed and watched failing before it was trusted — the perturbation altered a value, `99` to `98`, and the gate duly exited 1 naming only that claim. What nobody tried was **adding** one. A document reading `4, 11, 19, 35, 99, 250 MiB` contains the expected substring, so **a superset passes a check for the set**, and the gate returned exit 0 with `PASS (12 prose claims)` over a document asserting a minimum that does not exist in the matrix. Confirmed by running the same falsified document against both commits rather than by argument: passing at one, failing at the next. Two properties of this make it general. The check was authored in the very commit whose thesis is that an ungated correction is how the original defect ships, by the party that had just documented the expected-3-matching-3.3x class twice — and the same commit reintroduced a bare count, `5`, satisfied by the `4` heading the list it counts, which is that class a third time. Familiarity with a defect class does not confer immunity to it. And the failing perturbation was *representative-looking*: substitution feels like the natural way to falsify a list, so the battery reached a comfortable stopping point while an entire direction of falsification went untested. The defence is to enumerate the *shapes* a claim can be wrong in before choosing perturbations — for a set, at minimum: a changed member, a removed member, an added member, a reordering; for a count, a value too high and too low; for a range, each endpoint independently and the pair inverted — and to prefer expectations that cannot be satisfied by a superstring at all, binding the value to the punctuation or the words that introduce it. Note the asymmetry that makes this worth its own entry rather than a note under rule 28: a check too *strict* announces itself immediately by failing a correct document, and a check too *loose* is silent forever. Every defect in this class found so far has been a loose one.

37. A fact stated twice is gated in one place and unchecked in the other, and the gate cannot tell you which. `find_anchor` returns the **first** non-blockquote match, so a second statement of a gated fact is invisible to the checker by construction — no amount of care writing the check would catch it, because the check passes on the first copy and never looks further. Section 7's carry-forward restated ``srv`` binds in 30 of 30 cells and the arms tie in 0 of 30, both facts section 5.1 states and the prose checker verifies, phrased so the anchor does not match them. Confirmed by running it rather than by argument: falsifying section 7's copy alone, leaving section 5.1 correct, yields `RESULT: PASS (34 checks)` at exit 0. Gating both copies is the wrong repair, and this is the part worth remembering — **a fact stated twice can be right in one place and wrong in the other, with nothing in the artifact able to tell a reader which**, so the duplicate is removed rather than checked: the second statement cites the first and carries no count of its own. Half the duplicate had arrived in the commit that removed a *definitional* duplicate, which is the shape of the thing — the repair of one restatement introduced another. The rule needs a limit or it bans all repetition and guts the evidence sections: a section narrating a defect quotes the counts it came from, and an evidence section reproduces tool output verbatim; both quote a fact rather than assert it. The distinguishing question is whether a reader would take the sentence **as the source of the number**. Where they would, it must be the only place that states it. Related to rule 28 but not a case of it: rule 28 is about a claim no check reads, while this is about a claim a check reads *somewhere else*, which is worse because the gate reports green with specific authority.

38. A check whose correctness depends on a condition the shipped data does not contain cannot be validated against that data — the condition has to be built. `SharedMinimumMb` is a `Math.Max`, so on a tie both arms attain the maximum and a check written around strict `srv > wks` reports a *true* document false. The matrix has **0 ties in 30 rows**, so every perturbation of the shipped CSVs agrees with every perturbation of the wrong definition, and no amount of perturbing the real data distinguishes them. I established this by reasoning from the source and was right; the implementer established it by **constructing the tie** — raising one scenario's Workstation minimum to its Server value, holding `sharedMinimumMb` fixed so the formula claims stayed true by construction, and re-running: strict gives `srv = 27 of 30` while attainment gives `srv 30, wks 3, ties 3`, with exactly the two right checks failing and the binding-count check correctly not failing. The reasoning and the construction reached the same answer, and only one of them is evidence. That is the ungated-correction pattern one level up: a correct fix shipped with nothing showing it is correct. The durable defence is to make the two readings visible in every run rather than trusting that they agree — the checker now prints `strict would give srv=N` beside the attainment count, so the day they diverge is the day it is on screen. **This is not hypothetical for P0.6**: the maximum becomes a three-arm one the moment LXR is added, at which point ties stop being rare, and any check written around strict inequality will fail on true text and read as a prose defect. Second half, from the same commit and the same root: an anchor pinned to a *derived* number has a failure direction worse than rule 33's. Anchoring on `All ten converged` or `take five distinct values` means that when the data moves, **correcting** the document makes the anchor stop matching, so the checker reports the claim **deleted** rather than **changed** — the one edit that should reassure you is the one that looks like sabotage. Anchors must be literal-free: bind to the words that introduce a value, never to the value.

39. A count is not a claim until its unit is stated, and two true counts in different units read as a contradiction. §9 described P0.4's harness as "5 new sources, 12 modified ones, and one `.csproj`" while `git diff --name-status` reports **13 modified**. I published that as a stated count disagreeing with the tool. It was not: `12` counts modified *sources* and `13` counts modified *files*, the `.csproj` being the difference — every number in the original sentence is true, and 5 + 12 + 1 = 18 is the correct total. The real defect was that three labels read as disjoint buckets when two overlap, so the sentence licenses a false inference without asserting a false number. The implementer disputed my framing and was right, and the distinction is load-bearing rather than pedantic: **rule 32's defect is a number false of the population it names, which a checker can re-derive; this one has nothing to re-derive**, since every figure recomputes correctly, so prose-vs-data verification is structurally incapable of catching it and a rule built from rule 32's pattern will not extend to it. The defence is different in kind — state the unit beside the count and make overlapping buckets name their overlap ("13 modified, of which one is a `.csproj` and one is the test file") rather than listing exclusions beside the total. Twice in one step I read a units mismatch as a disagreement: this, and a `MinimumForArm` audit where my line-level count said 1 and the canvas's occurrence-level count said 2, which looked for half a minute like a parse defect and was one line containing the identifier twice. Both times the numbers were right and only the units were missing, and both times my first instinct was to report a defect rather than to ask what was being counted.

40. A verification recipe correct for one artifact can be structurally wrong for another, and the wrong answer arrives as a refusal that reads as a failure. My standing method is to verify from a `git archive` extract outside any worktree, so that what is checked is the committed tree rather than my working directory. That is right for the gate and **impossible** for the control: `verify-gate-of-the-gate.sh` performs its own `git archive` of the committed tree, so it needs a checkout to archive *from*, and in an extract it exits 2 with `not a git checkout, or no git that can read it`. Both the implementer and I hit this independently, each initially reading exit 2 as a failing control on a passing gate. The script is behaving correctly — it refuses rather than passing vacuously, which is the design rule 25 asks for — but a refusal and a failure are the same colour to someone applying a recipe by habit. The rule generalises past this instance: **a tool that consumes its own version control cannot be verified by detaching it from version control**, and any check that reads repository state (citations resolved against files, tree hashes, `ls-files --eol`) has the same shape. State the required environment beside each check rather than assuming one recipe covers the suite — the gate takes an extract, the control takes a checkout at the hash being attested. **Second instance, in a different medium, found while validating the rule floor raised for rule 106.** I copied `board-check.ps1` into the session artifacts directory with the floor set one above the board and ran it as a negative control: exit **2**, and no output line matching `floor`. Read as “non-zero, therefore the control fired” it was a clean pass — in fact `$PSScriptRoot` travelled with the copy, `roadmap.md` was never found, and the floor check never executed. Re-run in place it printed `FAIL rule count 106 is below the floor of 107` at exit **1**. The discriminator this rule needs was already being emitted: **2 is a refusal, 1 is a verdict.** So a negative control must assert the specific failing line it expects, never merely a non-zero exit — and the check it validates must distinguish “could not run” from “ran and rejected” in its exit code, which is the property that made this recoverable at all. **Third instance, same shape, one call later.** Probing the canvas host for the board it serves, `@($r.rules).Count` returned **1** against a state payload that has no `rules` field at all — PowerShell wraps `$null` into a one-element array, so a probe that cannot see its subject reports a plausible count instead of nothing. It survived only because 1 is absurd for a 106-rule board; had the expected answer been 1, it would have passed silently and I would have reported the served board as verified. What the three instances share is the generalization worth carrying: **a probe must distinguish “the subject is absent” from “the subject measured zero”**, and neither a non-zero exit code nor an array count does that by default — so the assertion has to name something only a real measurement could produce.

41. `git status` reports agreement with a ref, not agreement with the work, and a branch reset onto a remote is indistinguishable from one that always matched it. Through the late corrections of P0.5 the implementer and I were rewriting the same commit range simultaneously — it amending its tip, me re-implementing its content on top of what I had already pushed. That resolved cleanly and it should not be mistaken for a safe process: it worked **only because the two trees converged**. Had they differed anywhere except the one sentence we happened to fix independently, `reset --hard` onto the pushed ref would have silently discarded whatever the other party had and nothing would have shown it — the losing branch reports clean, the winning branch reports pushed, and the diff that would reveal the loss is one nobody runs because nothing looks wrong. The concrete defence is cheap: **diff the trees before resetting**, and if you intend to re-implement a collaborator's work rather than take their commits, say so, because their default is to keep amending the tip you last read. The same episode supplies the positive form. Two independently-authored sequences — four commits and eight, neither an ancestor of the other — produced byte-identical trees at `9f68f071f29ca01a2cf137a27a62aad39256a10c`. **Convergent tree hashes from disjoint histories are a stronger attestation than either party verifying alone**, because agreement cannot have come from copying a verdict; it is the one form of confirmation in this project that does not depend on trusting a report. Prefer `rev-parse '<rev>^{tree}'` over commit hashes whenever the question is whether two things are the same work, which is also the only way to verify across a squash merge.

42. A checker that derives every expectation from data cannot check a claim about the document's own structure, and such claims should be marked ungated rather than left silently unchecked. §5.1 introduced its list as `Two consequences` while three items followed: the list gained the tie consequence at my request and the lead-in was never touched, so the very section that documents *a stated figure disagreeing with the thing beside it* shipped carrying an instance of it. Gating this is not a small addition — every prose claim re-derives its expected value from a shipped CSV, and counting a list's items is a **second mechanism** inside a checker built around one. The right resolution is to fix the count and record that it is deliberately ungated, because an unchecked claim that is known and labelled is a different object from one nobody has thought about. The general bound: **a gate proves the claims it was built to derive, and its passing says nothing about any other kind of claim in the same document** — lead-ins, list counts, cross-references, type names, and section numbering are all outside it, so a document is never "verified" merely because its gate is green. The companion defect in the same commit was `HeapBaselines.SharedMinimumMb`, a **type** name that names no member: `SharedMinimumMb` is an instance member of `HeapBaseline` while the static `HeapBaselines` holds `LimitMb` and `For(...)`. Identifiers in prose are claims about the tree and nothing re-derives them either.

43. The verification tooling has its own defaults, and a count produced with the wrong one is indistinguishable from a defect in the thing being counted. PowerShell's `-match`, `-like`, `-replace`, `-eq` and `Select-String` are **case-insensitive by default**; only the `-c`-prefixed forms (`-cmatch`, `-creplace`) and `Select-String -CaseSensitive` are not. Counting `Claim\(` in the prose checker returned **13** against a script that derives **12**, and for about a minute that looked like a real discrepancy in a number this project has re-verified repeatedly. It was `derive_floor_claim(` — a function name whose tail matches the constructor when case is ignored. `-cmatch` returns 12 and everything agrees. This matters beyond the one instance because identifiers are the thing most often grepped when verifying a claim about source, and case is exactly what distinguishes a type from a member from a local in every language in this repo, so the default silently merges categories that the code deliberately separates. **Use `-cmatch` for any count of an identifier**, and treat a count that disagrees with a derived value as a suspected defect *in the count* until the matching semantics have been checked — rule 39's discipline applied one level down, to the instrument rather than the measurement. Nearly reporting this as a defect in the implementer's work would have been the same error I had spent the step correcting in it. **The implementer supplied the mirror instance, and I had already committed it without noticing.** Counting the tie value by grepping `0 of 30` returns **15**, because `0 of 30` is a substring of `30 of 30` and thirteen of the hits state the binding-arm count instead; the anchored count is **2**. I ran exactly that grep, printed all fifteen lines under a heading claiming they stated "the tie count in any form", and read past it because I had already isolated the two occurrences I was looking for in the block above. So the defect was not that the grep misled me — it did not — but that I published its output under a description only two lines satisfied. **An unanchored pattern answers a different question from the one asked, and answers it loudly**, which is the same class as the case-insensitive count: the instrument silently widens the category, and a wide answer is more convincing than a narrow one because it has more evidence in it.

44. A subtree hash carries an expensive attestation forward across every commit that does not touch it, which is the cheapest verification instrument this project has. The harness suite is slow to build and run, and P0.5's result was established at one hash with five further commits landing above it. Rather than re-running, `git rev-parse <hash>:docs/design/lxr-port/harness` at each: **`dfc395e68d34` at all six**, so the result attaches to the head without a second run. This is tree-identity from rule 41 applied to a subtree and to a *result* rather than to a comparison, and it answers the question that actually matters after a late fix — not "did the tree change" but "did anything I have evidence about change". It also states the negative precisely: the same command proves those commits touched only `.md` and two scripts, so no reasoning is required about whether a documentation fix could have perturbed a test. Record the subtree hash beside every expensive result, because an attestation without one is pinned to a commit and dies at the next amend. The count itself belongs in the step's evidence field and is not restated here, per rule 37.

45. The harness can only be built by an SDK that is not on the PATH, and it writes its output outside itself when moved. Rule 31 already records that `dotnet test` reports success here without executing anything; these are the two environment facts that sit beside it, each of which cost a turn to rediscover. The projects target `net11.0`, so the machine-wide 10.0 SDK fails them with `NETSDK1045`, and the only SDK that can build them is the one `global.json` pins — which exists on this machine solely inside a session worktree's `.dotnet` directory, not on the PATH and not in Program Files. More consequentially, `Directory.Build.props` derives `ArtifactsPath` from `HarnessRepoRoot`, computed by walking **four levels up** from the harness directory. That is correct in its repository position and silently wrong everywhere else: an extract placed elsewhere writes build output into whatever directory happens to sit four levels above it, and mine landed in the user's home directory. So the extract-based recipe of rule 40 is not merely inapplicable to the harness but **actively unsafe**, and the three defects together — this, rule 31's silent pass, and P0.4's worktree-hostile control script — mean the harness cannot currently be verified by any method this project actually uses. All three are P0.6 work.

46. A battery needs an assertion at **both** ends, because observing only one verdict cannot distinguish a working check from a broken battery. Observing only FAIL cannot tell "the check fired" from "the harness never reached the document"; observing only PASS cannot tell "no defect" from "the perturbation did not apply". The implementer hit the first end for real: invoking the prose checker with `--results` pointing one directory too deep produced **exit 1 with five `FAIL missing input` lines on a completely unperturbed tree**. A battery that scored FAIL as success would have reported four checks firing while the checker had never read the document. I reproduced it and proposed a discriminator — the broken run emits `exit≠0` with ZERO anchored `^RESULT:` lines, a genuinely fired check emits `exit≠0` with exactly ONE `^RESULT: FAIL` — **and the implementer defeated it, correctly.** Point `--doc` at a different but *valid* document with `--results` still correct: every anchor misses, so the checker emits a real verdict line, and my discriminator scores `RESULT: FAIL (12 of 12 prose claims disagree with the data)` as a healthy battery with all twelve checks firing, while it never read the document under test. `--doc` and `--results` are the two arguments of one invocation and they fail in opposite directions. Reproduced at `9f054925372`, five rows from one run: pristine `PASS (12)` exit 0; `--results` one level too deep, exit 1, 0 anchored, 5 `FAIL missing input`; `--doc` nonexistent, exit 1, 0 anchored; `--doc` valid-but-wrong, exit 1, **1 anchored, `12 of 12`**; genuine one-claim perturbation, exit 1, 1 anchored, `1 of 12`. **The guard that covers all five is a quantity, not a presence: assert that the fired count equals the perturbed count.** Zero anchored lines fails "equals one" as surely as `12 of 12` does, so the count subsumes the presence test rather than supplementing it. **The count-equality guard has its own precondition, found by running it on the `:468` probe: it presupposes you perturbed a string you expect to be gated.** Perturbing `:468` yields perturbed 1, fired 0 — which the guard reads as a broken battery and which is in fact the finding, that the occurrence is ungated. When the question is *whether* a string is gated, zero fired is the answer; when the question is whether the battery works, zero fired is the malfunction. Same numbers, and only the intent distinguishes them, so state which question you are asking before reading the count. **Bounded by an exhaustive trace, the shipped gate is immune to both:** `verify-baselines.sh` invokes the checker exactly once, at `:288`, as `--doc "$DOC" --results "$BASE"`, and both derive from `$ROOT` (`:77`, `:78`); the user-supplied `--results` binds to `RESULTS_DIR`, whose every use is locating checkpoint JSON at `:149` and printing the mode. The hazard therefore lives in ad-hoc batteries, which is where both of us met it, and the P0.5 deliverable is unaffected. Two repairs a later reader might wrongly draw from this rule: do **not** stop the gate branching on the checker's exit code at `:288`, because the exit code is the checker's designed interface for a *consumer* and the count matters only for a *battery* deciding whether a check fired; and do **not** thread `--results` through to the prose checker, because prose must be re-derived from the CSVs shipped beside the document rather than from a directory named on the command line.

47. When a check rejects true prose, whether that is a defect turns on **what can reach the false positive**, and the two cases need opposite responses. The arm-spread list check is order-sensitive: a document naming the same five minima in a different order is rejected, which I confirmed at the head — `4, 11, 35, 19, 99` fails while `4, 11, 19, 35, 99` passes, same set, same count. That looks identical in shape to the strict-`>` tie defect, where the checker would also have rejected a true document, and yet the right answers differ. The distinguishing test is whether the false positive **arrives on its own as data moves**: the tie fires when nobody has touched the prose, so it is a defect and had to be fixed; a reordering can only appear if an author deliberately writes a non-canonical rendering, so it is a **canonical-form constraint** and should be documented rather than relaxed. Loosening it would cost the property that makes the check worth having, since an order-insensitive set comparison is a short step from the substring match that let a superset through. Record the constraint where the next author will meet it, because the failure reads as a false alarm to anyone who has not been told it is deliberate.
48. **A duplicate cleared by "the checker does not see it" has been cleared by its own defect.** The P0.5 session audited section 5.1 for the duplication defect we had just removed from section 7, found a second statement of the gated tie count, and cleared it on the grounds that the gate's anchor matches the source line first and the second sentence is a different string. That invisibility is the rule-37 condition itself, not a clearance: a restatement the gate cannot see is precisely the one that goes stale silently while the gated copy fails loudly and gets corrected. The test stays rule 37's — would a reader take this sentence as the source of the number — and it is answered by reading, not by asking what the checker matches. I verified the reasoning rather than the verdict, which is the only reason it surfaced; a negative report is as reviewable as a positive one and deserves the same treatment.
49. **Rank duplicates by whether the fact is predicted to move, and the worst one will be inside the paragraph predicting it.** The restatement removed here sat in the consequence titled "Ties stop being hypothetical" — a paragraph written to warn that P0.6's third arm makes ties real — and the value it restated was the tie count. So the sentence would have gone stale in exactly the scenario it exists to anticipate, and the more the warning is heeded the sooner its own number becomes false. A duplicate of a stable fact is untidy; a duplicate of a fact the document itself forecasts changing is a scheduled defect with a known trigger date. When time is short, grep the restated values against the things the document says will change. **Bounded by the implementer, correctly, on the paragraph that generated it: rank by whether the fact is predicted to move, except where the sentence's assertion *is* the prediction.** Applied naively the rule selects `:468` first — an ungated second occurrence of a gated count, sitting inside the very paragraph forecasting that count will change, which is every condition the rule names at once. It is immune, because staleness is what it asserts: "a checker written around strict inequality reports the *correct* sentence "`srv` is the binding arm in 30 of 30" as false the first time two arms tie". A tie does not falsify that, it demonstrates it. And the repair destroys the sentence, since the quoted count is the grammatical object of "reports as false" — remove it and a checker reports *the correct sentence*, naming nothing. Contrast the restatement actually removed, "the tie count is published **at 0 of 30**", which asserted a present value: same paragraph, same number, opposite exposure. Audited the whole class rather than the one line, because the rule is only worth having if it partitions: `binding arm in 30 of 30` occurs **6** times, and perturbing each shows `:423` fires `FAIL (1 of 12)` while `:468` leaves `PASS (12)`. Of the five ungated, `:468` and `:1064` are prediction sentences and `:984`, `:1003` and `:1094` are §9 narration naming the string as a string — mentioned, not used. **No live defect among them, and the two exempt classes are prediction and citation.** State the bound explicitly, because the sentence most likely to trip the rule is the one that exists to state it.
50. **Resolve a ref at the moment of use rather than from memory — but check the clock before charging anyone with staleness.** I first recorded this rule against the P0.5 session for "re-verifying against `e68c3daa489`, the direct parent of its own fix". The method advice is right; **the attribution was wrong and I withdraw it.** Inter-session delivery was lagging by tens of minutes, and at the moment that check was composed `e68c3daa489` *was* origin — the verification was correct when written and stale only on arrival, which is a property of the channel and not of its method. What survives is the practice that makes a report robust to a slow channel: resolve with `git ls-remote` at the point of use, state the hash you checked against as well as the finding, and prefer a subtree or tree hash, which stays true regardless of when the message lands.
51. **Under delivery lag, the absence of a report is not evidence that none was sent — and a coordinator with filesystem access to a child will always see its work before its words.** I found `3cadda3d54f` by running `git worktree list` during cleanup, read it as an unpushed commit the session had not mentioned, and told it twice that it had lost track of its own work and that its `git status` hazard had turned inward. Its announcement arrived **47 minutes later**, carrying the hash, a three-line diff, gate and control results, and an offer to `git reset --hard` the commit away: it had reported within a minute of authoring. I also recorded that my replies were generating the repetition and instructed it to stop replying — but re-stating findings that cannot be confirmed received is the correct behaviour on a lossy channel, and the session said so in plain terms ("three status reports failed to get them to you"). **Default to "the message is in flight", never to "it concealed the work"**, and keep acknowledgements short and early, because a long one composed while the next report is already being written cannot arrive in time to stop it. This is the third time I have rewritten this theory; each rewrite discarded a true cause on encountering another, which is rule 49's error in a different costume. The queue also drains out of order and keeps draining after a stand-down: I have now received **three** separate messages announcing 3cadda3d54f as unpushed, every one composed before I pushed it and none mentioning the head that superseded it. So verify state and stop — do not answer each arrival as a fresh round, because every answer adds a turn to a queue that is already behind, and the state check costs one command while the reply costs a round trip on both sides. *This rule replaced a different claim under the same number and did not say so.* Its first version read — **“When a child re-sends materially the same message after a correcting reply, the reply is what is producing it”** — a diagnosis of a reply loop that did not exist, withdrawn at `be5bc610e00` in the same commit as rule 50’s staleness charge. Rule 22 requires the original be quoted when a claim is corrected in place; rules 50 and 98 do, this one did not for eleven days, and the omission was found by the history check that rule 103 motivated — the guard caught a real defect on its first run rather than a hypothetical one.
52. **A hash in prose is untyped, and reading a tree as a commit produces a confident false correction.** I published `495a4d5f1b3` as the tree of the commit being pushed. The P0.5 session read it as a commit, found nothing above `3cadda3d54f` on origin, and drafted a note telling me my branch pointer was wrong — then ran `git cat-file -t`, got `tree`, and confirmed `rev-parse 3cadda3d54f^{tree}` matched it exactly. Every git object name lives in one 40-hex namespace, so nothing in the string distinguishes a commit from a tree or a blob, and the failure mode is quiet rather than loud: `rev-parse` resolves it happily, while `log` and `merge-base` reject it in terms that read as "not on this branch" — which is indistinguishable from the branch pointer being wrong. **Run `git cat-file -t` before treating a hash you did not produce as a commit, and name the type when publishing one.** This is rule 39's shape arriving from the other side within minutes of that rule being written: the disagreement was real, and it was a unit error rather than a defect.
53. **A battery's self-check is a checker, and inherits every failure mode of the checks it supervises.** Rule 46 asked *is there a verdict line* where the discriminating fact is *how many fired* — presence substituted for occurrence, which is rule 36's superset satisfying a check for a set and rule 36's bare `5` satisfied by the `4` heading the list it counts, now recurring inside the tool built to catch them. Nothing exempts the supervisory layer: run on it the checks you run on what it supervises. I then supplied the instance from the other end, while testing that very case — `$args` is a PowerShell **automatic variable**, so `function Run($label, $args)` bound nothing, `@args` splatted empty, and all four cases silently ran the same unperturbed invocation and returned an identical `PASS (12 prose claims)`, including two that must fail. Reading only the uniform PASS would have confirmed the defeated rule and shipped it. Count-equality catches this too — perturbed 1, fired 0 — which is a second and independent argument for preferring it: it constrains the result rather than enumerating the ways a harness can break, so it is the one assertion that survives a battery broken in a way nobody anticipated.
54. **A backtick is markdown's code delimiter and PowerShell's escape character, so writing board text through a double-quoted string silently destroys every code span in it.** Appending to P0.6's entry that way produced, from one line, `` `re.IGNORECASE` `` → `<CR>e.IGNORECASE`, `` `find_anchor` `` → `<FF>ind_anchor` and `` `theta` `` → `<TAB>heta`: measured on a 53-character sample, the double-quoted form came back **47 characters with zero backticks and one CR, one FF and one TAB injected**, each control character having eaten the identifier's first letter. Three things make it worse than an ordinary typo. The injected bare CR is a line terminator to `ReadAllLines`, so the line silently splits and `git diff` reports the file as a whole-scale rewrite — 682 insertions against 682 deletions — which sent me looking at line endings, the wrong layer entirely, and `i/lf w/crlf text=auto` all checked out fine. A trailing backtick escapes the closing quote, so the failure sometimes arrives as an unrelated parse error dozens of lines later; that happened twice while trying to demonstrate the effect, which is the demonstration. And nothing about the result looks wrong in a commit message or a summary, so it ships. **Use the `edit` tool for prose, or single quotes and `@' '@` literal here-strings when scripting it**; the four board commits made through the edit tool changed 2, 3, 2 and 6 lines, which is what a correct edit looks like. Caught only because the format contract's trailing-whitespace check ran afterwards and flagged a line ending in a space — a check written for a cosmetic rule catching a content-corruption bug, which is the argument for running the cheap structural checks even when the edit "obviously" cannot have broken them.
55. **A rule written here reaches nobody who needs it: a spawned session reads only its kickoff prompt, so a kickoff must name, by number, the rules its step will exercise.** Every kickoff this project has issued — P0.1 through P0.5 — cited zero rule numbers, because `## Operating rules` sits fifty-odd entries deep in a file a child has no reason to open, and none of them opened it. The cost is measurable in P0.5 alone: the child independently rediscovered the presence-for-occurrence substitution and the unanchored-grep hazard *after* both were written down here as rules 53 and 43, spending a round each on findings it could have been handed for free. Name the numbers rather than paraphrasing them, because a paraphrase is a second copy that goes stale on its own schedule — rule 46 was rewritten twice inside one day, first around count-equality and then bounded with a precondition and with the shipped gate's immunity, so a kickoff that had paraphrased its original form would have handed the child the exact version that same child had already defeated. The general defect is broader than kickoffs: an invariant parked in one step's entry binds that step and nothing after it, which is how both the anchor-uniqueness check and the rules-46/49/53/54 requirement began life as P0.6 habits rather than rules, and promoting them here is what makes them bind every later step instead of only the next one. This rule is that promotion applied to itself.
56. **The `edit` tool reports "File updated with changes" for a no-op, so an edit is confirmed by the diff, not by the tool's success message.** Removing a blank line before rule 55, I passed an `old_str` and a `new_str` that were character-identical — both began with a single newline where the fix required the search string to begin with two — and the tool reported success. The file was unchanged: `git diff` produced the identical hunk and, decisively, the same blob hash `08d8e39d1b9` before and after, which is the only signal that distinguished "the edit applied" from "the edit matched nothing that needed changing". This is rules 46 and 53 once more, in the tool used to write the rules themselves: a success report confirms the call was well-formed, not that the intended change occurred, exactly as an exit code confirms a checker ran rather than that it read the right document. The remedy is the same shape as count-equality — assert the *result*, not the report: read the diff after every edit, and prefer an `old_str` that spans the boundary being changed (here, the tail of rule 54 plus the blank plus the head of rule 55) over one anchored on a single side, because a one-sided anchor is what let an identical pair look like a valid edit. The same reading turned up a second instance beside it: the rule-numbering scan matches `^\d+\. ` line by line and so passed 55 rules with no gaps in both layouts, being structurally blind to the blank-line change it sat next to — a check answering a different question loudly, which is rule 43.
57. **Compression preserves figures while deleting the clauses that scope them, so every number carried through a rewrite must be re-checked against its new neighbours.** The P0.5 PR body was cut from 5,170 words to 432 on the user's instruction, and `5 files added, 13 modified` survived into a sentence introducing the *deliverable*, where it is wrong. Both numbers were right as computed: the PR adds 43 files and modifies 14, of which `P0.5-baselines/` is **38 added and 0 modified**, the harness is **5 added and 13 modified**, and `P0.4-harness.md` is the fourteenth. The figure had been measured for the harness and the compression removed the clause saying so, re-binding a correct count to the wrong subject. That is harder to catch than a wrong number, because nothing local contradicts it — the arithmetic is sound and the source is real, and only recomputing the named subject exposes it. It is rule 39 from the other side: there, a count that disagreed might still have been a unit nobody asked about; here, a count that agrees perfectly is reported about something else. The second half is the handoff. The rewrite arrived with an explicit list of eight figures to re-verify and five deleted strings to confirm absent, and all thirteen checked out exactly — because **a battery drawn up by the author of a change inherits that author's blind spot**, and the single defect sat in the one figure the list did not name. Verify what a summary *asserts*, not only what its author thought to flag; the items volunteered for checking are selected by the same understanding that produced the error. The implementer sharpened this afterwards and the sharpened form is the operational one: the list was not a battery that happened to miss a case, it enumerated the figures the rewrite **preserved**, so by construction it could not cover the one figure the rewrite **changed** — and changed is where the defect lives. **When reporting an edit, list what you changed and let the reader check what survived.** That turns a caution about blind spots into a testable instruction, and it explains why a wholly accurate list still gave false assurance: every item on it was true, and the set was chosen by the wrong criterion. My own confirmation carried the same shape one level down — I counted the deliverable with the pattern `docs/design/lxr-port/P0\.5-baselines`, which is unanchored at its end and so matches both the directory and `P0.5-baselines.md`; the total of 38 was right only because the sentence's subject happens to be the union of the two, and it took the implementer's `37 + 1` partition to show the looseness and the subject coincided rather than the count being right by construction.
58. **Verify the consequence, not only the mechanism: the mechanism decides whether a defect exists, the consequence decides what you do about it, and a report can be exactly right about the first and wrong about the second.** The implementer traced a real defect in `verify-baselines.sh` — `--results` selects which checkpoints are audited (`:149`) while every data check reads `$BASE` (`:249`, `:270`, `:288`), so the two halves of one comparison come from two roots — and I confirmed all of it by reading. Attached to it was the assessment that the unsafe case is a rerun whose checkpoint filenames match the committed ones, which "can pass while never reading what you pointed it at". That part is false, and it is the part that drives the decision: `rederive-statistics.py` places three unconditional `return 1`s before its single `PASS`, failing on `checked_mean == 0` when the join produces nothing (`:226`), on `unmatched_cells` for a partial join (`:229`) and on `mismatches` for a full one (`:232`), so every manifestation of a split root fails loudly and a silent pass needs a fresh run to reproduce the committed numbers within a 1e-4 tolerance across every mean field. The flag is **unusable, not dangerous** — which turns "interrupt the merge and fix it now" into "fix it as P0.6's first tooling change", the opposite action. Note also that the implementer's own demonstration, a decoy directory yielding `FAIL (28 passed, 2 failed)`, is evidence for the *safe* direction and was offered as evidence for the dangerous one; a demonstration confirms the mechanism it exercises, not the severity claimed around it. And `:226` is count-equality (rule 46) already shipped by the script's own author, arrived at independently, which is the strongest support that pattern has. **The `28 passed, 2 failed` figure is no longer taken on report — it is reproduced here:** a minimal `{}` decoy under `--results` yields exactly that, the two failures being `schemaVersion is None` and `no decoy-invocations.csv`, which confirms both the number and that the mechanism fails closed.
59. **A gate that only compares can only catch mismatches; nothing in it catches a smaller input set.** `verify-baselines.sh` builds its checkpoint list with `find "$RESULTS_DIR"` (`:149`) and contributes one `ok` per checkpoint inside the loop at `:244-259`, so a supplied directory holding a correctly-named **subset** yields `RESULT: PASS (30 checks)` at exit 0 where the full set yields 34 — every check honest, every number re-derived, two thirds of the evidence never examined, and the banner still reading `mode: full audit`. This defeats my own universal: I declined to treat the `--results` split as dangerous because a split root "fails loudly in every case I could construct", and all four of those cases turn on a **mismatch** — differing names, empty join, partial join, differing numbers — while this one contains no mismatch at all. The defect is absence of coverage rather than presence of a fault, and no comparison fires on an absence. That is rule 57 one level up, and it lands on the author of rule 58: I verified the consequence, then bounded it by an enumeration selected by the same model of the tool that produced the claim. The bound on the fix is the script's own design principle at `:10-11` — *"Expected counts are DERIVED from the artifact, never repeated as literals at several assertion sites, so two cannot drift from a third unnoticed"* — which is precisely why no literal `3` guards the input set: **the principle that prevents brittle assertions and the hole are the same fact**, the second time on this gate that an immunity and a defect have turned out to be one property. Note also what the author did reach: `:414` names-vs-present, `:449` citations-vs-resolved, `:502` committed-vs-recomputed, each comparing two derived quantities and printing the difference. Five arrivals at count-equality in one repository, and the input set still unasserted — because every one of them compares two descriptions of **the same set** (contents) and none asserts that it is **the right set** (extent). The fix that respects `:10` derives the expected count from the artifact too, comparing checkpoints found against checkpoints the document names, in the exact shape of `:414`. **Assert the extent of the input set, derived, or the verdict's scope is unasserted no matter how many of its contents agree.** Discharged against my own board battery the moment the rule was written, because it had the identical gap: it asserted phases, steps, rule numbering, whitespace and control characters — all *contents* — and never the size of the field set, so an edit that clobbered a `- **Status:**` line in a step I wasn't looking at would have passed every check. Measured: **360 fields parse, 358 of them inside the 54 steps, 0 steps missing `Status`, 0 missing `Summary`, 6 ≤ fields per step ≤ 10.** The literal `360` is the part that goes stale the moment P0.6 adds a step, so the standing invariants are the derived ones — *every step carries a non-empty `Status` and `Summary`*, and *the step-field sum plus the phase-level fields equals the total* — which hold at any size. **Reproduced here rather than taken on report:** one real checkpoint under `--results` gives `PASS (30 checks)` exit 0 with the banner still reading `full audit`, against `PASS (34 checks)` for the same tree with no flag. Three decoy variants all produced **30 total checks** and differed only in the pass/fail split, so the total is a function of the input set's *size* and not its *content* — which is what makes count-equality the right guard here rather than merely a plausible one.
60. **When a discrepancy equals a structural count of the document, suspect the method before the artifact.** The implementer's `442` words against my `429` for one PR body differed by exactly `13`, which is the body's blank-line count. PowerShell hands multi-line command output back as an `Object[]` of lines — `gh pr view --json body --jq '.body'` returns **34 elements, not one string** — and `-split` applied to an array runs **per element**, so every blank line contributes one empty string to the total. Reproduced against the live body: naive split `442`, empties dropped `429`, `-join` then split `429`, `[regex]::Matches('\S+')` `429`, blank lines `13`, delta `13`. Note what identified the cause: not three methods outvoting one, but **the delta equalling a count the document already publishes**, which names the defect rather than merely outnumbering it. The underlying mechanism is this shell's array/scalar duality, and we hit it within the same hour **from opposite sides** — I wrote `$r57[0].Length` expecting a line and got `1`, because a single `Where-Object` match comes back as a bare string and `[0]` indexed a character. Assumed-array-got-scalar and assumed-scalar-got-array, one duality, one session. The guard is to force the shape before measuring: `@()` around anything counted, `-join` before any whole-text operation, `[regex]` over a string made scalar on purpose — and never read a shell's convenience reshaping as the shape of the data.
61. **Settle what a flag means before writing a guard for it, and enumerate the flag surface first — the coherent mechanism may already exist and the flag may be deletable rather than fixable.** `verify-baselines.sh` accepts exactly two long options — `--results` (`:42`) and `--no-results` (`:51`) — rejects every other `--*` at `:59`, and takes one positional `ROOT_ARG` (`:64`); `:77-79` then derive `DOC`, `BASE` and `HARNESS` from that single `$ROOT` **together**. So auditing a different tree *coherently* is already supported and always was: pass the positional. `--results` is **not** the only construct in the script that can split the roots — I wrote that and it is false. `$SELF_DIR` (`:34`) is a second root, and the two checks at `:193` and `:407` assert facts about the **audited** tree while reading the **apparatus** tree: `:193` tests `$SELF_DIR/run-p05-baselines.ps1` and concludes *"the run script ships with the results it produced"*, and `:407` tests `$SELF_DIR/$script` over names `:401` derived from `$ROOT`'s document — so `:414`, the very check I proposed as the model for the fix, is itself split-rooted. Note `:74`: with no positional, `ROOT` is derived *from* `$SELF_DIR`, so the roots coincide and the split is inert — the third construct on this gate whose immunity and defect are one fact. What `--results` is, is the only construct whose split was *undisclosed*; `:145` prints `self:` beside `root:`, so this one is deliberate and announced, and it is right for **executing** the apparatus and wrong only for these two checks that read it as evidence. That reframes the fork the implementer identified. **A** — *audit the committed document against another results tree* — is a real reproducibility check, and making it coherent means moving every data root to `$RESULTS_DIR` while the document stays committed; under it, a document-derived expectation for the checkpoint set is right and a mismatch **must** fail. **B** — *audit another tree on its own terms* — is the positional argument, and needs no flag at all. The defect was never the plumbing: **the flag has never had a stated meaning, and the code implements neither reading.** This lands on me. I recorded a `:414`-shaped guard as the fix without settling which reading it enforced; under A it is correct and under B it fires on every legitimate use, so I had written a check that votes on an unsettled interface without noticing it was voting. Note also what this costs P0.6: its stated first use — gate a fresh session — is B, so it wants the positional rather than `--results`, and even that is blocked because `:77-79` hardcode `P0.5-baselines.md` and `P0.5-baselines/` into the derivation. Correction to the implementer's framing, for the record: this script has **no `--doc` flag**; `--doc` belongs to `check-prose-claims.py`, so B cannot be expressed here by pairing `--doc` with `--results`, only by the positional.
62. **Re-verify a child session when the input that arrived could plausibly change the state, not when something merely arrived.** A bare idle notification carries no content and authorizes nothing, so it is not a reason to re-run `git rev-parse`; a message asking for work is. I stated this bound twice and it was ambiguous against its own instance both times, because I wrote it as *"a new input from me"* while my own closing and declining messages are inputs that cannot authorize a commit. The purposive form terminates and the mechanical one does not: **check when the arriving input could cause the change you are checking for.** Under it, four verifications of the same no-commit instruction were three too many, and the one that mattered was the first — immediately after countermanding the child's offer to fix, which is the only moment the instruction was under live pressure.
63. **A verdict has three things it can get wrong — its contents, its extent, and its subject — and count-equality reaches only the first two.** The implementer demonstrated the third with a paired control: a copy of the tree at HEAD with `check-calibration.py` deleted, audited from *this* checkout via the positional, returns `PASS (34 checks)` exit 0 and prints `ok document names 6 script(s), 6 present, difference 0`; the same perturbed tree audited by *its own* copy of the gate returns `FAIL (32 passed, 2 failed) missing: check-calibration.py`. Same tree, same perturbation, opposite verdicts, decided only by which copy of the apparatus you invoke. Both runs carry 34 `ok` lines, so rule 59's extent assertion cannot see it and neither can rule 46 — the subset case *loses* checks, this one keeps every check and gets one **wrong**, reporting a true statement about the wrong subject. **This is the silent mis-verification I argued could not happen** when I ruled the `--results` split inert; it exists, in the construct I had just called coherent, and it took someone else building the control to produce it. The rule that follows is the split itself: **trusted apparatus, suspect artifact** — execution paths belong on `$SELF_DIR` and every assertion *about* the audited tree belongs on `$BASE`/`$ROOT`, so `:193` and `:407` are simply on the wrong side of a line the script otherwise draws correctly and discloses at `:145`. Two bounds worth carrying, both **measured by deleting each of the six in turn**, with a pristine control between rows: coverage of the defect is **2 in 6, not 1 in 6** — I recorded 1 on the implementer's first estimate and it was wrong. `run-p05-baselines.ps1` and `verify-gate-of-the-gate.sh` are cited and fail at the citation checker; `check-calibration.py`, `derive-variance-table.py`, `rederive-statistics.py` and **`verify-baselines.sh` itself** all delete green at `PASS (34 checks)`. Reproduced independently here: `:401` extracts names requiring a `scripts/` prefix and yields 6; `:424` extracts citations with **no prefix required** and yields 4, of which `verify-gate-of-the-gate.sh:132` carries no prefix — which is exactly why an estimate made with the naming pattern missed it, rule 43 from the confirming side again. **The sharpest instance is deleting `verify-baselines.sh` from the audited tree**: the gate then reports `document names 6 script(s), 6 present, difference 0` and certifies that the tree ships the gate, while running from the only tree that has one. Note also that the fix has an in-file precedent twenty lines below the defect — `:437` resolves citations with `find "$ROOT/docs/design/lxr-port" …`, correctly rooted at the **audited** tree, which is precisely why the citation checker catches these two deletions and the `$SELF_DIR` checks catch none. And the near-misses that preceded it were rule 54's shape twice over — four uniform `exit=127` runs where WSL could not read a worktree whose `.git` is a file, and an `exit=2` with an empty `root:` that read as gate behaviour but was a PowerShell here-string shipping CRLF into `cd $'/tmp/p05ex\r'`. Discharged against my own apparatus immediately, and it has the identical construct: `store.mjs:6-7` resolves the board with `dirname(fileURLToPath(import.meta.url))` then `join(extensionDirectory, "roadmap.md")` — **structurally `$SELF_DIR`**, so the canvas reads the board beside whichever copy of the extension the host loaded, while my `git diff` and `Get-Content` checks read the copy I edit. Verified safe today for one reason only: exactly **one** copy of `roadmap.md` exists on this machine (`7F6BEBE5EB96`, 64 rules, in this worktree), so the two roots cannot diverge — immunity and defect as one fact for the fourth time, and the fourth is mine. The hazard is dated rather than hypothetical: **this branch is unpushed by design, and the moment it merges the main checkout gains a second copy**, after which any session outside this worktree carries its own board, the canvas renders one and my battery verifies the other, and no contents or extent check I run can tell — subject again. Honest bound on what I established: I proved there is one *candidate*, not that the host loaded it. **That bound is now discharged, and the claim inside it was wrong** — the extension log *does* carry a resolved path, `EXTENSION_PATH=…\kkokosa-fictional-giggle\.github\extensions\lxr-gc-roadmap\extension.mjs`, and since `store.mjs` resolves the board from its own file URL in that same directory, the host demonstrably reads the copy I edit rather than merely the only copy that exists. The subject axis is therefore established by **evidence rather than by uniqueness**, which is what makes it survive the second copy appearing at merge: the standing guard is to read `EXTENSION_PATH` from the log after each reload *and* assert copy-uniqueness, not to rely on either alone. (The count in the parenthetical above is point-in-time; the board carries 67 rules as of this note, and `board-check.ps1` asserts the copy count on every run.) **Fix scope corrected from two sites to three:** `:391` is also wrong-subject and is worse than the other two, so the repair is `:193` and `:407` reading `$BASE/scripts/` *plus* `:391` passing `--lxr-root "$ROOT/docs/design/lxr-port"` — one argument, since the flag already exists at `check-doc-code.py:214`. See rule 68.
64. **Do not correct a claim the other party did not make.** I told the implementer that `verify-baselines.sh` has no `--doc` flag "as a correction to its framing" — true of the script, and it had never claimed otherwise: its `--doc` usage was always about `check-prose-claims.py`, and its fork message named `:288` and `:414` as the document-coupled checks without asserting any flag. I manufactured an error and then corrected it, which costs more than a silent misreading because the other party may accept authorship of a mistake that was never theirs — this one nearly did, declining only because it could not verify its own sent text and refused to accept unverifiable authorship. The check is one line: quote the sentence you are correcting before correcting it, and if you cannot find it, you are arguing with your own paraphrase. The implementer added the reciprocal, which is the stronger half: **a hedge should name what it is hedging.** It declined authorship on *"I cannot verify my own sent text"* when the available claim was *"I did not write it"* — it could not reach the strong ground and settled for the weak without saying so, which reads as a concession and is not one.
65. **When a report carries both a measurement and an inference drawn from it, mark which is which; unmarked, the inference borrows the measurement's standing.** The implementer sent one message containing a paired, controlled demonstration — C1 green and C2 red on the same perturbed tree — and, in the same paragraph and the same voice, the bound *"1 in 6, the other five delete green"*, which was an extrapolation from a grep rather than a measurement. I recorded both into `f98c17684a9` at the demonstration's confidence, and the bound was **wrong: it is 2 in 6**, established afterwards by deleting each of the six. Nothing in the message was false about the demonstration and nothing marked where the evidence stopped. This is rule 57 applied to bounds rather than to batteries — the author selects the extrapolation with the same model that produced the measurement, so it inherits the blind spot while wearing the measurement's authority — and it compounds across sessions, because a coordinator copying a figure onto a durable board strips whatever tentativeness the prose carried. **The reader's guard is the cheaper one: a figure worth writing down is worth asking how it was obtained.**
66. **When a reproduction yields a different number, the first hypothesis is that you reproduced a different thing — not that the original is wrong.** Discharging rule 65 against this board, I re-ran the two gate figures I had recorded but never executed. The subset reproduced exactly. The decoy returned `FAIL (29 passed, 1 failed)` against a recorded `28 passed, 2 failed`, and that correction was one command away from being written down — but my decoy was a renamed *real* checkpoint while the implementer's was fabricated, so mine passed the `schemaVersion` check that theirs failed; a minimal `{}` decoy reproduces `28 passed, 2 failed` exactly. Both runs were correct measurements of different perturbations, and only one of them was a reproduction. **The tell was available before the run: the totals already agreed at 30 checks**, so the disagreement was confined to the split — a property of the decoy, not of the gate — and a discrepancy that leaves the structural figure intact is pointing at the input, not the subject. This is rule 43 from the confirming side, and rule 65 turned on its author: I corrected the implementer for presenting an inference in a measurement's voice, then nearly presented a neighbouring measurement as a refutation, in the same turn.
67. **A checker you retype is a checker whose patterns are unversioned; store the battery beside the artifact and run it.** The standing board battery was retyped from memory on each use, and in one sitting it drifted four times — every drift a false alarm or a blind spot on a healthy board. `^\d+\. \*\*` counted 19 of 66 rules, because the early rules are not bold-prefixed. `^- \*\*[A-Za-z ]+\*\*:` counted 0 of 360 fields, because the colon sits *inside* the bold. A staleness grep matched the very rule that documents the stale figure. And a Status-per-step expectation of 54 was wrong because phases carry a Status too, so the truth is 65 = 54 + 11 — caught on the stored script's first run by an assertion no ad-hoc version had ever bothered to write down. **Then its first draft reproduced rule 59's defect exactly**: rule contiguity derived from the artifact means deleting the highest rule leaves `1..N-1` perfectly contiguous, so the negative control passed a board with a rule removed. Shape can be derived; extent needs a floor stated from outside, raised deliberately. Two general forms, both of which I had already written down and neither of which I applied to my own apparatus: a battery that prints numbers without comparing them cannot fail, and one that derives every expectation from its subject cannot notice the subject shrinking. Lives at `board-check.ps1`, with a paired control as its acceptance evidence.
68. **The author of a demonstration is the worst-placed person to bound its class, because the bound comes from the same search that produced the case.** The implementer's phrasing, offered against itself after its second unmeasured bound in two messages — `1 in 6` when the truth was 2, and *"`:193` and `:407` are simply on the wrong side of a line"* when the truth is three of fourteen. Both bounds arrived attached to demonstrations that held, which is exactly why they were believed. The third site is `:391`, and it is the worst of them: `:193` and `:407` read the audited tree's *document* and test it against the apparatus, whereas `:391` invokes `check-doc-code.py` with no `--lxr-root`, so `:209`-`:211` derive the default from `__file__` and **both sides come from the apparatus** — the audited tree is never opened. That check is the F1 sweep, added because P0.4's gate compared the document to itself and therefore could not catch a document claiming a `calibrate` verb the source lacked; under the positional it compares the document to itself. Verified here rather than taken: all 14 `$SELF_DIR` uses sit at the lines claimed, `:386` `--selftest` is correctly apparatus-rooted because it asserts the checker's own probes fire, and `:326`/`:437` are correctly artifact-rooted. Measured with a `git archive` extract of HEAD and one line appended to the **copy's** `P0.4-harness.md`: unperturbed copy audited from the other checkout gives `PASS (34 checks)` exit 0; **perturbed copy audited from the other checkout also gives `PASS (34 checks)` exit 0**, printing `every one of the 11 harness flags … exists in source` — the apparatus's number, while the tree named in its own banner claims 12; the same perturbed copy audited by its own gate gives `FAIL (33 passed, 1 failed)` with `flag --bogus-flag claimed at P0.4-harness.md:947` and `claimed-but-absent: 12 - 11 present = 1`. That last payload was the implementer's one marked *inference* — it had lost the log — but **my run does not close it**: I perturbed `P0.4-harness.md` and it had perturbed `P0.5-baselines.md`, so mine corroborates the proposition on a different instance rather than settling that run, which it closed itself afterwards. Same verdict is not the same measurement. See rule 71, because the divergence bought something neither of us intended. **the discriminator between blindness and detection is a single digit in a count the gate already prints**, which is rule 26 earning its place.
69. **A destructive command placed in a script "just in case" is the one instrument defect that does not announce itself by producing a wrong number.** The implementer wrote `git -C "$REPO" checkout -- .` as a restore reflex in a throwaway demonstration script, when the edit it meant to undo had been made in the *extract* rather than the repo. It was a no-op purely because the repo had nothing uncommitted; against a dirty tree it would have silently discarded work in the middle of a demonstration, and no line of output would have mentioned it. Every other defect on this board announced itself with a number that came out wrong — 19 of 66, 0 of 360, 11 against 12, `PASS` on a perturbed tree. **This class produces no number at all, so the guard cannot be a comparison:** a throwaway script must not carry a destructive command it does not need, and a restore step belongs where the edit was actually made, named explicitly, never as a blanket over a whole tree. Self-reported by its author after the fact, which is the only route by which this class ever surfaces.
70. **Apparatus deserves the paired control that measurements get.** I claimed everything caught this week lived in a checker rather than in the thing checked. The implementer's boundary is right, and I verified it against git rather than taking it: **13 of the 14 commits on the P0.5 branch touch the document**, with subjects including *correct a false prose claim*, *stop section 7 from restating a gated count the gate already checks*, and *make the harness file counts match what git reports*. The artifact was **not** clean. What held is the **measurements at HEAD**, and the modification counts are exact: 15 of 15 changed paths under `P0.5-baselines/` are in `scripts/`, none is a data file. I then narrowed that to *no re-derivation from the raw CSVs has ever come out different* — **false, and falsified by the document I was auditing, at `:139-141`**: `rederive-statistics.py` found a real defect on its **first** run and moved **331 of 6,600 fields** (F21 — a published p99 of 0.3498 ms where its five invocations mean 0.7668 ms). My evidence could not have witnessed it *in principle*: every data file was added in commit 1 and never modified, so a re-derivation disagreement has no shape in this branch's history, and the fix landed in the working tree before the first commit. That is `:391`'s defect wearing the evidence rather than the checker — both sides of the comparison drawn from one place, so the method decides the answer. The generalisable part is the register: **narrowing a claim feels like the safe direction, so the narrowed version does not get re-tested.** Mine survived one `git log`; the implementer's replacement for it survived one `grep` (rule 72). The forward form is the implementer's, and it is what `board-check.ps1` became: an instrument ships with a paired control demonstrating that it fails on a perturbed input, exactly as a measurement ships with a baseline. An instrument with no control is an untested claim, and every instrument defect found this week surfaced either from someone building a control after the fact or from an author's own confession — never from the instrument itself.
71. **An exact reproduction tests repeatability; a divergent one tests generality — so do not standardise reproductions too hard.** The implementer perturbed `P0.5-baselines.md`; I chose `P0.4-harness.md`, its message having named no file. Neither of us intended a difference, and that difference is the only reason the doc/code sweep's **provenance** was ever tested: two documents, two line numbers — `P0.4-harness.md:947`, one past the end of a 946-line file, and `P0.5-baselines.md:1114`, being 1112 plus a blank and the text, both counts verified here from `git show HEAD:` — each named **correctly**. Run alone, neither result distinguishes correct provenance from *names the first document in its audit list* or from a hardcoded path, because each of us would have seen only our own filename come back and read it as confirmation. — **The second half of that sentence is false, and the implementer falsified it from source.** `check-doc-code.py:228` is `sorted(p for p in lxr.glob("*.md"))`, whose first element is `P0.1-benchmarks.md`; neither run ever returned that name, so *either* run alone already excluded **first-in-list**. The pair excluded only **hardcoded-path**, and a single run perturbing two documents excludes both — which it demonstrated, and which `:130-137` settles without any run at all, since `scan_markdown(doc.read_text(...), doc.name)` passes the origin label in beside the text, making provenance per-document **by construction**. All three verified here. **This is the reciprocal of rule 68:** if the author's search shapes the bound, then a second author's *different* search is worth more than a careful repeat of the first. What survives is the rule's general claim — a divergent reproduction does test generality, and this one tested a property neither of us had thought to specify. What does **not** survive is the necessity: divergence was sufficient, never required, and the coordination corollary I drew from it is retracted in favour of rule 73.

72. **Re-derivation and reading catch disjoint classes of wrong number, and a claim that either is "the only instrument with demonstrated power" is false in both directions.** Correcting rule 70, the implementer wrote that re-derivation from raw data is *the only* instrument in this deliverable that ever caught a defect in a published number. Measured against the committed ledger: **F11, F22 and F24 are all published numbers that are wrong, and re-derivation agrees with every one**, because each is arithmetically correct and wrong in what it *means*. F11's "measured minimum" of exactly 16 MiB for six of ten scenarios is the **search floor**, so every heap factor derived from those six has a denominator that was never measured (`:117-123`). F22's published throughput of **988 op/s** at wks/2.0x is the 1,000 op/s default **arriving successfully**, not capacity (`:154-157`). F24's throughput ratio is **1.000 by construction** — rate-limited by simulated I/O, and it would stay 1.000 for a collector twice as fast (`:549-554`). F21, the one re-derivation did catch, is the exact complement: arithmetic wrong, meaning fine. So the partition is **arithmetic vs meaning**, and this is rule 39's unit problem and rule 42's un-re-derivable claim arriving as measured numbers rather than as prose. — **"Each instrument is blind precisely where the other works" was my own over-reach and the implementer measured it out: that phrasing implies a pair, and there is no second instrument.** Four regimes, not two: F21 **fails the gate**; F22 is **encoded as a fix in code** at four sites (`rederive-statistics.py:85-93` and `:134`'s `phase_of(row["runId"])`, `compare-arm-ratios.py:26`, `compare-sessions.py:32`, `derive-variance-table.py:9-10,48`) so nothing re-checks it; F11 is **detected, printed, and deliberately never failed**; and **F24 has nothing at all** — verified here by a wider sweep than the one that found it, `pinning-heavy-io` appearing in **0** code files across the whole `P0.5-baselines` tree, and none of the 12 registered prose claims touching it. F11's report-don't-fail is the right call and argues its own case at `check-calibration.py:10-14`: failing "would push a future session toward raising the floor until the warning stops, which would replace a true statement with a comfortable one" — the one good argument against making a property an assertion, and it comes with the tension intact, since `:105` detects by `"floor" in note.lower()`, so rewording a note ends detection silently. The clinching detail is at `:163-166`: F22's second consequence is that **the re-derivation script itself carried a silent defect** — it joined published cells to raw rows on `mode`, which for this scenario reads `latency` in *both* phases, so it compared against the mean of ten invocations rather than five. The instrument being nominated as the only one with demonstrated power needed a different instrument to correct it, and the document places F22 first. Marking classes per rule 65: the ledger entries and their contents are **measured**, read from the committed blob; *which* instrument found F11, F22 and F24 is **not stated by the document and I have not established it** — but the structural claim needs no attribution, since re-derivation demonstrably agrees with all three. For P0.6 this is a design constraint, not a scoreboard: a third arm needs both instruments, and `ratioVsBaseline`'s immunity to F21 (it resamples the per-invocation vector, never the published scalar) is the property to preserve when LXR becomes a column.

73. **A free instance is a lottery; a control is N≥2 instances with distinct payloads — brief the control.** This replaces rule 71's coordination corollary, which I had already committed to applying to P0.6's kickoff. The implementer's construction: one extract, two documents perturbed, **one** gate run, `--flag-alpha` into `P0.4-harness.md:948` and `--flag-beta` into `P0.5-baselines.md:1114`, both named correctly, `FAIL (33 passed, 1 failed)`. Same property, deterministic, one session — so precision costs nothing here, and precision is what stops a child inventing its own standard of evidence. **The distinct payloads are load-bearing and worth stating explicitly, because `check-doc-code.py:136` is `found.setdefault(flag, []).extend(sites)`:** sites accumulate under the *flag* key, so two documents perturbed with the **same** flag name merge into one entry with two sites — that still exhibits two filenames, but it cannot detect a **label swap**, the failure where the checker attributes document A's finding to document B. One payload per instance is what turns a two-name result into a checked *pairing*. Two further reasons the control beats the accident, both the implementer's: **an accident cannot be re-run**, so our pair is evidence but not a regression test, whereas a two-document perturbation ships as one — and it should, since `verify-baselines.sh:386`'s `--selftest` asserts the checker's probes fire but nothing asserts they fire at the *right document*. The residue of rule 71 that survives is narrow and worth keeping honest: a free instance can still surface a property **nobody thought to specify**, which is exactly what happened here — but that is a lottery ticket, you cannot schedule a lottery, and a briefing standard must never rest on one. Specify the property, specify at least two instances, let the implementer choose *additional* ones freely.

74. **A control that lands in the blind spot of the check it controls converts an untested assertion into an apparently tested one — so a control must first prove its perturbation is visible to the check's own method.** Rule 73 sent me to audit `board-check.ps1` itself: 12 checks, and until that day controls for 2. Driving each with two distinct payloads found 11 of 12 firing, correctly labelled, with distinguishable output — and exactly one gap, in the **subject** check, which is the one that becomes load-bearing the moment PR #5 merges and creates the second board. Two decoy boards planted in a matching path, and it reported `ok exactly one board on this machine`. The cause, measured rather than reasoned: `Get-ChildItem -Recurse` does not descend into **hidden** directories without `-Force`, `%TEMP%` lives under the hidden `AppData`, and the same tree at the same instant yields **1 copy without `-Force` and 2 with**. So the check had a real blind spot *and* my first control for it landed inside that blind spot. **The dangerous part is the shape of the output**: not an error, but a fluent green sentence that was true of everything the check could see. This is rule 53's dual — 53 says observing only FAIL cannot distinguish *the check fired* from *the harness never reached the document*; the dual is that observing PASS on a perturbed input cannot distinguish *the check correctly passed* from **the perturbation never arrived**. The discipline that follows: a control asserts its own perturbation is detectable by the check's method before it interprets the verdict. Fixed with `-Force`; both payloads now fire and are distinguishable (`2 boards found` vs `3 boards found`), and coverage is now 12 of 12 checks at 2 payloads each. **The check fixed here was itself replaced the same day — see rule 75 — so `-Force` is no longer in the script; the lesson survives the check because it is about controls, not about that check.**

75. **A proxy about to become permanently false for a legitimate reason should be replaced with direct evidence of the property it stood for — not relaxed, and not deleted.** The subject check asserted *exactly one board exists on this machine*. The property it existed to defend is narrower: *the board this script audits is the board the extension host loads*. Uniqueness only ever **implied** that, and merging PR #5 creates a second copy legitimately — so the proxy goes red while the property stays true. Both usual responses to a red check that is not reporting a defect are bad: relax it until it can no longer fire (rule 72's F11 warning — replacing a true statement with a comfortable one), or delete it and lose the axis entirely. A third option existed here only because **the host already writes the answer down**: every extension log carries `EXTENSION_PATH=<dir>\extension.mjs`. Two assertions now replace the one proxy — the **newest** log's directory equals `$PSScriptRoot`, and the number of **distinct** paths across all 59 logs is 1 — and both survive the merge, because two files on disk are fine and two *loaded* boards are not. Six controls at two distinct payloads (rule 73), each printing `perturbation=reached` before its verdict is read (rule 74): fabricated paths `D:\decoy-alpha` / `E:\decoy-beta` fail identity while distinct stays green, and an older decoy beside a correct newest log fails distinct — naming the extra path — while identity stays green, so the two assertions are separately exercised rather than jointly. **The measured side-effect is the stronger argument:** the battery fell from **36 s to 0.37 s**, because the proxy was a recursive scan of all of `C:\` and the direct evidence is a regex over one directory. A battery slow enough to skip is a battery that gets skipped, which is a correctness property and not an ergonomic one. **The condition on this rule is real and should not be dropped:** direct evidence was available because the host happened to record the fact. Where nothing records it, inventing a field to check against is rule 72's `check-calibration.py:103-104` trap — a derived check silently never matches — and the honest move is to state the property as unenforced rather than to enforce a proxy for it. One environment fact worth carrying: the newest log is the **live** one and the host holds it open, so `File.ReadLines` throws on exactly the log carrying the current answer; the read needs an explicit `FileShare::ReadWrite`.

76. **A hazard annotated only at its definition site is undocumented for everyone who consumes the artifact.** `GcTelemetry.cs:233-237` explains, carefully and correctly, that `Gen0Collections` holds the all-generation total and that gen1/gen2 are subsets of it — *"not the gen0-only figure its name suggests."* That annotation is in C# XML doc on a property. What ships is the **name**: `gen0Collections`, in all 144 published rows across three result files, and in `P0.5-baselines.md` at `:541`, `:543`, `:544`, `:550`, with the semantics stated in neither. A P0.6 session reading the JSON key gets a wrong number and no warning, having never opened the file where the warning lives. **The prose is the proof that documentation-at-the-definition-site does not travel:** `:542`'s *"2,042 gen0 against 2,021 gen2 — nearly every collection is a full one, so it is thrashing"* is valid **only** under the subset reading (99%, not the ~50% a literal reading gives), so the author plainly knew — and still wrote "gen0" four times into the artifact a stranger will read. Corroborated: `gen2 > gen0` in **0 of 144 rows**. The discipline: an annotation must sit at the boundary the consumer crosses — the field name, the published schema, the column header — not at the definition, because the consumer's first contact with the concept is the name, and by then the reading is fixed. **The dual matters as much and is the trap I nearly walked into:** a name that *looks* wrong may be right, and renaming the computation rather than the label injects the defect. `MeanPauseMs` divides `GC.GetTotalPauseDuration()` by `Gen0Collections`, which reads like the same unit error and is instead the **correct** denominator — a mean over all pauses needs all collections, exactly as `:271-274` argues — and that fallback branch is **entirely untested** — the whole 1188-line suite builds exactly two `GcSummary` fixtures, `(Observed 20, Gen0 20)` and `(0, 0)`, so nothing exercises it (rule 80). Same misleading name, one place it must be repaired and one place it must be left alone; only reading both settles which is which.

77. **Evidence that a check *mentioned* your input is not evidence that it *consumed* your input.** Rule 74 says a control must prove its perturbation is visible to the check's method. My first implementation of that proof asserted the fabricated log directory appeared somewhere in the battery's output — and it reported `reached` on all 27 cases while **every fabricated log was invisible**, because the battery selects with `-Filter '*lxr-gc-roadmap*.log'` and I had named them `ext-1.log`. The path appeared in the output for the worst possible reason: it is quoted in the message *"no extension log under '…'"* that reports them missing. A presence test on the input's name is satisfied by the error that says the input was absent. The repaired form asserts the **consumption signature** instead — no case may emit `no extension log` — which is a statement about what the check did with the payload rather than about what it printed. **The second instance the same run produced, and it is the same error in the other direction:** the "correct path" payload was built locally from `$env:TEMP`, which yields the 8.3 short form `KONRAD~1`, while the battery's `$PSScriptRoot` resolves long — so identity failed on all 27 cases *including pristine*, and a control that fails everywhere is as uninformative as one that passes everywhere. The fix was to stop reconstructing the value and **ask the instrument for it**: the battery prints `self :` in its banner, so the control now takes the check's own answer and no normalization rule has to be kept in sync. Both defects are one shape — the control and the check disagreed about what the input *was*, and in neither case did the disagreement announce itself as an error.

78. **A universal that quantifies over another party's evidence is unfalsifiable by its author — and this one was contradicted by a finding that author had produced himself.** The implementer wrote *"every defect either of us has produced since PR #5 opened is in apparatus"*, a claim about **my** commits. I justified this as unfalsifiable-by-its-author on the grounds that the implementer could not reach my branch — **that justification is false and the implementer disproved it.** Linked worktrees share one object database (`--git-common-dir` is `C:/github/runtime-fork/.git` for both), so every board commit, including ones made minutes earlier, is readable from its checkout by `git cat-file`. Unpushed means unreachable from another *clone*, and we are not in another clone. The case is therefore the same shape as the other four — reachable by both parties, checked by neither — and the "new variant" claim is withdrawn. What survives is the discipline, on its own merits: state the scope you can inspect. Measured: `c5ff8b90988` (15:00, PR #5 opened 08:18) committed rule 70 asserting *"no re-derivation from the raw CSVs has ever come out different"*, and `133418924af` corrected it 13 minutes later. That is a defect in the artifact, inside the stated window, and it sits on the **measurements** side of the implementer's own partition — it is a statement about what re-derivation runs have shown, not about apparatus. **The falsifying evidence was the implementer's own catch**, delivered two exchanges earlier: `P0.5-baselines.md:139-141`, `rederive-statistics.py` failing on its first run. So the claim was not merely unverified by its author; it was refuted by something the author personally found and could not connect, because connecting it required reading a history he has no access to. The discipline: state the scope you can inspect, and let the other party answer for theirs — a shared universal needs both halves measured, and only one of us can measure each. **A tooling fact that nearly hid this:** `git log -S<phrase>` reports commits where the *occurrence count* changes, so a correction that **retains the false sentence as a quotation** — which is exactly how a retraction should be written — is invisible to it. `-S` returned 1 commit and `-G`, which searches the diff text, returned 2. Searching for a retracted claim with `-S` finds the assertion and hides the retraction.

79. **Widening another party's finding needs the same standard of evidence the finding itself did — a data signature is not a class.** The implementer declined to bound F24's class (its rule 68), so I tried to, from a signature I could compute in one pass over the per-invocation CSV: throughput spread under 10% while collection counts vary several-fold. Four of ten scenarios matched, and I was one commit from filing a blocker four times the size of the one I was verifying. **Three of the four are correct by design.** `aspnet-request-load` already carries `Primary = PrimaryMetric.Latency` in `ScenarioCatalog.cs` — F24's own prescribed fix, already applied, which the signature cannot see. `low-allocation-compute` and `pointer-chasing` record zero collections in every valid row because they are a deliberate paired barrier experiment: the first isolates the write barrier LXR pays on every reference store "regardless of how much garbage there is to reclaim", the second is its high-store partner so that "the pair separates per-store barrier cost from per-object cost". Under the signature alone I would have declared the two most carefully justified scenarios in the catalog vacuous — including the one the harness uses as its own resolution test — on the grounds that the collector never ran, which is the entire point of them. What separated the real case from the three false ones was a **declaration**: a metric enum and a rationale, both in code I had not read, neither recoverable from the numbers. Two consequences. **For the finding:** the class is exactly one **in the throughput phase, which is the only phase this signature was computed over** — a scope I failed to state and which rule 83 shows is load-bearing, because the same property holds for nine of ten scenarios in the latency phase. The gate that bounds it must read declarations rather than infer intent from data. **For the method:** this is rule 76's dual arriving from the other side — there a *name* looked wrong and the number was right, here the *numbers* looked wrong and the design was right. Before escalating a peer's finding, read the thing that says why the code is the way it is. Cost of the discipline: four commands. Cost of skipping it: a false blocker in the next step's kickoff, aimed at the harness's precision instrument.

80. **A published quotient whose inputs are unpublished cannot be re-derived, and the gate quietly downgrades it to a copy check — `pauseAverageMs` is that field, and I republished an inference about it as fact.** The implementer reported that `MeanPauseMs`'s untested fallback is "live in 8 of 144 rows", marking the branch-to-`pauseSource` mapping as an inference corroborated by the partition being exact with no residue. I carried it into two places on this board **without the marking**, which is rule 65 failing at the receiving end: a peer's marked inference becomes an unmarked fact the moment it is restated. Measured, the inference does not hold and the corroboration cannot bear on it. `GcTelemetry.cs:195` makes `distributionTrusted` false in **two disjoint ways** — `pauses.Length == 0`, which reaches the untested fallback, or `pauses.Length > 0 && shortfall > 2`, which reaches the *tested* suspensions branch — and both emit the same `total-pause-duration-only` label and both suppress the percentiles, which is exactly what the 8 rows show (`withP99=0, withAvg=8`). The partition being exact is true by construction of the ternary chain at `:207-215` and discriminates nothing. **It is undecidable from the artifacts**: 55 published keys per row include `pauseAverageMs` and `pauseSource` but neither `totalPauseMs` nor `observedPauseCount` nor `pauseShortfall` — the label is published and its inputs are not. Two consequences worth more than the original claim. **The gate cannot see this field's value:** `rederive-statistics.py:52` classes `pauseAverageMs` as a `COPY_FIELD`, so re-derivation checks the published scalar equals an invocation's scalar and never touches the quotient — the one instrument with demonstrated power is structurally blind to how the number was computed, for want of a denominator. **And what needs no inference at all is the real finding:** the fallback branch has no fixture anywhere in the 1188-line suite, which builds exactly two `GcSummary` values, `(20, 20)` and `(0, 0)`. So P0.6 owes two things, not one — add the missing fixture, asserting the denominator is the all-generation count against *both* misreadings (`TotalPauseMs / (20-19)`, the gen0-only "correction" rule 76 warns about, and `TotalPauseMs / 41`, the triple-count the existing test already guards); and **publish the denominator**, which promotes the field from copy to derived, makes the branch decidable by one grep, and would have prevented the implementer's own near-miss from a defaulted `r.get("totalPauseMs", 0)` reading a key that exists in none of the 144 rows.

81. **An audit that searches one location for items that legitimately live in several reports absence as a defect — and acting on that verdict would have duplicated a claim, which is the hazard rule 63 exists for.** Auditing whether everything I had told the user P0.6 "carries" is actually retrievable from the board, I grepped the P0.6 `Open` field for eleven carry-forward items. Two came back `ABSENT`. One was real: rule 80's *publish the denominator* deliverable existed only inside the rule, never in the field an implementer reads — precisely the F24 shape I had just endorsed, an assignment written where nothing points at it, and it is now carried into `Open` with its two concrete assertions. **The other was my checker's fault:** `large-object-pressure` is absent from `Open` because the inversion lives in P0.6's `Correctness` field, which is its correct home. Had I trusted the verdict I would have copied it into `Open`, producing two statements of one finding that can drift apart — rule 63's two-subject hazard, manufactured by a control built to prevent omissions. The general form: **a single plausible `ABSENT` is more dangerous than a control that fails everywhere**, because rule 77's all-red case is obviously broken while one red item looks like a finding and invites a fix. Scope the search to where the property may legitimately live, which for a step means every field of that step, not the field the auditor happens to have in hand. A second-order note worth keeping: the same pass produced a **false positive** in the other direction when `[regex]::Match` returned `Success=false` with `Index=0`, and printing context around index 0 showed the field's opening sentence as though it were a hit — a not-found result rendered as a plausible excerpt, which is rule 74 in a formatting helper.

82. **Every over-reach in this run was a universal quantified over history, and history is the one subject with no standing instrument.** The implementer's observation, and it is the durable output of the whole exchange: across five corrections on both sides, *not one* of the false claims was about a measurement or about code. They were `everything`, `only`, `never`, `nowhere else`, `first`, `each` — assertions about the record. A measurement can be re-measured and code can be read, so a wrong claim about either meets an instrument built to meet it; `rederive-statistics.py` catches an arithmetic defect and reading catches a semantic one. A claim about what has happened has nothing pointed at it, and both parties kept shipping them at exactly the rate the absence predicts. **Two things this replaces.** My rule 78 justified its discipline on the claim being unfalsifiable by its author, and that was wrong: linked worktrees share one object database, so the board was always one `git cat-file` away from the implementer, and its own commits were always one away from me. Reachable by both, checked by neither — which is the *same* shape as the other four, not a variant. **And the implementer's counterexample beats mine on its own terms.** `669a3dba027` (author 11:04:09, forty-six minutes after PR #5 opened at 10:18:27 local) put two false claims into `P0.5-baselines.md` itself — *"Its minimum is 4 MiB where Server needs 35–43"*, which the document now records at `:431` as previously-read-and-wrong, and *"odd or leaves a remainder"* as the mechanism defeating exact ×1.3, true but naming the wrong cause since 36 is even and 36 × 1.3 = 46.8 still fails — corrected 39 and 55 minutes later. That falsifies *"every defect either of us has produced is in apparatus"* under **both** readings, with no argument about categories, because it is in the deliverable. My counterexample needed the argument, since `133418924af` repaired rule 70 while touching **zero bytes** of the deliverable. **And the argument itself was ill-formed**, which is the sharpest thing either of us said: apparatus-versus-artifact is a **role, not a location**. `roadmap.md` is artifact to `board-check.ps1` and apparatus to P0.5, and it changes side with the question being asked, so a claim using that partition must name the deliverable or it silently means two things. What to do about it, since no instrument is coming: when a sentence quantifies over the record, either bound it to what a named command over a named range returns, or do not write it. The `-S`/`-G` fact from 78 survives intact and was independently reproduced from the shared object store.

83. **The defect F24 names is nine times larger than F24 states, it is in the committed matrix, and both of us bounded it to one scenario by measuring only the phase where the bound holds.** The implementer proposed flipping `pinning-heavy-io` to `Primary = Latency` and I verified every number behind it — all reproduce exactly: `latencyP99Ms` 0.31714/0.29814, 0.2674/0.20448, 0.19778/0.17312 give srv/wks of 0.940, 0.765, 0.875 while `ratioVsBaseline` publishes 0.999998, 0.999999, 0.99999 on those same rows, with `operationsPerSecond` pinned at 21,481.6. `Primary` is genuinely consumed, at **four** sites rather than the three it named — `Aggregator.cs:112` picks the field, `:159` names the statistic, `MatrixRunner.cs:204` forces the mode, and `Reaggregator.cs:213` is the fourth, the one that makes its own no-re-run path possible. **Then following it generalised the finding past both our bounds.** Measured over the 68 valid wks/srv latency-phase rows in the three committed result files: **nine of ten scenarios publish a latency-phase ratio within 0.16% of 1.000**, and the sole exception is `aspnet-request-load` at 12.80% — the only scenario in the catalog declared `Primary = Latency`. The correspondence is exact and the mechanism is documented at `Lxr.Harness.Runner/Program.cs:200`: latency-phase arrival rates are *fixed from a prior throughput pass*, so `operationsPerSecond` is pinned by construction for every arm, and `Aggregator.cs:112` reads exactly that field for all nine Throughput-primary scenarios. F24 is not a fact about one scenario's I/O rate; it is a fact about **choosing the ratio basis per scenario when what is measurable is a property of the phase**. **This changes the fix.** The flip repairs one of nine and costs something — `MatrixRunner:204` would force `mode=latency` in both phases, the F22 shape, safe today only because all four consumers join on `runId` and unsafe the moment a P0.6 tool joins on `mode`. Making the basis a function of **(scenario, phase)** repairs all nine, touches no catalogue row, leaves `Primary` and therefore `:204` alone, and needs **no re-run at all**: `latencyP99Ms` is populated in **68 of 68** latency-phase rows of the published JSON, against **0 of 69** in the throughput phase for every scenario except `aspnet-request-load`. One decision it forces rather than settles, and P0.6 must take it deliberately: `aspnet-request-load` is the single row where a phase-dependent basis changes a currently informative number, because its throughput-phase ratio of 1.1305 is computed on latency today. **And the scope lesson is rule 82 arriving one message later, in the artifact rather than the prose.** My rule 79 bound F24 to one scenario of ten from a throughput-phase signature and stated it without naming the phase; the implementer bound it to one scenario from latency-phase P99s and also stated it unscoped. Both bounds are true of the phase measured and false of the matrix. Neither of us wrote a universal about history this time — we wrote one about **data**, and it failed the same way, because the quantifier ranged over a set neither of us had enumerated. **And the first draft of this rule committed the error it names.** It published *"0 of 6 throughput-phase rows"* for the scenarios lacking `latencyP99Ms`; 6 is `pinning-heavy-io`'s own row count, and the figure over the set actually claimed is **0 of 69**. Enumerating afterwards, which is what I had not done: three result files and no other `p0-5` result JSON in the tree, `collector` taking only `wks` and `srv`, **143 of 144 rows valid** with the single exclusion being `lifecycle-semantics/wks/1.3` at `status=crashed` — in the *throughput* phase, so the latency-phase figures are untouched by it and `68 of 68` and `nine of ten` both stand. Naming a failure mode does not confer immunity to it, and the interval between naming and repeating it was one commit. The operational form is therefore not *"remember the scope"* but the mechanical one: **before a count ships, print the denominator of the set it ranges over and check it is not the denominator of the case that suggested it.** **A second self-caught over-reach in the same hour, of a different shape and worth separating:** carrying the four `Primary` consumers into P0.6 I wrote that changing `Aggregator.cs:112` without `Reaggregator.cs:213` would make re-aggregated output diverge from a fresh run. False — `:213` reconstructs a cell and **delegates** to the same `Aggregator` at `:96`, `:97` and `:127`, so the fix propagates for free. I had inferred an independent decision path from a symbol match, `Primary = ScenarioCatalog.Get(...)`, without reading what the file does with it. Reading it produced the better finding, now in the directive: `:213` needs no change but the design does, because a phase-keyed basis needs a phase *on the cell* and neither `MatrixCell` nor `RunResult` has one — `RunId` lives on `RunnerOptions.cs:55` and reaches the artifact only as free text in `notes` via `Aggregator.cs:368`. **The denominator error came from not enumerating a set; this one came from not opening a file**, and only the first is caught by printing denominators. A grep hit locates a symbol and says nothing about the control flow around it.

84. **"Name the deliverable" is not advice about wording; it is the failure mode itself, and this run produced three instances in one hour.** Rule 82 recorded the implementer's point that apparatus-versus-artifact is a role rather than a location. The sharper fact is that *artifact* was already ambiguous inside a single step, because P0.5 publishes **two** files with overlapping column names and different contracts: the JSON result rows and the per-invocation CSV at `Publisher.cs:36-41`. `pauseAverageMs`, `gen0Collections` and the pause percentiles appear in both. Three claims failed on exactly that. **Mine, first:** P0.6's rule-80 deliverable said to publish `observedPauseCount` and `totalPauseMs` *"beside `pauseAverageMs` in the result rows"*, which names the JSON — but `rederive-statistics.py` takes both files (`:112-113`) and the quotient is formed **per invocation**, so the CSV is where the inputs have to land for the gate to evaluate the switch. **Mine, second:** carrying the phase precondition I wrote that the run id reaches "the artifact" only as free text in `notes`; the CSV has `runId` and `mode` as first-class columns (`Publisher.cs:37`), which is precisely why `rederive-statistics.py:134` can call `phase_of(row["runId"])`. True of the JSON, false of the CSV, and the gap is one artifact wide rather than two. **The implementer's, and it is the mildest:** that a JSON-only change leaves the instrument *"exactly as blind as it is now"* — too strong, since publishing all three fields in one row permits an intra-row arithmetic check that does not exist today. Weaker than the CSV fix, not equal to nothing. Its recommendation is right and improves the item twice over: put the fields in the CSV, and then **`pauseSource` becomes derivable rather than copied**, retiring a `COPY_FIELD` instead of adding two. **And the contract was written down the whole time.** `Publisher.cs:33-34` says the CSV's columns *"are the exact vector the bootstrap resamples, so a reader can recompute the published mean and re-run the interval without the harness."* `pauseAverageMs` sits in that header at `:39` and is the column the promise fails for — a quotient whose numerator and denominator are both absent from the file that promises recomputability. That is the third time this run that the sentence bounding a finding was in a file adjacent to the numbers and unread: the catalogue docstrings for rule 79, `:553` for the implementer, this for both of us. **Operational form:** when a field name appears in more than one published file, a claim or a deliverable naming that field is ill-formed until it names the file, and the check is mechanical — grep the name across the emitters before writing the sentence. **Measured afterwards, the surface is most of the schema:** of the CSV's 36 columns, **33 appear in the JSON rows too**; only `runId`, `mode` and `invocation` are CSV-only, and 22 keys are JSON-only, among them every `ratio*` field and `pauseSource`. So the ambiguous case is the default and the unambiguous one is the exception — which is why treating this as a wording nicety was wrong. Sweeping P0.6's directive under that rule found no further false claims but one weak one, now fixed: *"needs no re-run"* rested on `latencyP99Ms` being **68 of 68** in the published JSON, when `Aggregator.TryReadPrimary` reads the field from an invocation's **report**. The per-invocation CSV settles it properly at **340 of 340** latency-phase against **30 of 359** throughput-phase, and the entry now carries both. A projection agreeing with its source is evidence; a projection *standing in for* its source is the same error at one remove. **The same ambiguity governs file citations, and there it resolves to plausible wrong content rather than to an error.** Measured over the board's 60 `file:line` citations at the reviewed tree: 18 name a file that matches more than one path. `Program.cs` matches **four** files under the harness alone and 217 across the repository, and **two of the four have a line 200** — the Runner's is the arrival-rate mechanism, the Tests' is unrelated but readable — so the reader who resolves it wrongly gets something that looks like an answer. `Aggregator.cs` collides with `System.Diagnostics.DiagnosticSource`'s own aggregator, which is worse than a random collision because the wrong file is also about aggregation. P0.6's entry now declares its citation root. **And the sweep that measured this is the reason no citation checker was built.** A naive one flags 4 of the 60 as out of range — `P0.4-harness.md:947`/`:948` past a 946-line file and `P0.5-baselines.md:1114` past a 1112-line one. All four are correct: they cite lines a perturbation control *appended*, and the board says so in the sentence beside them, having verified both counts from `git show` when it wrote them. A checker with a 6.7% false-alarm rate on a healthy board is one that gets ignored, and an ignored checker is worse than none — which is the failure `board-check.ps1`'s own header already records, a staleness grep matching the very rule that documented the stale figure. The line-number claims are pinned to an immutable ref and cannot rot; **what needed fixing was the ambiguity, not the arithmetic**, and that took an edit rather than an instrument. **One more instance, from the sweep itself.** My first pass read the directive by line index, which rule 84's own insertion had shifted by one, and reported **zero** ambiguous field names in it — a clean bill of health produced by scanning a different line. That is rule 74 inside the audit meant to enforce rule 84: a non-arrival returning a plausible verdict, and the plausible verdict here was *pass*. The standing convention to locate board fields by content rather than index exists for edits; it applies identically to reads, and a sweep that finds nothing should be the most suspected result, not the most reassuring. **Fourth axis, and the sharpest form the rule takes: two different quantities landing on the same integer in one document.** P0.5's session reported that `inducedCollections` is constant across **699 valid invocations** and, in the same message, that the ratio directive yields 699 well-formed pairs — the first a count of CSV rows, the second a count of (cell, column) pairs, and the second serving as the denominator of a derived false-positive estimate. **The collision was manufactured by a keying defect, which is the sharpest thing this axis has to say.** Under the design key that rule 91 later forced, the second quantity is **812 well-formed pairs**; the shared integer existed only while the key pooled all three sessions into single arms of 10 and 13, and a coincidence produced by a defect is worse than an ordinary one because it reads as corroboration — the figure that was wrong is the one the agreement made look checked. The first figure is correct, the second was a different quantity computed over a population that was never run; nothing in either sentence distinguishes them, and a reader checking the arithmetic has no way to know which 699 the estimate divides. Naming the artifact does not help here, because both derive from the same artifact — what disambiguates is naming the *unit being counted*. So the rule's operational form gains a third clause: name the artifact, name the estimator, and name what the integer counts. — **Measured, and it retires the item's stated precondition: `pauseSource`'s load-bearing value is derivable from the CSV *as it stands today*.** Rule 84 concluded that moving the fields into the CSV would make `pauseSource` derivable rather than copied. It is already derivable. Over the union of all three invocation CSVs — **699 valid rows** (s2 595 + s3 80 + s4sdk 24), 36 columns — the predicate “`pauseAverageMs` parses numeric **and** `pauseP99Ms` is blank” selects exactly **25 rows**, because a total pause duration without individual samples *is* `total-pause-duration-only (event stream incomplete)`: the numerator survives, the percentile cannot. Those 25 are **set-identical** to the 25 selected by the disjoint criterion “blank `pauseP99Ms` with `gen0Collections > 0`”, group for group — `cyclic-garbage`/wks/throughput 9 rows at 132,257 collections, srv/throughput 8 at 89,045, `multi-thread-throughput`/wks/throughput 3 at 9,846, `cyclic-garbage`/srv/latency 2 at 15,097, wks/latency 2 at 13,894, `multi-thread-throughput`/srv/throughput 1 at 2,949 — two predicates over non-overlapping columns returning one row set. **The derived signal is strictly better than the label it would replace.** Those 25 rows span **10** distinct (scenario, collector, mode, heapFactor) cells, while a census of all three JSON artifacts finds only **8** `total-pause-duration-only` labels (8 in s2, **0** in s3 and s4sdk, matching the signature's own s2-only distribution), because the label is emitted under `ValidOutcomes[^1]` — last invocation only. The derived predicate is per-row and complete; the label under-reports by two cells. The same census independently settles the tally: 78 + 21 + 12 + 8 = **119** labels plus one `None`, the crashed `lifecycle-semantics`/wks/1.3 row. So the P0.6 item is real and its **precondition is false** — publishing the fields buys *diagnosis*, not *detection*, and a gate over today's artifact can already flag every damaged row. The bound that hid this is also false by cardinality: of 36 columns only **12** parse numeric in all 699 rows (six of them configuration or identity — `heapFactor`, `heapLimitMb`, `invocation`, `warmupSeconds`, `steadyStateSeconds`, `seed`), 26 parse numeric in at least one, and **14 are numeric in some rows and blank in others** — the ten latency/rate columns at 329 blanks each, `committedMb` 101, `pauseAverageMs` 131, `pauseP99Ms` and `pauseMaxMs` 156. **A field the artifact lacks is not the same as information the artifact lacks; look for a derivable proxy before ranking a schema change as a blocker, because “the column is missing” is a statement about the header and “the signal is missing” is a statement about the data.**
85. **A guard against a check matching *nothing* is not a guard against it matching *fewer things than before*.** `board-claims-check.ps1` was written with rule 74 in mind: a pattern that locates no sentence fails loudly, because a reworded claim otherwise retires its own check in silence. Its coverage control then found the case the guard does not cover. Perturbing **nine of ten** to **three of ten** moved that site outside its own closed alternation `(nine|\d+)`, so the pattern matched **one** site instead of two, the surviving site was the uncorrupted one, and the checker printed `1 occurrence(s), all agree` at exit 0 over a board that had just been falsified. Non-arrival was the failure mode I had guarded; **partial** arrival is the one that got through, and no non-empty test can see it, because the population that shrank is the population being counted. Two fixes, and the second is the general one: widen the pattern so corruption stays inside it *and is then rejected* rather than falling out of scope, and **declare how many sites each claim occupies**, so a site leaving is arithmetic rather than inference. The site count is the one number in that file not derived from the artifacts — it is a property of how often the board chooses to state a fact — which is exactly why it needs its own control, and each site is now proved individually load-bearing by removing it alone. The general form for anything that verifies by search: **a search-based check must assert its own hit count, or a claim can be deleted from under it without changing the verdict.**

86. **When coverage is missing because a *selector* picks one item, enlarging the set it picks from relocates the gap rather than closing it.** The measured case: `pinning-heavy-io`'s pause differs by 6.98x srv/wks on rows publishing a ratio of 0.9999, and the proposed repair was a third member for `PrimaryMetric`. But a selector returns one field per row whatever its domain, so a pause basis would have bought the pause ratio by giving up the throughput ratio — and the throughput ratio is not noise there, it is the finding that the I/O limiter binds. The tell is that the answer to "why does X have no statistics?" was "X is not selectable", which invites widening the domain; the real answer is that **selection was the wrong operation** and statistics should be computed for every comparable column, with the selector demoted to choosing which result leads. Two distinct questions had been conflated in one field — which measurement is this scenario's headline, and which columns deserve a confidence interval — and no value of the first can answer the second. Before accepting a proposal that adds a member to an enum, a variant to a mode, or a case to a switch, state which existing case now goes uncovered; if the honest answer is "one of the ones that was covered", the fix is one level up. A second instance, in a different instrument and reached by a different route: section 6.4's variance table omits `pauseP99Ms` because `derive-variance-table.py:28` is `PHASE_METRIC = {"throughput": "operationsPerSecond", "latency": "latencyP99Ms"}` — one metric per phase — so a column that is no phase's designated metric can never receive a row, and adding a third phase would not help, because pause is not a phase. Two instruments, two selectors, the same gap, neither closable by widening a domain.

87. **A derived figure that reproduces no published figure is a guess however close it lands, and near-agreement between two such guesses is not corroboration.** Two independent derivations of the same *absent* table row landed at 94 cells / median 8.52% and 96 cells / median 8.37%, agreeing exactly on `48` cells above the floor and on a `180.27%` maximum; agreement on the extrema made both look sound and neither was checkable. What settled it was reproducing first the two rows the document already publishes — `operationsPerSecond` 59 / 1.60% / 5.82% / 0 of 59 and `latencyP99Ms` 60 / 11.67% / 187.07% / 34 of 60 — which failed on the first attempt and named its own cause: `derive-variance-table.py` takes **one** CSV (`load_cells(args.csv)`), and unioning the three session CSVs charges cross-session drift to within-cell variance. The partition was right and the population was wrong, so the *cell counts* came out exactly right (59, 60) while every statistic was inflated (median 1.79%, max 13.27%, 3 above the floor instead of 0). **Cell counts agreeing is evidence about the partition and none whatever about the population.** Reproduce the published rows exactly before publishing a new one, and read a failed control as a statement about your derivation rather than about the document.

88. **Before building a checker for a class of error, run its predicate against the artifact when it is *healthy* and count the false alarms — a predicate that sounds exact can fire on most of what it is meant to bless.** Two candidate instruments for cross-artifact citation correctness were killed this way, both proposed as exact and both measured before any code was written. *Citations must carry a verbatim fragment of what they cite*: across the eight `rule N` citations in P0.5's deliverable, independently verified as **4 of 4 distinct rules correct**, only **2 of 8** carry a 5-or-more word verbatim run — the predicate flags six correct citations, a 75% false-alarm rate on a document with no citation defects at all. *A cited identifier must be stable, so a rule's text may gain sentences but never be replaced*: across the **100** commits touching this board up to `a4eb4dbaedb`, **22** edits extend a rule and **25** replace text in place, because correcting a rule where it stands is rule 22 operating as designed. The pin is not decoration and was not there when this was first written: the very next board commit, `5d3246b1163`, appended rule 84's fourth axis in place and moved the extension count to 23, so a sentence that was exactly true when committed was false one commit later — see rule 90. The second measurement also refuted the estimate that motivated it — the scan was written expecting a single violation, and its hardcoded closing line asserting "the one real identity change" shipped in the same output as the twenty-five that contradict it. What survives is the decidable residue, which is one line: **every cited rule number must exist**, currently 0 dangling across both documents. The rest is discipline — resolve the text out of the shared object store and quote it when the claim leans on it — and that discipline caught a real miscitation this week where neither checker would have. — **And the rate is predictable before you measure it, from the index's density in the range being searched.** The implementer's F38 prose-figure checker scans only comma-formatted integers (≥ 1,000). That reads as a stylistic scope choice; it is in fact the threshold at which the check's evidence becomes valid. Measured against P0.5's deliverable and its three invocation CSVs (**1,041** distinct invocation values, **377** cell means): as written the scan extracts **24** prose figures and reports **0** single-invocation leaks. Widening the *same* predicate to bare sub-1,000 integers extracts **696** figures and reports **74** leaks over 24 distinct values — and every one is spurious: `16 MiB` the search floor, `128 MiB` a heap limit, `166 probes`, `474 preserved result files`, `99 MiB` a heap minimum, and `72` matched from inside a SHA-256 digest. A **100%** false-alarm rate, worse than the 75% that killed the verbatim-fragment predicate above. **The cause is not loose writing but index occupancy:** of the integers in [1, 1000) the invocation index holds **275 of 999 — 27.5%** — against **2.8%** in [1000, 10000) and **0.04%** above 10,000. When a quarter of the value range sits in the index, exact equality is not evidence; coincidence is the expected outcome. The predicate never changed and its precision fell to zero purely by moving the range it searches. **Before widening a matcher's scope, compute what fraction of the new range the index already occupies — precision is a property of the index rather than of the predicate, and a scope restriction that looks like fussiness is often the whole reason the check works.**

89. **Before adopting a threshold, compute the best value the design can attain and check that the threshold lies inside it — a claim can be *unreachable* rather than merely unlikely, and no amount of data rescues it.** The exact one-sided permutation test at five invocations against five enumerates `C(10,5) = 252` arrangements, so the smallest attainable p is **1/252 = 0.003968** and the largest usable one is 12/252 = 0.047619. Bonferroni over an interpreted family of size *m* demands p ≤ 0.05/m: at **m = 12** that is 4.1667e-3 and the floor clears it; at **m = 13** it is 3.8462e-3 and the floor does not. So **twelve is the largest interpreted family in which any cell of this design can be called significant** — above it, a cell whose arms separate completely, with every one of the 251 other arrangements less extreme than the observed one, still fails correction. Rule 86's "significance for every comparable column" over 693 well-formed pairs would need 7.2150e-5, which is **55** times below the floor: the directive is not noisy at n=5, it is **unattainable**, and that is a property of the design rather than of the measurements. Two escapes exist and both are design changes rather than analysis choices — hold the interpreted family at or below twelve, or raise n, since `C(12,6) = 924` moves the floor to 1.0823e-3 and the budget to **46**. This is the dual of rule 88 and the pair should be read together: there a predicate fired on artifacts that were healthy, here a predicate cannot fire at all, and both failures are invisible until the predicate is evaluated against the *design* rather than argued from its wording.

90. **A measurement over a population that your own commit joins must be pinned to a hash in the same sentence, or it is false as soon as you write it down.** The rule-88 figures — 100 commits touching this board, 22 extensions, 25 in-place replacements — were exact when committed at `a4eb4dbaedb`. The commit that *stated* them left them intact, but the one after it, `5d3246b1163`, appended rule 84's fourth axis in place, which is an extension, and the count became 23. The checker caught it and the board did not, because a figure ages out silently while the sentence around it still reads as a finding. **Note what distinguishes this from ordinary staleness: the falsifying edit was not related to the claim, was not a correction of it, and could not have been anticipated by whoever wrote it** — every future edit to this file is a draw from the same population, so an unpinned self-referential count decays on a schedule set entirely by unrelated work. This is rule 84 turned reflexive: there the fix was to name the artifact and the unit, here it is to name the **revision**, and the fix is one hash in the prose plus the same hash in the walk that derives it. The general form is broader than git history — any statistic over "everything of kind K so far" where the writer is still producing things of kind K needs the same treatment, and the tell is that you cannot state the number without the act of stating it being in scope.

91. **Before using a column as a partition key, check the group sizes it produces against the ones the design can produce; a size the design cannot produce means the column is not the partition.** The P0.5 invocation CSVs carry both a `runId` and a `mode`, and `mode` names a phase, so keying comparison cells on `(scenario, heapFactor, mode, collector)` looks exact and reads as exact. It is not: **30 of the 699 valid rows carry `mode=latency` under a `.throughput` runId**, because a run executes both passes, so `mode` crosses runs rather than partitioning them. The pooled key produced arms of **10 and 13** where the design produces **5**, and that impossible size is the whole detection: re-keyed on `(runId, scenario, heapFactor, mode, collector)` the population is **135 arms of 5 and 8 of 3, with none of 10 or 13**. The cost of not checking is not a rounding error but a reversed conclusion. Rule 89 computes the attainable floor as `1/C(2n,n)`, so at the artifact 10-versus-10 the floor reads **5.41e-06** against the **7.22e-05** a 693-wide family needs and rule 86 comes back **achievable**, which is the exact claim rule 89 exists to refuse; at the design five-versus-five it is **3.97e-03**, **55.00x** short. A derivation that computes its own attainable range from the data inherits every keying defect in that data, so the key is checked before the range, not after.

92. **A rank can be invariant under a change of basis while the values it ranks are not, so a claim citing both must declare the basis for the values even though the rank does not need one.** P0.5 section 6.3 carried a superlative falsified by ranking `aspnet-request-load`/wks **9th of 20** arm-series by relative 1.3x-to-6.0x latency change, and that rank is **9 of 20 under all four** combinations of published-aggregate-versus-mean and pooled-versus-runId-keyed, so the falsification needs no basis declared and is as strong as a measurement over this data gets. The percentages published beside it do not share that property. `large-object-pressure`/wks is quoted at **-82.3%**, its value over means, where it ranks **3rd of 20**; over the last-invocation aggregates this document publishes everywhere else it is **+6.7%**, ranking **19th of 20** - opposite in sign and all but last in the ordering it was cited to illustrate. Two of the eight series exceeding the subject swap out in each direction between the two bases. The conclusion survives and the illustration inverts, so a reader checking the vivid number against the table above it finds a contradiction the conclusion never had.

93. **A capture group is a window, and a corrupted value can be wider than the window the pattern opened for it — which is not one failure but two, and both misdiagnose.** If the wider value still matches, the check reads the truncation and quotes it back as though it were what the text says: `([\d,]+)` over a board reading `1019.11 pairs` captures `1019`, so `1,018.5 pairs` would have passed outright. If it does not match, the check reports the *sentence* as reworded and silently retired when the sentence is intact and only the number moved — pointing the reader at the wrong artifact entirely. Both were live here, and this is the third instance of one class after negatives excluded by a `^\d+` shape test and thousands separators excluded by a bare `\d`, so the class is what gets fixed and not the instance: every numeric capture admits a decimal fraction, and every captured numeral is then expanded to the full numeral it sits inside before comparison. Expansion is asymmetric — leftward over digits and separators, rightward over digits and a following fraction but never over a comma, because `870, 92` is two figures and must not merge into one. The same root shows up in the matching direction as an unterminated literal: `0 of 69` matched four times whole-text against three terminated on the board as this was found, because a figure written later, `0 of 693`, contains the earlier one as a substring — so a check obeying rule 85 and asserting its own hit count reports `4, expected 3`, which reads as *a site was added to the board* when the board's sites are untouched. Writing this rule down moved those counts to **six and four** as of this revision, since the sentence you are reading contains both literals: the statement of the count changed the count, which is rule 90 arriving from underneath — a self-referential figure is not merely fragile against later edits, it can be falsified by the edit that records it. The general form: **a numeral has two boundaries and a check must assert both** — termination in the pattern, width in the capture — because **a check's domain is the values its text could hold, not the values its author expected to read**, and a pattern narrow enough to be readable is narrow enough to be wrong quietly.

94. **A control whose subject list is restated rather than derived cannot report “every”; it reports “every one I was told about”, and the difference is invisible in a green run.** `board-check-coverage.ps1` holds a hand-written case per check, and the moment a fifteenth check was added to `board-check.ps1` it printed **every check fired on both of its distinct payloads (29 cases)** and exited 0 with that check at zero coverage. Nothing was broken and nothing was skipped — the control simply could not see a subject it had not been handed, so its headline was true of its own list and false of its subject. This is the shape of the implementing session's F25 in a second artifact: work that never reaches the verdict, under a word that says it did. Where restating cannot be removed — a perturbation has to be written by hand for each check, unlike the sibling battery which extracts its patterns from the checker it audits — the **count** is asserted against the subject instead: the subject reports how many checks it ran, every one must own a distinct case, and a mismatch is a failure rather than a smaller number. Watched failing before it was trusted: dropping the two new cases yields `COVERAGE INCOMPLETE: board-check runs 15 check(s) but this battery declares payloads for 14`, exit 1. The general form: **a control must derive its subject from the subject, and where it cannot, it must at minimum derive the subject's size** — otherwise the next thing added to the subject is unaudited by construction, and the audit says otherwise.

95. **The basis on which a figure is published is a property of the field, not of the document, so “this document publishes basis X” is a universal over columns and has to be measured one column at a time.** A single file, `p0-5-baselines-s2.json`, carries two aggregation families with no overlap and no residue: `latencyP99Ms` is the arithmetic mean of its five invocations in **60 of 60** cells and the last invocation in **0 of 60**, and `operationsPerSecond` is a mean in **113 of 113**, while `pauseP99Ms` is the last invocation in **84 of 84**, `pauseAverageMs` in **92 of 92** and `workingSetMb` in **113 of 113**, each a mean in none. The coordinator established the last-invocation rule on the pause fields, carried it to a latency table, and reported an undeclared basis against a table whose basis was declared in the sentence directly beneath it; both halves were false and false for different reasons — one about the document, one about the field. The first attempt to measure this said **54 of 60**, which looks like a rule with six exceptions worth explaining and was instead a pool keyed without the run: rows from the throughput run also carry `mode=latency`. A near-universal with an unexplained remainder is a keying defect until proven otherwise, and the corrected key is total in both directions.

96. **A correction rewrites a sentence, so it inherits every number in that sentence: the one it came to fix and the ones it did not.** P0.5 section 6.3’s blockquote read **40 of the table’s 40 cells** are means **and 1 is the last**. The rewrite moved the cell count to **60**, which is correct and was the point of the edit, and the coincidence count to **4**, which is not: at the document’s own printed precision that count is **1** — exactly the value the correction replaced — and it is **0** exact, **5** at two decimals, **2** under “equals some invocation”, and **4** under nothing. One clause, one edit, one figure fixed and one broken, and the broken one had been right until the correction touched it.

97. **A check’s subject list is part of the check: enumerate what the tree actually holds rather than the kinds of file you had in mind, and make the check assert that extent.** P0.5’s finding-id check resolves ids cited from `*.cs`, `*.py` and `*.sh` and reports **12 cited, 12 resolved**, which reproduces exactly under its own rules. The same tree holds **6** `.ps1` scripts, they cite **4** finding ids, and one of those — `F17`, at `measure-open-loop-rates.ps1:8` — occurs **exactly once** in the whole `docs` tree and resolves to no document. The check written to catch dangling finding-id citations ships with one standing beside it, and the file type it omits is the one the data-producing run scripts are written in. This is the fourth subject-too-narrow defect in the same pair of checks, after the silent skip, the blindness to the `F<n>` form, and basename resolution, so the operational form is not “widen the list” but “derive the list and print its size”.

98. **Over a lagged channel a state report is a timestamp, not a fact: it can be true when written and false when read, so the reader must re-derive it before acting rather than blame the sender for the delay.** *This rule’s first draft said the opposite — “re-read HEAD before asserting it” — and cited the P0.5 session reporting `HEAD = 6cdeea5ba1e` while its tip was `5858cc4616e`, and quoting `132,267` collections after its own commit `750a708dde4` had corrected the figure to `132,257`.* **Both charges are false.** Measured: `git merge-base --is-ancestor 750a708dde4 6cdeea5ba1e` returns non-zero — the correction did not yet exist at the revision the message was composed at — and `1a3cffe35c0` really is four commits above PR #5, exactly as its next message said. Every state report the session sent was accurate when it left. The lag is the channel’s: replies queue, and each describes a tree that has moved by the time it arrives. The obligation is therefore the **reader’s** — re-derive from the object store, which costs one command — and the writer’s only duty is to name a hash the reader can position. The first draft got the diagnosis right in its own words (“true when written and false when read”) and then drew from it a prescription that would have changed nothing, because re-reading HEAD returns the same answer the sender already had.

99. **A count does not survive a change of population, and a counterfactual about a different population has to be recomputed rather than carried — including the part of it that flatters the other side.** Conceding the pause-variance population, the P0.5 session added that on its own n≥3 basis the coordinator’s “exactly half the cells above the median” would have been `48 of 94 = 51.06%` and so slightly wrong. Measured: at n≥3 it is **47 of 94**, which is *also* exactly 50.0%. It held the numerator fixed while moving the population, but the median moves with the population and the count above it recomputes. Rule 96 governs the numbers a correction inherits from its own sentence; this governs the numbers an argument carries into a population it is not using. Both are the same failure to re-derive, and this one is harder to see because the conclusion it supported was a concession.

100. **“Occurs” is not “is defined”: a resolution check whose predicate is occurrence passes on any mention, including a range row that defines nothing and the prose recording that nothing is defined.** P0.5’s summary table collapses nine findings into one `F13–F21` row. Measured across that tree: **F17 occurs in no markdown at all** (0 occurrences, 1 citation from `measure-open-loop-rates.ps1:8`), while **F13 occurs 8 times and is defined nowhere** — its only occurrences outside the range row are the prose documenting its absence. These are two different defects in one check and they need different fixes: F17 falls to rule 97 (widen the subject to the `.ps1` files the tree actually holds), F13 does not fall to it at all, because widening the subject cannot change a predicate. The coordinator reported F17 as a subject defect and was right about F17 and wrong that the subject was the whole of it. When a check resolves an identifier, state whether it demands a *definition* or merely a *sighting*, and make a range row fail.

101. **A band has two edges, and a counterexample confirms only the edge it arrived from: checking the near end of a range says nothing whatever about the far end.** The coordinator falsified “aspnet’s latency sits two to three orders of magnitude above the others” by finding scenarios too *close* to it, and reported the claim as holding for 6 of 9. Measured against the declared basis, the split is **3 / 3 / 3**: three scenarios nearer than 100x (`lifecycle-semantics` 8.6x, `long-lived-cache` 19.5x, `large-object-pressure` 49.6x), three inside the band, and three *beyond* 1000x (`multi-thread-throughput`, `pinning-heavy-io`, `pointer-chasing`, the last at ~3105x). It holds for **3 of 9 and fails for 6** — the falsification was correct and undercounted itself by half, because the search stopped at the end the first counterexample came from. Enumerate the whole ordering; a bound stated as a range needs both of its inequalities tested.

102. **Re-deriving a published figure, change nothing you are not forced to change — and when a subset disagrees, the identity of that subset names the change that did it.** Re-deriving P0.5’s ten scenario means the coordinator moved population *and* estimator at once: pooled raw invocations over the union of all three sessions, where the table declares the mean of six per-cell means over s2 alone. Six figures agreed and four did not — `long-lived-cache` 23.92 against **23.17**, `large-object-pressure` 10.61 against **9.10**, `allocation-churn` 3.29 against **3.46**, `low-allocation-compute` 0.43 against **0.56** — and the four are **exactly the set s3 re-ran** (set equality verified, not eyeballed), because pooling raw double-weights whichever cells a later session added five invocations to. Both defects were already on this board: rule 87 for the population, rule 95 for the basis. The recovery is the useful half: a partial disagreement is more diagnostic than a total one, so name the disagreeing set before theorising about the cause.

103. **A citation resolves to a number, not to a claim, so correcting a rule in place silently re-targets every citation of it — and an existence check is structurally unable to see that.** Walking all 107 board commits that touch `roadmap.md`, exactly **three** rule numbers have had their headline claim *replaced* rather than extended or amended: 50 and 51 at `be5bc610e00`, and 98 at `0f13853a89b`. All three still resolve, so a dangling-citation check returns clean on every one of them. The P0.5 session offered rule 50’s replacement as “a better argument for the dangling check”; it is an argument for the opposite, because rule 50 was **overwritten, not deleted**, and overwriting is precisely the case existence cannot detect. Blast radius today is zero — 0 self-citations on the board and 0 in the child’s tree at `79da938240d` for all three numbers — but that is a property of what happens to be cited, not of the practice. Since rule 22 *requires* correcting in place, this board’s numbers are mutable by design, and a citation of one must name the revision it was written against exactly as rule 14 already demands for reference locations. **Corroborated independently one round later, against this board.** The P0.5 session audited its ten distinct rule citations at board commit `2d4d196d8c7` and found one whose *number* resolved and whose *content* did not: it quoted rule 84’s operational form as “name the artifact, name the estimator, name the unit” where this board’s third clause reads “name what the integer counts” — and the compression dropped exactly what that clause was added to supply, since naming a unit still does not say which of two coincident figures the arithmetic divides. Existence was never in question: 10 of 10 resolved, 0 dangling, verified here. The defect lives in the fit between the citation and the claim it is offered for, which is why the practice that catches it is resolving the cited text and quoting it — not testing that the number exists.

104. **Search the board for a rule’s subject before adding it; a rule that exists and is not read is worth nothing, and the recurrence it was written to prevent will not announce itself.** All **three** in-place headline replacements in this board’s history are withdrawn accusations against the same child session — 3 of 3, the entire population of identity drift here. Rule 50’s corrected headline, written by me at `be5bc610e00`, says *“check the clock before charging anyone with staleness”*. Forty-eight rules later I wrote rule 98 charging that session with reporting stale state, and retracted it after running `git merge-base --is-ancestor 750a708dde4 6cdeea5ba1e` — the clock check rule 50 already prescribed, against the same session, for the same mistake. The prophylactic was in this file, under a number I had written myself, and nothing in the act of appending rule 98 required me to read it. Growth of the rule set is not the same as growth of the practice.

105. **Rot and narration-ahead are different defects with different fixes: rot was true and stopped being true, narration-ahead was never true.** The distinction is the P0.5 session’s, returned against an attribution I had made — I filed its F26 as the third instance of “a conclusion written ahead of its data”, and F26 records what was found, with a citation that was correct when written and became wrong when the file grew above it. This board carries an instance of the same class: rule 13’s reference moved from `src/plan/barriers.rs:317` to `:315-319` at `e02c462a27e`. Only narration-ahead is fixable by verifying before asserting; rot needs an anchor that survives movement, which is why a rot finding can legitimately ship unfixed. The count of three survives with a different member — the §6.3 Kestrel clause, which the session supplied against itself: reviewed, its citation resolved, reported “unchanged and correct” by a check that could not reach either the causal or the comparative claim.

106. **An edit’s net effect is not its intent: when an insertion moves a count by less than it added, the difference was destroyed by the insertion itself — and a tally of what you added cannot detect it.** The P0.5 session’s `79da938240d` inserted two `### F<n>` headings and the document’s heading count rose by **one**. Verified here at four revisions: `### 9.1` is present at `9f054925372` and `a46333b9b7d`, **absent** at `79da938240d` — leaving 180 lines of §9.1 hanging under `### F34` — and restored at `609009f6fdb`. Counting `###`+`####` gives 24 → 32 → 33; counting `##`+`###`+`####` gives 33 → 41 → 42. The finding holds under either basis and the constant +9 between them is exactly the nine `##` headings, so declaring the basis converted an apparent contradiction between our two counts into agreement — rule 95 earning its keep on someone else’s artifact. **The second half is why nothing caught it:** the gate at that revision touches headings in exactly one place, the `### F<n>` definition test added for rule 100, so findings headings were covered as a *by-product* of resolving citations and every other heading in the document was unowned. Coverage acquired as a side effect of a different check is coverage of that instance, not of the class. And the damage was done by the commit acting on rule 100 — which is to say by work I asked for: an insertion that satisfies the check it was written for can still destroy the artifact around it. The session’s own account adds that its first structural check fired on the correct document as well as the broken one; that is rule 88 already, run its predicate against a healthy artifact and count the false alarms, so it is cited rather than restated.

107. **A caveat that accurately bounds one claim makes the unbounded claims beside it look audited.** F27 published a collection total of `132,267` for the nine `cyclic-garbage`/wks/throughput rows that lost their pause samples, and the review of that finding said — correctly — that summing the counters double-counts, but that the `>0` versus `==0` split is monotone in the total, so the 105/25 boundary is identical under either reading. Every word of that is true and I have re-derived it from the raw per-invocation CSV rather than argued it: over the **130** blank `pauseP99Ms` rows the split is **25 / 105** under gen0-only, under gen0+gen1+gen2, under those plus induced, and under gen1+gen2, moving only to **13 / 117** under gen2-only, which is no reading of “the total”. But `132,267` is not a boundary. It is a magnitude, three sentences up, produced by precisely the sum the caveat had just named as double-counting. **Naming a defect and then reasoning about it in one direction is worse than not noticing it**, because it converts an unexamined number into an apparently examined one — and neither party challenged the magnitude afterwards. The correct figure is **132,257**: `gen0Collections` alone, which `GcTelemetry.cs:233` documents as counting every collection of generation 0 *or higher*, so the generation counters are nested and the sum adds the 10 gen1 collections a second time. I derived `132,257 / 10 / 0` over those nine rows myself. It is P0.4’s `MeanPauseMs` triple-count recurring inside a document that cites that defect. **A caveat must travel with the number it covers rather than with the finding it appears in, and naming a systematic error obliges a sweep of every figure that error touches — not an argument that one conclusion survives it.** *The reviewing sentence is quoted to me as mine and I neither claim nor disclaim it, per rule 64: my own sent messages are not in my retrievable record, `assistant_response` being far too short to hold them, so a search for the phrase returns nothing whether or not I wrote it — rule 40’s probe that cannot see its subject, caught this time before the conclusion was drawn. Rule 64 already records this session declining unverifiable authorship in the opposite direction. What is verifiable, and is the rule, is that both parties read the finding and neither corrected the magnitude.*
108. **A zero-width interval has at least four causes and only one is the defect the detector is hunting; the discriminator is whether more data dissolves it.** This corrects the shape of the degeneracy check proposed for P0.6, which would “compute universally” and flag every constant cell. Measured over the P0.5 invocation CSVs with cells keyed on (scenario, collector, mode, heapFactor) — **113 cells**, s2 595 rows, union 699 — **five** columns are degenerate in **113 of 113** cells and not one of them is a finding. `heapFactor` is degenerate **because it is a member of the cell key**: grouping by a variable makes it constant inside every group, so a detector applied to partitioned data necessarily fires on its own partition, and it fires hardest on the variable the analysis is *about*. `heapLimitMb`, `warmupSeconds` and `steadyStateSeconds` are configuration echoed into the metrics file. And `inducedCollections` is the case curation cannot reach — a **genuine measurement**, neither a knob nor a key member, constant at zero in every cell because nothing induced a collection. An exclusion list built by asking “is this a knob?” keeps it, and it fires anyway. **The discriminator is the implementer's own monotonicity lemma turned into an instrument.** Adding invocations to a cell can break a constant vector and can never create one, so degeneracy is non-increasing under more data — therefore the degeneracy that *survives* more data is structural, and the degeneracy that *dissolves* was undersampling. Exactly **16** cells gained invocations between s2 and the union, touching exactly the four scenarios s3 re-ran, and exactly **four** columns moved: `arrivalRatePerSecond` 54→48, `gen1Collections` 60→57, `gen0Collections` 48→45, `gen2Collections` 83→82, totals 830→817. Those four are the true positives; the five that did not move are structural. So the predicate is not “is this column constant” but **“is this column constant *and* does it stay constant when the cell grows”** — mechanical, needing no curated list, and classifying the grouping key correctly for free. **A check that reports a true statement about the partition rather than about the data is not a false positive; it is a correct answer to a question nobody asked, which is harder to notice and much harder to argue with.**

## P0 — Parity contract, reference oracle, and measurement baseline
- **Status:** planned
- **Summary:** Establish what parity means before writing any collector code: build both candidate oracles, extract the paper's real acceptance targets, enumerate every mechanism to be ported, and capture honest built-in GC baselines.

### P0.1 — Pin and build both reference revisions

- **Status:** done
- **Summary:** Both oracle pairs built under WSL2 and verified genuinely LXR by runtime banner rather than exit code; HEAD is fully observable, PLDI is not, and the mechanism-organized diff with per-mechanism oracle recommendations and coupled sets is delivered in `docs/design/lxr-port/`.
- **Correctness:** HEAD `(0682434, 304ce69d)` passes 96/96 release runs with zero crashes or panics; PLDI `(abbdd1d, df8d30a3)` **fails outright** at 3/8 and 5/8 single-iteration and 1/8 and 2/8 sustained, tripping its own barrier assertion `assert!(old.is_null() || rc::count(old) != 0, "zero rc count")` at `plan/barriers.rs:315-319` (condition at `:316`) in `FieldLoggingBarrier::slow` — a field overwrite whose old target already has RC zero, gated by `cfg(any(sanity, debug_assertions))`, which is why release SIGSEGVs on the same corrupt state instead of panicking. The instability is **not** explained by revision skew: a differential run at core `4d4e516c` agreed with the pin in all 24 antlr/`-n 5` cells and produced the same pass set, disproving the hypothesis that the pin's `Fix stw LXR` commits were the difference. **Corrected in P0.3:** HEAD's clean runs are not evidence that the RC invariant holds there, because at `304ce69d` no barrier-side check exists at all — the only `zero rc count` assertion is in `util/sanity/sanity_checker.rs:311-316` (condition at `:312`) behind `cfg(feature = "sanity")`, which is not a default feature and was not enabled by the build recipe, so it was compiled out. The comparison was never run.
- **Benchmarks:** Reference-only numbers, no .NET comparison; recorded per configuration with invocation counts and every failure listed. Anomaly carried to P0.3 and closed there: hsqldb p99 of 0.34-0.40 ms on PLDI versus 54-56 ms on HEAD like-for-like at `-n 5` and 2000 MB, two orders of magnitude on the metric LXR exists for, from one benchmark on a noisy host, traced by P0.3 probe 2 to induced-collection policy rather than any LXR mechanism.
- **Dependencies:** none
- **References:** `docs/design/lxr-port/`; oracle pairs (`abbdd1d`, `df8d30a3`) and (`0682434`, `304ce69d`); secondary builds at core `4d4e516c` and `9625c174`, behaviorally indistinguishable; `plan/barriers.rs:315-319`; `src/args.rs` `LAZY_MU_REUSE_BLOCK_SWEEPING`

### P0.2 — Extract paper acceptance targets from the PDF

- **Status:** done
- **Summary:** Targets extracted and independently re-verified against the local PDF; the paper is the arXiv Extended Version of 1 Nov 2022, not the PLDI proceedings, so citations name arXiv table and page numbers and proceedings numbering is not assumed to match.
- **Correctness:** Every quoted number cites a table or figure in the local PDF, cross-read four ways including a rendered-page check, with the coordinator independently confirming Tables 1, 5, 6 and 7 from the pixels; two errata were found in the paper itself, Table 7's `!Lazy%` summary rows printing a minimum of 22 above a maximum of 2 against a true range of 0 to 22, and a 90th-percentile column that section 4 promises "in tabular form" but Table 4 does not contain.
- **Benchmarks:** Verified targets are throughput geomean `0.958` versus G1 at 2x heap, LXR-only pause p50/p95 of `5.0`/`7.5` ms mean and `3.0`/`4.7` ms geomean, reclamation split `94.3`/`0.6`/`5.1` from the mean row, and barrier overhead `1.016` geomean measured against a no-barrier full-heap Immix build rather than against G1.
- **Dependencies:** P0.1
- **References:** `C:\github\lxr-reference\paper\2210.17175.pdf`; `docs/design/lxr-port/P0.2-paper-targets.md`; Tables 1, 4, 5, 6, 7

### P0.3 — Build the mechanism parity ledger

- **Status:** done
- **Summary:** Delivered 26 rows across seven groups, each declaring its oracle, provenance tag, .NET realization and closure evidence, merged as PR #3 (squash `8a63e85178b`). Two findings changed the plan: LXR declares pinning unsupported, so row R05 has declared oracle `none` and must be closed port-side; and induced GC is serviced at HEAD rather than ignored, the earlier claim having rested on citations behind a non-default feature.
- **Correctness:** `scripts/verify-ledger.sh` run with no argument against a `git archive` extract of the merge commit `8a63e85178b`, outside any repository: 127 citations resolved, 50/50 byte-identical against the read-only reference, 11 flagged / 11 acknowledged / 0 unacknowledged, 26 index rows / 26 detail blocks matching §4's self-reported total, one declared oracle per coupled group, `C06 -> R05` redirect intact, 0 trailing whitespace, exit 0, byte-identical across two runs. The gate rejects citations to code that is not compiled, self-locates to the tree it ships in, and ships `verify-gate.sh` as its own positive control requiring PASS / FAIL / PASS on a copy differing by exactly one row.
- **Benchmarks:** None directly; the ledger defines which mechanisms later benchmark deltas must be attributable to. It also closed P0.1's hsqldb p99 anomaly (PLDI 0.34-0.40 ms versus HEAD 54-56 ms) rather than carrying it: probe 2 proved the cause is induced-collection policy and not an LXR mechanism difference, since hsqldb's `MemoryWatcherThread` calls `System.gc()`, HEAD services it as a full stop-the-world collection, and PLDI cannot because the body of `handle_user_collection_request` is commented out.
- **Dependencies:** P0.1, P0.2
- **References:** `docs/design/lxr-port/P0.1-mechanism-diff.md`; paper §2-§3; head `mmtk-core/src/plan/lxr/*` versus the PLDI `lxr` feature closure over `src/policy/immix/*`, `src/util/rc.rs`, `src/util/cm.rs`; `mmtk-openjdk/openjdk/barriers/*`

### P0.4 — Build the benchmark harness and scenario matrix

- **Status:** done
- **Summary:** Harness landed on `main` as squash merge `f6e93305259` (PR #4, 48 files, +10,994), covering all ten scenarios with a runner, worker, control suite, verification gate and gate-of-the-gate. No runtime, EE or JIT change proved necessary: effective GC configuration reads back through `GetConfigurationVariables`, tiering and PGO through `TieredCompilationSettings`, the future collector arm through `GCName`/`GCPath`, and bounded dumps through `DbgEnableMiniDump`.
- **Correctness:** Verified at the merge commit rather than at the attested tip, because the squash means `f5f1c6ed8b3` is not an ancestor of `main` and no attested hash carries. Tree identity was checked instead and holds across the whole repository, not just the harness path — the tree hashes of the attested tip and `f6e93305259` are equal, so the squash neither dropped nor swept in anything. The gate was then run from a `git archive` extract of `f6e93305259` outside any worktree: `PASS` with a full smoke results set, `exit 2` and **zero** `RESULT: PASS` lines when results are absent, `exit 2` on an empty results directory, `exit 3` and `DEGRADED` under `--no-results`, and `exit 1` naming the gap when pointed at a partial two-of-ten-scenario run. Two High review findings were fixed and regression-tested: `MeanPauseMs` triple-counted collections because `GC.CollectionCount(0)` counts gen0-or-higher, reporting 1.9306 ms against a true 3.9578 ms with a factor that varies by generation mix and so does not cancel between arms; and the bootstrap interval was zero-width at n=1, which made any ratio other than exactly 1.0 read as significant on the default smoke path.
- **Benchmarks:** No collector comparison is published here — this step builds and proves the instrument, and P0.5 measures with it. The measurement result that does bind later steps is negative: control 7 could not reliably resolve a 2% injected effect on this host, reaching the 1.6% target at n=8, at n=15, and not at all across three runs of the same binary, with direct paired probes returning 1.0221, 0.9649 and 0.9922 — a 2% signal under a ±3% single-invocation spread with the sign flipping. That is the paper's 1.6%-inside-noise problem reproduced locally. The assertion was therefore split: a coarse ~16.7% injection is asserted, a null replicate guards against manufactured effects, and the barrier-scale arm is measured and published without gating the verdict.
- **Evidence:** Run artifacts are gitignored and lived only in the child worktree, so 919 files (10.36 MB) covering `runs/`, `smoke/` and `probe/` were copied to session state before that worktree was removed, verified byte-identical by SHA256 with a negative control proving the comparison can detect a difference. Crash dumps were excluded as regenerable bulk. Without this the numbers quoted in §8 of the design document would have had no surviving source.
- **Dependencies:** none
- **References:** paper §4 methodology

### P0.5 — Measure Workstation and Server GC baselines

- **Status:** in_progress
- **Summary:** Baselines captured for both built-in collectors across the ten-scenario matrix at three heap factors, published with the raw data needed to re-derive every statistic. The step delivered what it was asked for, and what it measured is that this host cannot support the acceptance criterion as written: absolute results are not reproducible across sessions, so the comparison must move to within-session ratios.
- **Correctness:** Tested and **returned negative**, which is the step's most valuable output. 12 of 16 cells re-measured in a second session disagree beyond both their own spread and this host's measured 8.35% resolution floor — median 14.5%, max 89.6%. Decomposed rather than averaged: 6 cells had a different offered load (F23), and of the 10 comparable cells 7 still disagree. All 8 throughput comparisons moved the same direction, a 1-in-256 event under noise, tracking background load at 12.73% against 16.41%. The variance was recorded rather than averaged away, so the criterion's *method* held even as its *claim* failed.
- **Benchmarks:** `wks` and `srv` only, verified through the live canvas — 3 checkpoints, 144 collector fields, 0 naming LXR, 0 load problems. Within-session `srv`/`wks` ratio drift is 7.28% median against 19.06% for absolutes, and **three of four** throughput ratios fall below the 8.35% floor — the fourth, `long-lived-cache` at 8.74%, sits just above it while still being 3.3× more stable than its own absolute drift of 28.49%. The conclusion is unaffected: **every later step that measures LXR must do so against a baseline arm run in the same session and treat the ratio as the quantity of record** — recorded as rule 29 rather than assigned to a single step, together with the finding that the shared heap limit itself moves when LXR joins the calibration; absolute throughput here carries ±28% session-to-session, several times anything the paper reports. Throughput CV is 1.60% median with 0 of 59 cells above the floor, while p99 latency CV is 11.67% median with 34 of 60 above it — all four figures recomputed from the committed CSVs — so the quantity LXR is meant to win on is the one this host resolves worst, which sets a hard limit on what any later latency claim can assert.
- **Evidence:** PR #5 at `9f054925372`, 57 files, all under `docs/design/lxr-port/`. Verified from a `git archive` extract outside any worktree: gate `RESULT: PASS (34 checks)` exit 0 with exactly 1 anchored `^RESULT: PASS`; `--no-results` exits 3 and an unknown flag exits 2, each with 0 anchored PASS lines; control `RESULT: PASS (11 of 11 cases)` with its summary derived rather than written — "passed 2 clean extract(s), refused 1, and failed 8 single perturbation(s)". The prose checker re-derives 12 quantified claims from the shipped CSVs, and falsifying any one yields exit 1 and `1 of 12 prose claims disagree`. Eight commits past the first attestation, four of them corrections found after the step was first declared finished: a false four-ratio sentence, a set check a superset could satisfy, a gated count restated where `find_anchor` cannot see it, and a set of file counts each individually true but labelled so that two overlapping buckets read as disjoint. Three of the four were found by the child session after I had closed the step. The harness suite is now verified by me rather than reported: built from my own extract of the head with the pinned 11.0 SDK and run as a dll, giving **58 passed, 0 failed** with 58 individual pass lines. The harness subtree hashes `dfc395e68d34` identically at all six commits in the chain, so that result attaches to the head and not merely to the commit it was run at. A sixth correction removed an ungated restatement of the tie count from section 5.1's third consequence, which the session had audited and cleared on inverted grounds (rule 48); the surviving gated claim was re-perturbed with a did-it-apply guard to confirm the removal did not weaken it.
- **Corrections carried:** Two P0.4 defects found before any measurement and fixed in place at `P0.4-harness.md:90` and `:900` with the false text kept visible — a `calibrate` verb claimed twice and never written, and an inert heap axis that would have published three identically-configured unpinned runs under three different heap labels. Nothing P0.4 published was wrong, because all 528 `heapFactor` fields in its 474 preserved files are `null`; its first user would have been this step. Two further false claims were then found in this step's own document, both by me after opening the PR, and both are corrected in rule-22 form with the original visible: `:543` claimed all four throughput ratios drift below the floor when its own CSV puts `long-lived-cache` at 8.744%, and §5.1 claimed Workstation's minimum is 4 MiB against a Server range of 35–43 when the true spread is {4, 11, 19, 35, 99} against 35–139. The first passed 30 gate checks and I repeated it verbatim in the PR body; the second was written into a section added in response to my own request and survived three consecutive tips. Both are now gated, and the §5.1 fix is a merge of two commits written in parallel by the session and by me — recorded as rule 35, with the positional trap that nearly shipped the replacement ungated as rule 34.
- **Open:** `large-object-pressure` has `srv` 1.26× faster than `wks` at 88 MiB and 19× slower at 134 MiB, reproduced independently, with the `srv` side still only a hypothesis — first thing for P0.6. The blind-band assertion fired in its primary form but its bound sits 1.2× above the observed floor rather than the ~10× intended. Achieved per-arm heap factors diverge sharply from the nominal labels: at nominal 1.3x, `srv` gets 1.30–1.31x while `wks` gets 1.83–14.00x, and at 6.0x `wks` reaches 64.5x. F20 pause statistics remain single samples beside `invocations: 5`. The `sdk` cross-check is unresolvable as run because host build and session state are perfectly confounded. P0.5 also found a code prerequisite for the third arm that it did not fix and cannot fix from a measurement step; P0.6's Open states the mechanism and owns it, and this entry deliberately does not restate it, because a mechanism described in two places can be corrected in one.
- **Dependencies:** P0.4
- **References:** paper §4; `docs/design/coreclr/botr/garbage-collection.md`

### P0.6 — Resolve the baseline anomalies and fix the measurement methodology

- **Status:** planned
- **Summary:** Close the five measurement questions P0.5 opened before any LXR number is ever produced, so that the first LXR comparison is made on a methodology that has already been repaired rather than on one known to be broken.
- **Correctness:** The `large-object-pressure` inversion is explained rather than hypothesised — `srv` is 1.26x faster than `wks` at an 88 MiB limit and 19.1x slower at 134 MiB, reproduced across sessions, with the `wks` side demonstrated by its collection counts and the `srv` side still only a guess about a per-heap LOH budget. `lifecycle-semantics` needs a calibrated minimum that does not assume monotonicity, since viability there is genuinely non-monotone at 4 MiB viable, 56 OOM, 86+ viable.
- **Benchmarks:** No LXR column; this step measures only to repair measurement. The blind-band bound must be re-placed — it fired in its primary form but sits 1.2x above the observed floor rather than the ~10x intended, so it is currently a bound that would miss most of what it exists to catch. The latency acceptance statistic must be chosen explicitly: p99 at n=5 has a median within-cell CV of 11.67% with 34 of 60 cells above this host's 8.35% floor, so either n rises, or the statistic changes to one that concentrates better, or no latency claim below about 12% can be made on this machine.
- **Runtime changes:** None currently identified.
- **Open:** P0.4's merged `harness/scripts/verify-harness-control.sh` cannot run from a linked worktree, which is how every session in this project is checked out. It invokes `git -C "$REPO"` at `:26` and `:45`; under WSL's `git` a linked worktree's `.git` file holds an absolute Windows gitdir path that is resolved relative to the current directory, producing `…/kkokosa-sturdy-meme/C:/github/runtime-fork/.git/worktrees/kkokosa-sturdy-meme` and exit 128, while `git.exe` resolves that correctly but cannot accept a `/mnt/c` path to `-C`, resolving it against its own install root as `C:/Program Files/Git/mnt/c/…`. Both halves were reproduced directly rather than inferred. The script's own guard turns this into `not a git checkout` and exit 2, so the auditor sees a refusal rather than a portability failure. P0.5 resolved it by probing `git` then `git.exe` and invoking as `(cd "$REPO" && "$GIT" …)`; P0.4's copy is unfixed and is a merged artifact, so an auditor reaching for it from a worktree gets a refusal on a gate that is in fact sound. Separately, this step has a code prerequisite it cannot skip: `HeapBaselines.SharedMinimumMb` is a `Math.Max` over two named properties and `MinimumForArm` dispatches on `armId.StartsWith("srv")` with Workstation as the fallback, so the type cannot express a third arm and `MinimumForArm("lxr")` returns the *Workstation* minimum today — silently, validly, and in the direction that gives the new collector the smallest heap in the table with nothing failing. `HeapBaselines.cs` must gain a third arm before any LXR heap axis exists, and wiring an LXR arm anywhere else first is the ordering that makes this misfire. See §5.1 for the derivation. Two harness defects found while verifying its tests independently join this: `dotnet test` exits 0 without building or running anything, so only the produced dll reports a count (rule 45); and `Directory.Build.props` derives `ArtifactsPath` by walking four levels above the harness directory, so any copy built outside its repository position writes build output into an unrelated directory — mine went to the user's home. Both should be fixed alongside `verify-harness-control.sh`, since all three make the harness unverifiable by the methods this project actually uses. Two consequences P0.5 established and this entry did not carry, both from section 5.1 and both prerequisites rather than optional refinements. First, **the shared minimum must be recalibrated with LXR present as a third arm, and the negative case established by a run rather than inherited** — P0.5's "Workstation never binds, 30 of 30" is a fact about two arms and constrains nothing about three, so re-asserting it for a three-arm table would be a claim carried past the population it was measured on, which is the defect class that cost this step six corrections. Second, **P0.5's absolute MiB figures cannot serve as the comparison arm for LXR**: they were produced in a different session, on a different tree, under a shared minimum computed without LXR, so the first LXR comparison must carry its own same-session Workstation and Server arms and publish **the ratio as the quantity of record**, with absolutes as supporting detail only. Both are the reason section 5.1 exists; neither survives if this step measures LXR against numbers it inherited. Gate the anchor-uniqueness invariant, which P0.5 established by sweep but does not enforce: importing `CLAIMS` and `find_anchor` from the checker and matching every anchor against the document shows **11 of the 12 match exactly once**, and the only anchor that repeats is `heap-binding`, whose extra matches at `:468` and `:1064` are both prediction sentences that a tie demonstrates rather than falsifies. So no ungated restatement currently sits anywhere an anchor can reach, and that is worth asserting rather than re-establishing by hand — this step found duplicates one at a time across six review rounds, and the sweep finds all of them in one run. P0.6 adds a third arm, which is precisely the edit that introduces new sentences beside the gated ones, so the invariant should become a check with a named exemption list rather than a habit. Any such check must import the checker's own `CLAIMS` and `find_anchor` rather than retype the regexes, both because retyping is the transcription defect section 9 already records twice and because `find_anchor` compiles with `re.IGNORECASE` and skips blockquote lines, so a hand-rolled matcher answers a different question. The sweep script is in the session artifacts and is about thirty lines. **Carry rules 46, 49, 53 and 54 into the kickoff explicitly**, because the operating rules live in a section a spawned session never reads, and all four are tooling rules this step will exercise on its first day: 46 for how a perturbation battery is scored and the precondition on that score, 49 for which duplicated figures are worth chasing and the prediction sentences that are exempt, 53 for the battery's self-check being a checker in its own right, and 54 for the editing hazard that corrupts code spans. Listing the numbers rather than restating them is deliberate — a kickoff that paraphrases a rule creates the second copy the rules are about. Ten more were earned after this entry was written and belong in the same list: **59, 61, 63, 65, 66, 67, 68, 69, 70, 71**. Three change how the kickoff itself is written. **61** is why the A-or-B-or-delete fork is settled before a guard is written rather than inside it. **68** is why a bound stated by the author of a demonstration is not evidence: both bounds offered on the `$SELF_DIR` class this week arrived attached to a sound demonstration, in the same message, and both were wrong — `1 in 6` measured as 2, `those two` measured as three — because the search that finds a case also shapes the estimate of its class. And **71** inverts the instruction this entry gives — but only as amended by **72** and **73**, which are the operative pair: state the **property** to be demonstrated and then **specify at least two instances carrying distinct payloads**, letting the implementer add more freely. The earlier form of this, *leave the instance free*, is **retracted**: it was drawn from an accidental two-session divergence and the same property turned out to be reachable deterministically in one run, so freedom of instance is a lottery where a two-instance control is a control. Two consequences for this step's own tooling: a two-document perturbation ships as a **regression test**, because `verify-baselines.sh:386`'s `--selftest` asserts the checker's probes fire but nothing asserts they fire at the *right* document; and **72** is a design constraint on the third arm — re-derivation catches arithmetic, reading catches meaning, F11/F22/F24 are published numbers that re-derivation agrees with, and `ratioVsBaseline`'s resampling of the per-invocation vector rather than the published scalar is what made it immune to a defect in its own inputs. **F24 is this step's quietest blocker and has zero instrument coverage.** `pinning-heavy-io` is rate-limited by its own simulated I/O: both arms sit at 42,983–43,363 op/s, a spread under 1.0%, while gen0 counts differ by 2.5x, so its throughput ratio is **1.000 by construction and would stay 1.000 for a collector twice as fast** (`:549-554`). The document says that column "must not be counted as evidence that two collectors performed equally" and assigns the repair here at `:554-555` — *either its I/O rate rises until the collector binds, or it is declared latency-only* — and **nothing enforces the assignment**: `pinning-heavy-io` appears in **0** code files across the whole `P0.5-baselines` tree, and none of the 12 registered prose claims reaches it. The failure mode is specific and expensive: LXR lands at 1.000 against both baselines on a row where the collector was never on the critical path, and the matrix reports parity. Settle it before the third arm produces a number, not after. **Verified independently here from the committed tree, and the class is now bounded: F24 is exactly one scenario, and its fix already exists in the repo.** Re-derived from the per-invocation CSV (595 rows, `valid=true`, phase taken from `runId` as `rederive-statistics.py:134` does rather than from the `mode` column, which F22 makes unreliable): across every throughput-phase cell `pinning-heavy-io` moves **4.63%** end-to-end while its collection counts vary **10.7x**. Three other scenarios carry the same data signature and **all three are correct by design**, which is exactly why the signature alone cannot bound the class. `aspnet-request-load` (2.21%, 9.0x) is already declared `Primary = PrimaryMetric.Latency` in `ScenarioCatalog.cs`. `low-allocation-compute` and `pointer-chasing` record **zero collections and zero pause time in every valid row of both phases** — deliberately, as a paired barrier experiment: a zero-allocation workload isolates the field write barrier LXR pays on every reference store "regardless of how much garbage there is to reclaim", and `pointer-chasing` is its high-store partner so that "the pair separates per-store barrier cost from per-object cost". Both will discriminate LXR precisely because the barrier is mutator-side. The discriminating datum is therefore **collector work happened and throughput did not see it** — collections greater than zero together with a small spread — and not small spread alone; on that test exactly one scenario of ten is contradicted by its own data, `pinning-heavy-io`, declared `Primary = PrimaryMetric.Throughput`. **That bound is true of the throughput phase, which is the only phase the signature was computed over, and it is false of the matrix — do not implement against it.** Measured over the 68 valid wks/srv latency-phase rows in the three committed result files, **nine of ten scenarios publish a latency-phase `ratioVsBaseline` within 0.16% of 1.000**; the sole exception is `aspnet-request-load` at 12.80%, the only scenario declared `Primary = Latency`. **That bound is tighter than it reads:** the largest of the nine is `long-lived-cache` at **0.158%**, so it clears by two thousandths of a point, and any re-derivation moving one scenario by that much flips the claim. A rounded threshold stated without its margin looks robust and is not; the margin is now checked. The mechanism is at `Lxr.Harness.Runner/Program.cs:200` — latency-phase arrival rates are fixed from a prior throughput pass, so `operationsPerSecond` is pinned by construction for both arms, and `Aggregator.cs:112` reads exactly that field for all nine Throughput-primary scenarios. **So prefer making the ratio basis a function of (scenario, phase) over flipping one catalogue row.** The flip repairs one of nine and forces `mode=latency` in both phases via `MatrixRunner.cs:204` — the F22 shape, safe today only because all four consumers join on `runId` (`rederive-statistics.py:134`, `compare-arm-ratios.py:47`, `compare-sessions.py:56`, `derive-variance-table.py:48`) and unsafe the moment a tool added here joins on `mode`, which is a constraint to carry alongside the change rather than rediscover. **`Primary` is consumed at four sites and a phase-dependent basis must change two of them together:** `Aggregator.cs:112` picks the field, `Aggregator.cs:159` names the statistic (`"mean over invocations of operationsPerSecond"`), `MatrixRunner.cs:204` forces the mode, and `Reaggregator.cs:213` reconstructs a cell from a stored `RunResult` before delegating to the same `Aggregator` (`:96`, `:97`, `:127`). Two consequences, and they differ. **`:159` must change with `:112`:** it names the statistic from `cell.Primary` independently of the field choice, so a phase-dependent `:112` alone publishes a `ratioStatistic` **describing a field the bootstrap no longer resampled** — the exact label assertion (a) keys on. **`:213` needs no change but blocks the design:** because it delegates, a fix at `:112` propagates to re-aggregation for free, but a basis keyed on phase needs a phase *on the cell*, and neither `MatrixCell` nor `RunResult` carries one. **Name the artifact, because the two disagree:** the per-invocation **CSV** has `runId` *and* `mode` as first-class columns (`Publisher.cs:37`), which is why `rederive-statistics.py:134` can call `phase_of(row["runId"])` on a real field; the **JSON** result rows have neither, and `RunId` reaches them only as free text inside `notes` via `Aggregator.cs:368` (`$" runId={runId}"`). `Reaggregator` reads the JSON, so the re-aggregation path — and only it — can recover the phase solely by substring-parsing `notes`. An earlier draft of this item said the run id reaches "the artifact" as free text full stop, which is false of the CSV; the gap is one artifact wide, not two. **Giving `RunResult` a first-class phase or run-id field is a precondition of the phase-dependent basis, not an optional tidy-up**, and it retires the `notes`-parsing that F22 forced on every downstream tool. The phase-dependent basis repairs all nine, touches no catalogue row, leaves `Primary` and `:204` alone, and needs **no re-run** — a claim that has to be made at the per-invocation level, because `Aggregator.TryReadPrimary` reads the field from an invocation's report, not from either published file. Both artifacts agree. **Amendment, measured: the phase-dependent basis is necessary and not sufficient, and the remedy is not a third enum member.** `PrimaryMetric` has exactly two members (`Lxr.Harness.Core/Scenario.cs:13-14`) and `Aggregator.cs:112` is a binary ternary, so *any* selector over it — phase-keyed or not — chooses between `operationsPerSecond` and `latencyP99Ms`, and the largest collector-discriminating signal in the matrix is in neither. `pinning-heavy-io` publishes `ratioVsBaseline` of 0.999855 / 1.000395 / 0.991251 in the throughput phase while its `pauseP99Ms` differs srv/wks by **6.98x / 4.07x / 1.33x**, with `ratioStatistic`, `ratioCiLow` and `ratioCiHigh` all null on those rows. Widening the enum trades one gap for another: it is still one basis per row, it would forfeit the throughput ratio that correctly reports the limiter binding, and declaring pause the *primary* metric of a scenario whose headline genuinely is throughput is false. **Decouple the statistics from the headline instead** — compute ratio, bootstrap CI and significance for every comparable numeric column present on both arms, and keep `Primary` for what it is named for, selecting which computed ratio leads the report. That adds no enum member, touches no catalogue row, and *subsumes* the phase-dependent basis: with every column carried there is nothing to select per phase, and the latency phase's 1.000 on `operationsPerSecond` becomes self-explaining because the pause and latency ratios sit beside it. **It needs no re-run, demonstrated rather than argued:** the per-invocation CSV already carries `pauseP99Ms` at n=5 per arm per heap, and an **exact** one-sided permutation test — all 252 arrangements enumerated, no resampling and no seed — gives srv/wks point ratios of **4.05x**, **4.70x** and **2.61x** at p = **1/252**, **1/252** and **2/252**. At 1.3x and 2.0x the observed split is the most extreme of all 252, which is the floor a five-against-five design can report. **Do not state complete separation:** it holds at 1.3x and 2.0x but **fails at 6.0x**, where srv's minimum of 0.8471 falls below wks's maximum of 0.9203. A 4,000-resample bootstrap was the first thing computed here and is the wrong instrument for a board claim — a resampled interval cannot be re-derived by a checker that does not share its seed, which is rule 82 applied to a number this very amendment introduced. **Read the CSV, not the aggregate row:** `Aggregator.cs:301-305` states that the published `pauseP99Ms` is copied out of a single invocation and that *"a p99 of one invocation's pauses and the mean of five invocations' p99s are different estimators"* — which is why the aggregate reads 6.98x where five invocations read 4.05x. Direction and significance agree, magnitude does not, and the single-invocation figure is the fragile one. Same conclusion as the pause-denominator item, for the same reason: the instrument has to read the per-invocation artifact. **Published JSON rows:** `latencyP99Ms` present in **68 of 68** latency-phase, **0 of 69** throughput-phase excluding `aspnet-request-load`. **Per-invocation CSV, which is the one that settles it:** **340 of 340** latency-phase rows, **30 of 359** throughput-phase — the 30 being `aspnet-request-load` entire, every other scenario at 0. The JSON count alone would not have established this, since it is a projection of the reports rather than the reports themselves. **Citation root for this entry:** every bare source file name below lives under `docs/design/lxr-port/harness/src/`, in `Lxr.Harness.Runner` unless the name says otherwise — `RunResult.cs` and `ScenarioCatalog.cs` are `Lxr.Harness.Core`, and test citations name `Lxr.Harness.Tests` explicitly. This is not pedantry: at the reviewed tree `Program.cs` matches **four** files under the harness alone and 217 across the repository, and two of the four have a line 200 — the Runner's is the arrival-rate mechanism this entry depends on, the Tests' is unrelated but entirely readable code, so following the wrong one yields plausible content rather than an error. `Aggregator.cs` also collides with `System.Diagnostics.DiagnosticSource`'s own aggregator, `Publisher.cs` with `System.Security.Policy`'s, and `RunResult.cs` with `Wasm.Build.Tests`'. **Assertions this step owes:** (a) for every scenario and every phase, the field named by `ratioStatistic` is one whose value is not pinned by the harness's own offered-load control in that phase, and (b) a cell whose ratio CI spans 1.000 while an adjacent published collector-sensitive column differs by more than the 8.35% resolution floor is reported, since `pinning-heavy-io`'s latency rows differ by 23.5% in P99 — 2.8x that floor — and its throughput rows differ by 6.98x in `pauseP99Ms`. **One decision this forces rather than settles:** `aspnet-request-load` is the single scenario whose throughput-phase ratio (1.1305) is computed on latency today, so a phase-dependent basis changes that number and P0.6 must take it deliberately rather than mechanically. **Correction, measured: that number is not informative, and calling it the informative exception was wrong.** At the latency phase's published rate of 7,996 op/s, `pauseP99Ms` is **0.50% to 2.76%** of `latencyP99Ms` (means over invocations from the per-invocation CSV, not the published JSON row, whose nine copied columns are single draws) — so 97.2-99.5% of the p99 is queueing delay, not collector behaviour. At the throughput phase's 1,004.7 op/s the relation inverts and is no better: the pause p99 runs **1.6x to 6.0x** the latency p99, so a single pause exceeds the percentile and is too rare to reach it. The only latency-primary scenario in the matrix is blind to the collector at both load points, for opposite reasons. **The exact test settles which column carries the signal, and it is not the one with the statistics.** Over the 30 latency-phase invocation rows, srv against wks, all 252 arrangements enumerated: `latencyP99Ms` — the column that has a ratio, a CI and a significance test today — gives p = **0.548 / 0.675 / 0.230**, significant at no heap factor; `pauseP99Ms`, which has none of the three, gives p = **0.048 / 0.008 / 0.008**, significant at all of them. **The machinery computes statistics for the column with no detectable signal and withholds them from the column that has one**, on the very scenario this entry proposed to preserve as its informative exception. That is assertion (b)'s first customer and it is a stronger case than `pinning-heavy-io`, because here the failure is inside the scenario's own declared primary phase. **One caveat that has to travel with it:** at 1.3x the two estimators disagree in *sign* — the aggregate row's single-invocation p99 gives srv/wks 1.199 while the mean of five gives 0.692 — and 1.3x is also the marginal p (12/252). The 2.0x and 6.0x results carry the finding; 1.3x does not. **So option two of `:554-555` is a one-field change with a working in-repo precedent.** The reusable gate for P0.6: assert that every scenario's declared primary metric actually varies with the collector, carving out the barrier scenarios **by declaration rather than by inference**. The rules are readable in the coordinator's board under `## Operating rules`; a spawned session should be given the path and told to **read** them, never to copy them into its own tree — a copy is precisely the two-subject hazard rule 63 is about. GATE DEFECT, found by the implementer during P0.5 close-out and confirmed here by reading: `verify-baselines.sh --results DIR` selects *which checkpoints are audited* but not *what they are audited against*. `:149` finds checkpoint JSON under `$RESULTS_DIR`, while `:249` builds the CSV path from `$BASE`, `:270` reads calibration from `$BASE` and `:288` passes `--results "$BASE"` to the prose checker, so a checkpoint from the supplied directory is re-derived against raw data from the committed one, and the `mode: full audit (--results …)` banner at `:161` names the only root that is not supplying the data. Every published `PASS (34 checks)` is unaffected because no run passed the flag and `:81-83` then sets `RESULTS_DIR="$BASE"`, making the two roots the same directory; the gate's immunity to a wrong `--results` and this defect are the same fact. **The consequence is that the flag is unusable, not that it is dangerous, and P0.6 must not repair it on the assumption that a split-root audit can pass silently — it cannot.** Every manifestation fails: differing checkpoint filenames fail at `verify-baselines.sh:250`, and once inside `rederive-statistics.py` a join that produces nothing fails at `:226` on `checked_mean == 0`, a partial join fails at `:229` on `unmatched_cells`, and a full join with different numbers fails at `:232` on `mismatches` — three unconditional `return 1`s standing before the single `PASS` at `:236`. A silent pass would require a fresh run to reproduce the committed numbers within `close()`'s 1e-4 tolerance across every mean field. Note that `:226` is count-equality (rule 46) already implemented in the shipped tooling by its own author, which is independent support for the pattern. The minimal fix is `$BASE` → `$RESULTS_DIR` at `:249` and `:288`, but it changes gate semantics across `infer`/`explicit`/`none` and requires re-running both the 34-check gate and the 11-of-11 control; do it as P0.6's first tooling change, before the flag is used to audit a fresh session, and add a case asserting that a split root fails loudly rather than merely that the gate still passes. A fifth manifestation, found by the implementer and confirmed in source, is not covered by that case and needs its own: a supplied directory holding a correctly-named **subset** of the checkpoints produces `RESULT: PASS (30 checks)` at exit 0 against 34 for the full set — no mismatch anywhere, every check honest, the banner still reading `mode: full audit`, and two thirds of the evidence silently unexamined. It fails loudly nowhere, so a test asserting loud failure cannot reach it; assert the **expected check count** as well, and derive that expectation rather than writing a literal `34`, because `:10-11` forbids literal expected counts at assertion sites and that principle is exactly why the input set is unguarded today. The shape to copy is `:414` (`named_count` vs `present_count`, difference printed): compare checkpoints found against checkpoints the document names. See rule 59 — this is also the correction to my own severity ruling, which held only across the four mismatch-shaped cases I could construct. **Settle the flag's semantics before writing either guard (rule 61), because the two candidate meanings want opposite checks.** The option surface is smaller than it looks: the script takes only `--results` and `--no-results` plus one positional `ROOT_ARG`, and `:77-79` derive `DOC`, `BASE` and `HARNESS` from that root together — so *auditing another tree coherently is already supported by the positional argument*, and `--results` is the only construct that can split the roots at all — **false, corrected twice: `$SELF_DIR` splits them too — at `:193`, `:407` *and* `:391`, three of its fourteen uses, on the positional path — and that split produces silent mis-verification (rule 63). The three do not share a fix: `:193` and `:407` read `$BASE/scripts/`, while `:391` is different in kind and needs `--lxr-root "$ROOT/docs/design/lxr-port"`, because `check-doc-code.py:209-211` defaults that root from `__file__`, so the F1 sweep takes **both** sides from the apparatus and never opens the audited tree at all — the check written because P0.4's gate compared the document to itself compares the document to itself. Measured, not argued: a perturbed extract claiming `--bogus-flag` passes `PASS (34 checks)` from the other checkout and fails `FAIL (33 passed, 1 failed)` from its own, the discriminator being `11` against `12` in a count the gate already prints. All three land independently of how the fork is settled**.. Reading **A**, audit the committed document against another results tree, is a genuine reproducibility check: move every data root to `$RESULTS_DIR`, keep the document committed, and a document-derived checkpoint expectation is then correct and must fail on mismatch. Reading **B**, audit another tree on its own terms, is the positional and needs no flag — which means the honest options include **deleting `--results`** rather than repairing it. P0.6's own first use is B, so it wants the positional; but `:77-79` hardcode `P0.5-baselines.md` and `P0.5-baselines/`, so a P0.6 tree needs its own derivation regardless. Decide A-or-B-or-delete **first**; the guard follows from the answer and contradicts it otherwise. **`gen0Collections` is a mislabelled published field and P0.6 inherits it (verified here, read-only).** `GcTelemetry.cs:161` fills it from `GC.CollectionCount(0)`, which counts gen0 *or higher*, so it is the all-generation total; `:233-237` documents that precisely — *"not the gen0-only figure its name suggests"* — but that annotation lives in C# XML doc, and the name ships unannotated into all 144 published rows and into `P0.5-baselines.md`, which says "gen0" at `:541`, `:543`, `:544` and `:550`. The document never states the semantics anywhere. **The prose reasoning is correct and that is the evidence the author knew:** `:542`'s *"2,042 gen0 against 2,021 gen2 — nearly every collection is a full one, so it is thrashing"* is only coherent if gen2 is a subset of the 2,042; read as gen0-only it would be ~50% and the inference would not follow. Corroborated across the data: `gen2 > gen0` occurs in **0 of 144 rows**, and `gen2 == gen0` nonzero in 3. So the hazard is purely the label, and it is P0.6's because the LXR column will be read from these same keys by someone who did not write the harness. Fix at the boundary: rename to `totalCollections`, or publish the semantics beside the data. **Do not "fix" `MeanPauseMs`** — `GcTelemetry.cs:276-281` prefers `ObservedPauseCount` and falls back to `Gen0Collections`, and for a mean over `GC.GetTotalPauseDuration()` the all-generation count is the *correct* denominator, as `:271-274` argues; switching it to a gen0-only count would introduce the defect it looks like it is removing — and **nothing would stop that edit**, because the fallback branch has no fixture anywhere in the suite (rule 80). 8 of 144 rows are *labelled* `total-pause-duration-only`, but whether any of them took that branch is **undecidable from published data**, so do not repeat the "live in 8 rows" phrasing this board carried until now. **Two deliverables follow from that and neither is optional.** First, **add the missing fixture**: a `GcSummary` with `ObservedPauseCount = 0` and `Gen0Collections = 20, Gen1Collections = 19, Gen2Collections = 2`, asserting the result is `TotalPauseMs / 20`, is *not* `TotalPauseMs / (20 - 19)` — the gen0-only "correction" this paragraph warns against — and is *not* `TotalPauseMs / 41`, the triple-count that `Lxr.Harness.Tests/Program.cs:544` already guards on the other branch. Today the whole 1188-line suite builds exactly two `GcSummary` values, `(20, 20)` and `(0, 0)`, so the fallback has none. Second, **publish the denominator**: emit `observedPauseCount` and `totalPauseMs` **into `Publisher.InvocationsHeader` (`Publisher.cs:36-41`), the per-invocation CSV — not only into the JSON result rows.** Naming the artifact is the whole of this item: `pauseAverageMs` exists in *both* files, and `rederive-statistics.py` takes both (`--checkpoint` and `--csv` at `:112-113`), joining published JSON records to CSV rows by cell key. The quotient is formed **per invocation**, so the CSV is where both inputs must sit for the gate to evaluate the switch at all; a JSON-only change is weaker rather than useless, since it would permit a new intra-row arithmetic check but leave every per-invocation value unchecked and the branch still unidentifiable. **The contract this restores is already written at `Publisher.cs:33-34`** — *"Its columns are the exact vector the bootstrap resamples, so a reader can recompute the published mean and re-run the interval without the harness."* `pauseAverageMs` is in that header at `:39` and is the column that promise fails for, because it is a quotient whose numerator and denominator are both absent from the file that promises recomputability. **And `pauseSource` then becomes redundant**: with both candidate denominators present a checker evaluates `pauseAverageMs == totalPauseMs / (observedPauseCount > 0 ? observedPauseCount : gen0Collections)` and derives the label instead of copying it, which retires a `COPY_FIELD` rather than adding two — strictly better than publishing the inputs alongside a label nobody can check. Note that the CSV cannot identify fallback rows today at all: it carries no `pauseSource` column, so the branch question is undecidable **in principle** from the committed artifacts, on the JSON by absent inputs and on the CSV by an absent label. **Section 6.4's variance table has no `pauseP99Ms` row, and the omission is structural rather than an oversight**: `derive-variance-table.py:28` designates one metric per phase, so pause is unreachable by construction — rule 86's second instance. Derived under the document's own convention and control-reproduced against both published rows per rule 87: `pauseP99Ms` is **96 cells, median within-cell CV 8.37%, max 180.27%, 48 of 96 — exactly half — above the 8.35% floor**, so the median pause cell sits **0.02 points above** this session's demonstrated resolution. That column is also published as a single draw rather than a mean: `Aggregator.cs:262` reads `ValidOutcomes[^1]`, and across all three sessions the published `pauseP99Ms` equals the last valid invocation in **108 of 108** cells and the column mean in **0 of 108**. The same holds for the other eight copied columns (`pauseAverageMs`, `pauseMaxMs`, `gen0Collections`, `gen1Collections`, `gen2Collections`, `inducedCollections`, `workingSetMb`, `committedMb`); the carve-out at `Aggregator.cs:301-305` is correct and reasoned — a p99 of one invocation's pauses and a mean of p99s are different things — but F21's rule at `docs/design/lxr-port/P0.5-baselines.md:136-137` states the mean rule as a universal and names no exemption, so the deliverable asserts of nine columns something its own code deliberately does not do. **Consequence for the amendment above: decoupling statistics from `Primary` is necessary and not sufficient.** It yields a pause ratio for every cell, but at n=5 the median pause cell is already at the resolution limit, so those ratios will mostly carry intervals spanning 1.000 — precisely the state `aspnet-request-load` is in today, where pause separates the collectors by exact test at p = 0.048 / 0.008 / 0.008 while its published ratio carries no interval at all. P0.6 must therefore raise the invocation count for pause specifically and state the n at which the median pause cell clears the floor, before any pause ratio is treated as evidence; a wider column set over an unchanged n moves the pause metric from unreported to reported-and-inconclusive, which is a different failure and not a smaller one. **Post-merge item, and the child session's reversal on it is unsupported.** Commit `abf6d75b3db` corrects the second half of that paragraph and states in its message that "the Kestrel attribution is unchanged and correct (`AspNetRequestLoadScenario.cs:87`, verified by reading the file)". Reading that file establishes that Kestrel is **present**; the sentence claims it is **why** `aspnet-request-load` sits "two to three orders of magnitude above everything else", which is a causal and comparative claim that no single file can settle. Measured from the per-invocation CSV, it is false twice over. First, holding the socket path constant: the same scenario through the same Kestrel yields a mean `latencyP99Ms` of **283.9-583.7 ms** in the latency phase at 7,996 op/s and **1.09-1.72 ms** in the throughput phase at 1,004.7 op/s — a factor of **165x to 534x** with Kestrel unchanged, so the socket path cannot be what produces the height, and aspnet's own low-rate p99 lands *inside* the 0.145-52.7 ms range of the nine non-Kestrel scenarios, below four of them and above five. Second, as a superlative: against the nearest member of the set it exceeds, `lifecycle-semantics` at 52.66 ms, the gap is **8.6x** — less than one order of magnitude. "Two to three orders of magnitude" holds against 3 of the 9 other scenarios and fails against 6 — three nearer than 100x (8.6x, 19.5x, 49.6x) and three beyond 1000x, `pointer-chasing` reaching ~3105x. The first draft of this sentence read "6 … and fails against 3", because both it and the claim checker enforcing it tested only `ratio >= 100` and never the upper edge (rule 101). This is the same defect the adjacent blockquote was written to correct, one sentence earlier in the same sentence's own paragraph: a superlative is a claim about a set and was ranked against nobody. The blockquote ranked the 47.4% drop 9th of 20 and left "above everything else" unranked. **The cross-artifact citation gap filed here is retired as an instrument and kept as a discipline, on measurement rather than judgement.** The gap is real — `board-claims-check.ps1` verifies the board against P0.5's artifacts and the deliverable's gate verifies that document against its data, so nothing checks the deliverable's `rule N` citations into the board, which its author cannot read from that worktree. Two exact predicates were proposed and both were measured against the healthy artifact before any was written, per rule 88. Requiring a citation to quote its rule flags **6 of 8** citations that independent verification confirms are correct (4 of 4 distinct rules: 22, 25, 26, 28 across `:821 :926 :933 :938 :967 :1013 :1051`). Requiring rule text to be append-only flags **25** legitimate in-place corrections against 22 extensions. What remains decidable is one check — **every cited rule number must exist on the board** — currently 0 dangling in both documents, cheap enough to fold into `board-claims-check.ps1` rather than justify a new instrument. **The identity hazard has now fired three times, and existence is the wrong guard for it:** rules **50, 51 and 98 were replaced** across `be5bc610e00` and `0f13853a89b`, old text surviving nowhere under a stable number, with **9** live citations to them from this board. *That figure’s own first draft said **8**, and was falsified by this passage’s closing sentence, which added the ninth while explaining the defect — the trap fired twice inside one correction.* *This passage first read “One real identity change exists and bounds the residual risk: rules **50 and 51 were replaced** at `be5bc610e00` … with **0** live citations to either from either document”, and both figures were falsified by the act of writing rules 103–105, whose text cites those very numbers* — the blast radius went from zero to eight inside the edit that measured it as zero, which is rule 90’s shape and rule 93’s trap in one. A dangling-citation check cannot see any of this, because all three numbers still resolve; the guard that can is `board-check.ps1`’s disclosure check, which derives the replaced set from history and requires a replaced headline to say so — and which caught rule 51 undisclosed on its first run, eleven days after the fact. The existence check stays worth folding in, but as the cheaper half of a pair, not as the answer; alongside it the practice that actually caught a miscitation this week — resolve the rule out of the shared object store and quote it when the claim leans on it. **Amendment, measured against the committed CSVs: "every comparable numeric column" is too broad, and it re-admits the two things this step already legislated against.** Applied literally to the 20 metric columns of the per-invocation CSV over the 59 comparison cells — a cell being (scenario, heapFactor, phase) carrying at least three valid invocations on both arms — the directive yields **870 (cell, column) pairs, of which 92 are undefined and 693 are well-formed**, the balance being pairs whose bootstrap interval has width exactly zero. Two columns dominate the damage and each is a rule this step already wrote. **`inducedCollections` is undefined in 59 of 59 cells** because the column is the string `0` in **all 699 valid invocations across all three sessions** — it is an assertion the harness makes, not a measurement it takes, and no schema-level predicate can tell those apart. **`arrivalRatePerSecond` contributes 33 zero-width intervals**, and that is precisely the field the harness's own offered-load control pins, which is F24's mechanism and which assertion (a) above already forbids as a ratio basis; stated as "every column", the directive re-admits it 33 times. A zero-width interval is also P0.4's n=1 defect arriving by a second road: `MinimumInvocationsForBootstrap = 3` does not fire because n is 5 — the *variance* is zero, not the sample — so every resample draws the same value and any ratio other than 1.000 reads as significant. **The repair keeps the insight and narrows the claim: compute universally, claim selectively.** Computing a ratio should never have been conditioned on a headline choice, and that half needs no re-run. What must stay conditioned is the *claim*: two mechanical domain predicates — the denominator is defined, and the vector is not constant across both arms — plus a declared per-scenario subset that significance language may range over, everything else published as a computed value carrying no verdict. So `Primary` is demoted from *which column receives a ratio* to *which ratios may be called significant*, which is the pre-registration it was always fumbling toward, and one notch less far than the demotion this entry first proposed. The family size is the reason the second half is not optional: at 693 well-formed pairs, roughly 35 exclude 1.000 by chance at a nominal 95% even if every null is true, so "significant" stops meaning what it means in a pre-registered comparison the moment the family is that wide. **One population caveat, and it runs in the direction that strengthens the case rather than weakening it.** Under the design key of rule 91 the union is not the same instrument as s2 at all: it is **1,018 pairs, 113 undefined and 812 well-formed** against s2’s **870, 92 and 693**, because the union adds cells rather than widening them. The earlier reading that the pair total and undefined count were *invariant* to which sessions are included — 870 and 92 either way — was true only of the pooled key, which merged all three sessions into single arms of 10 and 13 and so could not add a cell. Degeneracy moves either way. Derived over the three sessions unioned, the zero-width count is 93 and `arrivalRatePerSecond` contributes 37; derived over s2 alone, which is the population `derive-variance-table.py:106` uses for every published variance figure, they are **85 and 33**. Unioning subset re-runs into a cell can break a constancy that the published population actually has, so the union *understates* the defect it is being used to demonstrate, and the figures of record here are the s2 ones. **The significance half is now settled structurally rather than by argument, and it comes with a number this step must design to: twelve.** Per rule 89, the exact test at five against five has a floor of 1/252 = 0.003968, so a Bonferroni-corrected interpreted family can hold at most **12 comparisons** before no cell can be significant even when its arms separate completely; 693 well-formed pairs would need 7.2e-5, eighteen times below the floor. **So "compute universally, claim selectively" is not a stylistic preference over correcting for multiplicity — at this sample size it is the only route to a claim that survives its own family.** The design constraint is therefore concrete and checkable before any LXR number exists: **either the interpreted family stays at or below 12 per report, or n per arm rises**, with `C(12,6) = 924` giving a floor of 1.082e-3 and a budget of 46 if six invocations per arm is affordable. Note the conclusion is invariant to the population dispute recorded above — the two candidate populations sit at **57.75x** and **67.667x** the budget of twelve, so the s2-versus-union correction changes the figure and not the finding. **A second result retires the question that generated this thread.** For a fixed pool the arm sums satisfy `ratio = A/(S-A)` and `diff = (2A-S)/n`, both strictly increasing in `A`, so the two statistics induce the *same ordering* over all 252 arrangements and yield identical p-values — verified by enumeration on the committed data, where `p(ratio)` and `p(diff)` agree at 1/252, 1/252 and 2/252 across the three heap factors. **Under the exact test, ratio-of-means and difference-of-means are the same test, so "which field is the ratio basis" — the question F24 opened and rule 86 answered — is empty for the instrument that replaced it.** It remains live only for the reported effect size. **One boundary on that proof, which strengthens the domain predicate this entry already carries.** The equivalence needs the denominator defined on *every* arrangement, not merely the observed one: a pool containing at least n zeros admits an arrangement placing all of them on the baseline arm, and enumeration confirms exactly 1 of 252 arrangements goes undefined in that case. So the predicate must read **"the pooled vector contains fewer zeros than the smaller arm"**, not "the observed denominator is non-zero" — the two coincide on the committed data at **0 of 693** well-formed pairs, which makes this free to adopt now and expensive to discover after an LXR arm adds columns that are zero on most invocations.
- **Dependencies:** P0.5
- **References:** `docs/design/lxr-port/P0.5-baselines.md` sections 5, 6.4 and 9; paper Table 1

## P1 — Runtime enablement as generic GC-contract capabilities

- **Status:** planned
- **Summary:** Extend the GC contract with capabilities any collector could use — a parameterized barrier family, exact allocation notification, object reference enumeration, and negotiated header bits — then prove they cost nothing when unused.

### P1.1 — GC capability negotiation and barrier-family contract

- **Status:** planned
- **Summary:** Let a GC declare which barrier family and optional runtime services it requires, and have the EE select and patch the corresponding implementations at initialization.
- **Correctness:** The built-in GC declares the existing card-table family and its generated code does not move; existing standalone GCs build and run unchanged against the extended contract.
- **Benchmarks:** Codegen diff proving byte-identical output for the built-in collectors, plus the P0.4 matrix as a regression check.
- **Runtime changes:** Adds a capability-declaration surface and a structurally described side-metadata slot-log barrier family parameterized by metadata base, granularity shift, polarity, and slow-path helper, covering LXR's unlog bit, SATB/deletion barriers, and generational field-log barriers; additive and gated by a `GC_INTERFACE_MINOR_VERSION` bump, never reshaping existing members.
- **Dependencies:** P0.3
- **References:** `src/coreclr/gc/gcinterface.h:9,14,49-56`; `mmtk-core/src/plan/barriers.rs`

### P1.2 — Implement the slot-log barrier family on x64

- **Status:** planned
- **Summary:** Implement the family on x64 in MASM and GAS, reproducing the reference fast path's shape while reading its parameters from the descriptor rather than hardcoding them.
- **Correctness:** Barrier stress shows no lost or duplicated log entries under contention; volatile and SIMD state is preserved across the fast and slow paths; an unused family leaves the built-in barrier untouched.
- **Benchmarks:** Barrier-only microbenchmark and allocation rate against the stock barrier, plus end-to-end low-allocation compute and allocation churn scenarios.
- **Runtime changes:** Adds x64 Windows and Linux assembly for the family — side byte load at base plus slot shifted right, zero-byte early-out, bit extract, polarity compare, tail-call to the configured slow helper — selected through the existing write-barrier patching path so an unused family costs nothing.
- **Dependencies:** P1.1
- **References:** `mmtk-openjdk/openjdk/barriers/mmtkFieldBarrier.cpp:78-127`; `src/coreclr/vm/writebarriermanager.cpp`; `src/coreclr/vm/amd64/JitHelpers_FastWriteBarriers.asm`

### P1.3 — Cover all reference-store paths for the family

- **Status:** planned
- **Summary:** Emit the family barrier on every managed reference-store path and generalize the reference's bulk fast path into a family-level operation.
- **Correctness:** A store-path audit shows no reference write reaching the heap without its barrier, including struct copies containing references and interlocked reference operations.
- **Benchmarks:** Array copy, span fill, and struct-copy microbenchmarks against the stock barrier, plus the multi-thread throughput scenario.
- **Runtime changes:** Extends RyuJIT emission and the helper set across `CORINFO_HELP_ASSIGN_REF`, checked and unchecked variants, `CORINFO_HELP_BULK_WRITEBARRIER`, `Array.Copy`, `Span`/`Unsafe`, and interlocked reference operations; adds a family-level bulk operation that scans the destination metadata a machine word at a time and skips when already fully set, so any slot-log consumer inherits it.
- **Dependencies:** P1.2
- **References:** `mmtk-core/src/plan/lxr/barrier.rs:209-232`; `src/coreclr/jit/`; `src/coreclr/inc/corinfo.h`

### P1.4 — Add an exact allocation-complete notification service

- **Status:** planned
- **Summary:** Provide an opt-in per-object callback fired after header initialization and before the object escapes to managed code, because neither existing hook establishes exact birth metadata.
- **Correctness:** Every allocation path — object, array, string, boxed value, fast and slow — delivers exactly one notification, and no object becomes reachable from managed code before its notification completes.
- **Benchmarks:** Allocation microbenchmarks at several object sizes and the allocation churn scenario, comparing registered and unregistered configurations.
- **Runtime changes:** Adds an opt-in allocation-complete callback carrying object, size, and flags across the x64 fast helpers and slow paths, generalizing beyond LXR to any RC collector, exact allocation sampling, and heap-birth metadata; the inline allocator keeps its bump-pointer shape and the unregistered path targets byte-identical codegen, with any residual cost measured in P1.7 rather than assumed negligible.
- **Dependencies:** P1.1
- **References:** `src/coreclr/gc/gcinterface.h`; `src/coreclr/vm/gchelpers.cpp`; `src/coreclr/vm/amd64/JitHelpers_Fast.asm`

### P1.5 — Add GC-side object reference enumeration

- **Status:** planned
- **Summary:** Let GC code enumerate an object's reference slots without an EE round trip per object, closing a plain gap in the standalone-GC contract that every such collector pays for.
- **Correctness:** Enumeration matches the EE's own view for every layout — arrays of references, structs with references, generics, and collectible types — verified against the existing scanning path.
- **Benchmarks:** Scan-rate microbenchmark over representative object graphs, plus pointer chasing and long-lived cache scenarios where scanning dominates.
- **Runtime changes:** Exposes an inlinable, bulk/streaming reference-slot enumeration API to GC code; confirmed absent from this fork today, and required because RC increment processing and tracing both sit directly on this path.
- **Dependencies:** P1.1
- **References:** `src/coreclr/vm/object.h`; `src/coreclr/vm/methodtable.h`; `src/coreclr/gc/gcinterface.h`

### P1.6 — Define negotiated GC-reserved object header bits

- **Status:** planned
- **Summary:** Define how a GC reserves object-header bits and how those bits coexist with the sync block index, thin lock, and hash code, then prove the placement before any copying code exists.
- **Correctness:** Reserved bits survive every runtime operation that touches the header — thin lock inflation, hash code assignment, sync block creation — under targeted stress, with no observable interference in either direction.
- **Benchmarks:** Lock and hash-code microbenchmarks proving no regression, since these paths are contended and header-sensitive.
- **Runtime changes:** Adds a header-bit negotiation mechanism serving any copying or concurrent collector; MMTk keeps two forwarding bits in header available bits and spins on a `BEING_FORWARDED` state, whereas .NET's `ObjHeader` is already contended, making this the highest-risk unknown in the port and the gate on all of P6.
- **Dependencies:** P1.1
- **References:** `mmtk-core/src/util/object_forwarding.rs:8-11`; `src/coreclr/vm/syncblk.h`; `src/coreclr/vm/object.h:106`

### P1.7 — Gate runtime changes on measured zero regression

- **Status:** planned
- **Summary:** Prove that the contract changes cost nothing measurable on the built-in collectors before any LXR code depends on the new surface.
- **Correctness:** Codegen diffs are byte-identical wherever that was claimed in P1.1-P1.6; any divergence is investigated as a design defect rather than annotated as acceptable.
- **Benchmarks:** Workstation and Server GC across the full P0.4 matrix, plus barrier and allocation microbenchmarks, compared against the P0.5 baselines on identical machine state.
- **Runtime changes:** No new surface; this is the enforcement point for the R2 no-cost-when-unused and R5 evidence-per-change rules, and a regression here is fixed in P1.1-P1.6 rather than accepted.
- **Dependencies:** P0.5, P1.2, P1.3, P1.4, P1.5, P1.6
- **References:** paper §5.1 barrier overhead; P0.5 baselines

## P2 — Side metadata and Immix substrate

- **Status:** planned
- **Summary:** Build the address-to-metadata layer and the Immix block/line heap the whole collector sits on, at the reference's exact geometry and granularity.

### P2.1 — Build the side metadata framework

- **Status:** planned
- **Summary:** Provide address-to-metadata mapping with atomic bit and byte operations plus bulk word reads for every metadata spec the collector needs.
- **Correctness:** Concurrent updates to neighboring bits within one byte never lose an update, verified by a dedicated contention stress test at every declared granularity.
- **Benchmarks:** Metadata access microbenchmarks for single-bit, single-byte, and bulk word paths, since RC and sweeping are dominated by them.
- **Dependencies:** P1.1
- **References:** `mmtk-core/src/util/metadata/side_metadata/`; `mmtk-core/src/util/rc.rs:13-26`

### P2.2 — Implement block and line geometry and the state machine

- **Status:** planned
- **Summary:** Implement 32 KiB blocks, 256 B lines, 128 lines per block, the block state machine, and the phase epoch that distinguishes mutator from GC phases.
- **Correctness:** Block state transitions match the reference's allowed set; the phase epoch is bumped at both pause start and release, and drives nursery, reusing, and GC-reusing predicates identically.
- **Benchmarks:** Block acquisition and release rates under allocation churn.
- **Dependencies:** P2.1
- **References:** `mmtk-core/src/policy/immix/block.rs:76-98,300-374`; `mmtk-core/src/policy/immix/line.rs:20-29`

### P2.3 — Implement the sticky 2-bit RC table and straddle lines

- **Status:** planned
- **Summary:** Implement two reference-count bits per 8-byte granule, saturating at 3 and never decremented once sticky, plus straddle-line marking for objects spanning lines.
- **Correctness:** Neighboring counts in the same byte survive every compare-and-swap; sticky counts are never decremented; straddle lines and the continuation sentinel match the reference for multi-line objects.
- **Benchmarks:** RC update microbenchmark under contention, and the allocation churn scenario where increment volume dominates.
- **Dependencies:** P2.1, P2.2
- **References:** `mmtk-core/src/util/rc.rs:13-26`; `mmtk-core/src/util/constants.rs:122-124`; note .NET's `MIN_OBJECT_SIZE` is 24 B versus MMTk's 8 B

### P2.4 — Implement allocation, holes, and recycled lines

- **Status:** planned
- **Summary:** Implement bump allocation within holes, hole discovery by bulk RC-table read, fresh versus recycled block acquisition, and mutator block ownership.
- **Correctness:** Hole discovery finds exactly the free line runs the reference would find, and a line handed out during concurrent marking bumps its reuse counter, which P6.4 depends on.
- **Benchmarks:** Allocation throughput on fresh versus recycled blocks, and the long-lived cache scenario where recycling dominates.
- **Dependencies:** P2.2
- **References:** `mmtk-core/src/policy/immix/block_allocation.rs`; `mmtk-core/src/policy/immix/line.rs`

### P2.5 — Implement the large object space

- **Status:** planned
- **Summary:** Implement a large object space for allocations at or above 32 KiB, with its own mark and nursery bits and a page reuse counter.
- **Correctness:** The threshold is the full block size, matching `MAX_IMMIX_OBJECT_SIZE`; large objects are never placed in Immix blocks and are released independently of block sweeping.
- **Benchmarks:** Large-object pressure scenario against both baselines.
- **Dependencies:** P2.2
- **References:** `mmtk-core/src/plan/lxr/mod.rs:11-19`; `mmtk-core/src/policy/largeobjectspace.rs`

## P3 — Work-packet scheduler and pause taxonomy

- **Status:** planned
- **Summary:** Build the ordered bucket scheduler and the four pause kinds before any RC or tracing code depends on them, because bucket ordering is the reference collector's RC correctness proof.

### P3.1 — Implement work packets and strictly ordered buckets

- **Status:** planned
- **Summary:** Implement the ordered bucket sequence in which each stage opens only once all prior stages have drained, with work stealing and reference-sized packet granularity.
- **Correctness:** Bucket opening order is enforced and asserted, since reordering or renaming buckets silently breaks RC correctness with no compile-time signal; increments land in the first STW bucket and decrements in the last.
- **Benchmarks:** Scheduler overhead and parallel scaling on the multi-thread throughput scenario.
- **Dependencies:** P2.1
- **References:** `mmtk-core/src/scheduler/work_bucket.rs:456`; stage order Unconstrained, Concurrent, ConcurrentResumable, FinishConcurrentWork, Initial, Prepare, Closure, Release, STWRCDecsAndSweep, Final

### P3.2 — Implement the pause taxonomy and per-pause schedules

- **Status:** planned
- **Summary:** Implement the four pause kinds — Full, InitialMark, FinalMark, RefCount — and the schedule each one installs.
- **Correctness:** Each pause kind schedules exactly the reference's bucket set, including disabling unnecessary buckets for RefCount pauses.
- **Benchmarks:** Pause count and duration distribution by kind across the scenario matrix.
- **Dependencies:** P3.1
- **References:** `mmtk-core/src/plan/concurrent/mod.rs:16-31`; `mmtk-core/src/plan/lxr/global.rs:738-789`

### P3.3 — Implement the deferred concurrent bucket

- **Status:** planned
- **Summary:** Implement the double-buffered bucket queue where deferred work is added to the inactive queue and published by a flip once mutators resume.
- **Correctness:** Work deferred during a pause becomes visible only after the flip at GC finish; getting the buffering backwards either defeats laziness or drops the work entirely, so both directions are tested explicitly.
- **Benchmarks:** Pause duration with lazy decrements enabled versus disabled, isolating what deferral actually buys.
- **Dependencies:** P3.1
- **References:** `mmtk-core/src/scheduler/`; `mmtk-core/src/plan/lxr/args.rs:109`

### P3.4 — Implement lazy sweeping completion counters

- **Status:** planned
- **Summary:** Implement the RAII completion counter with a decrement-specific sub-counter that schedules mature block sweeping at decrement-zero and runs the next-GC decision at overall-zero.
- **Correctness:** No block is released while lazy work referencing it remains outstanding, and the next pause's kind is decided only after the previous GC's lazy work drains; this is also the mechanism that keeps finalizable promotion ahead of block release.
- **Benchmarks:** Lazy-decrement backlog depth and its effect on subsequent pause times.
- **Dependencies:** P3.3
- **References:** `mmtk-core/src/lib.rs:79-143`

## P4 — Coalescing reference counting core

- **Status:** planned
- **Summary:** Implement the coalescing RC engine — log buffers, increment and decrement packets, delayed root decrements, and metadata-only nursery reclamation.

### P4.1 — Implement mutator log buffers and flush

- **Status:** planned
- **Summary:** Implement the per-mutator increment and decrement buffers the barrier feeds, with the reference's capacity and flush points.
- **Correctness:** Increments carry slot addresses and decrements carry old target objects, not the reverse; buffers flush on full, at every pause start, and at thread teardown, with no entries lost at teardown.
- **Benchmarks:** Barrier take rate and buffer flush frequency under allocation churn and multi-thread throughput.
- **Dependencies:** P1.7, P2.3, P3.1
- **References:** `mmtk-core/src/plan/lxr/barrier.rs:61-116`; `mmtk-core/src/plan/lxr/args.rs:86-98`

### P4.2 — Implement increment packets and first promotion

- **Status:** planned
- **Summary:** Process increment slots by loading the current target, unlogging that exact slot, and incrementing, treating a zero-to-one transition as first promotion.
- **Correctness:** First promotion stamps the object start, arms field unlog bits in bulk, marks straddle lines, and recursively enqueues child slots; recursive increments drain synchronously within the packet so they cannot leak past the bucket boundary.
- **Benchmarks:** Increment rate and promotion rate against the paper's reported figures.
- **Dependencies:** P4.1
- **References:** `304ce69d:src/plan/lxr/rc.rs:40` (`ProcessIncs`); paper §3.1

### P4.3 — Implement decrement packets and recursive death

- **Status:** planned
- **Summary:** Process decrements so that a one-to-zero transition runs dead-object handling, recursively decrementing children and queueing the containing mature block.
- **Correctness:** No object memory is zeroed and only metadata is cleared; large objects are released through their own path; sticky counts are never decremented.
- **Benchmarks:** Decrement processing cost and its share of pause time, plus the cyclic garbage scenario where RC alone cannot reclaim.
- **Dependencies:** P4.1
- **References:** `304ce69d:src/plan/lxr/rc.rs:669` (`ProcessDecs`); PLDI `df8d30a3:src/util/rc.rs:747` (`impl ProcessDecs`)

### P4.4 — Implement one-epoch-delayed root decrements

- **Status:** planned
- **Summary:** Delay root decrements exactly one epoch by swapping the current and previous root sets at release and draining the previous set in the following pause.
- **Correctness:** Roots incremented in an epoch are never decremented in that same epoch, because doing so races recursive increments; the swap happens at release and nowhere else.
- **Benchmarks:** Root set size over time and its effect on pause duration.
- **Dependencies:** P4.2, P4.3
- **References:** `mmtk-core/src/plan/lxr/global.rs:314-318,911-931`

### P4.5 — Implement nursery sweeping without object parsing

- **Status:** planned
- **Summary:** Reclaim young objects purely from block state and bulk RC-table reads, never parsing the heap.
- **Correctness:** The sweep never calls any size or layout query and walks at 8-byte granularity; reclaimed regions match what the reference would reclaim on identical heaps.
- **Benchmarks:** Nursery reclamation rate and sweep cost as a share of pause time; this path is where an object-parsing design would show up as cost proportional to live heap.
- **Dependencies:** P2.4, P4.2
- **References:** `304ce69d:src/policy/immix/rc_work.rs:128` (`SweepBlocksAfterDecs`); `304ce69d:src/policy/immix/block.rs:634` (`rc_dead`), `:658` (`sweep`)

## P5 — SATB concurrent trace and cycle reclamation

- **Status:** planned
- **Summary:** Add the concurrent snapshot-at-the-beginning trace that reclaims the cycles reference counting cannot, including the safety invariant that binds it to the RC path.

### P5.1 — Implement the InitialMark snapshot

- **Status:** planned
- **Summary:** Turn the incremented root set into the concurrent trace's initial worklist and enable concurrent-marking state as mutators resume.
- **Correctness:** Concurrent marking state and remembered-set recording turn on at pause end, not pause start, matching the reference's ordering.
- **Benchmarks:** InitialMark pause duration against the paper's reported pause distribution.
- **Dependencies:** P3.2, P4.2
- **References:** `304ce69d:src/plan/lxr/cm.rs:328` (`ProcessModBufSATB`); paper §3.2

### P5.2 — Implement concurrent, preemptible trace packets

- **Status:** planned
- **Summary:** Implement self-splitting trace packets that drain greedily between pauses and flush their remainder when an STW pause interrupts.
- **Correctness:** An interrupted packet resumes without losing or double-visiting objects; the packet count reaching zero is the termination signal that makes the next pause a FinalMark.
- **Benchmarks:** Concurrent marking duration and its mutator overhead, plus pause p99 during marking.
- **Dependencies:** P5.1
- **References:** `304ce69d:src/plan/lxr/cm.rs:32` (`LXRConcurrentTraceObjects`), `:286` (its `GCWork` impl)

### P5.3 — Implement the mark-and-scan-on-delete invariant

- **Status:** planned
- **Summary:** Ensure reference counting never deletes an unmarked object while a snapshot trace is underway, by marking the dying object and force-marking its unmarked children into the tracer's worklist.
- **Correctness:** This is the paper's core safety property; a targeted test constructs the race — an object dying by RC during concurrent marking with unmarked children — and shows the children survive. The barrier's decrement buffer is shared into both the SATB modbuf processing and decrement processing, with modbuf processing ordered before the decrement bucket.
- **Benchmarks:** Frequency of the mark-on-delete path and its cost, plus the cyclic garbage scenario.
- **Dependencies:** P4.3, P5.2
- **References:** PLDI `df8d30a3:src/util/rc.rs:747` (`impl ProcessDecs`), `:870-874` (packet dispatch); HEAD `304ce69d:src/plan/lxr/rc.rs:669` (`ProcessDecs`); paper §3.3

### P5.4 — Implement FinalMark

- **Status:** planned
- **Summary:** Flush mutator barrier buffers, clear concurrent-marking state inside the pause, rescan roots, run the weak reference closures, process the mature evacuation remembered set, and schedule mature sweeping.
- **Correctness:** An interrupting emergency GC forces the trace to finish inside the STW pause rather than being abandoned; concurrent-marking state is cleared inside the pause, not after it.
- **Benchmarks:** FinalMark pause duration against the paper's reported figures, which the paper places at a few milliseconds.
- **Dependencies:** P5.2
- **References:** `mmtk-core/src/plan/lxr/global.rs`; paper §3.2

### P5.5 — Implement dead-cycle and mature sweeping

- **Status:** planned
- **Summary:** Reclaim objects with a nonzero reference count that the trace did not mark, then release blocks whose contents are entirely dead.
- **Correctness:** Dead-cycle handling deliberately does not recurse, unlike the RC death path; sweeping parses no objects; whole-block release happens only after the bulk dead check passes.
- **Benchmarks:** Cycle reclamation share of total reclamation, compared against the paper's Young/Old-RC/SATB split.
- **Dependencies:** P3.4, P5.4
- **References:** `304ce69d:src/policy/immix/rc_work.rs:181` (`SweepDeadCycles`); `304ce69d:src/policy/immix/block.rs:658` (`sweep`)

## P6 — Evacuation and defragmentation

- **Status:** planned
- **Summary:** Add copying — nursery evacuation during increment processing and mature evacuation driven by fragmentation — on top of the header-bit decision made in P1.6.

### P6.1 — Implement the copy and forwarding protocol

- **Status:** planned
- **Summary:** Implement the three forwarding states and the claim protocol in which a thread that loses the race spins until forwarding completes.
- **Correctness:** There is no read barrier, so every reader must resolve forwarding explicitly; an audit confirms every closure and verification site resolves forwarding before testing liveness.
- **Benchmarks:** Copy throughput and forwarding contention rate under multi-thread allocation.
- **Dependencies:** P1.6, P2.2
- **References:** `mmtk-core/src/util/object_forwarding.rs:8-11`

### P6.2 — Implement nursery evacuation inside increment processing

- **Status:** planned
- **Summary:** Copy surviving nursery objects during the promotion edge operation, rewriting stable slots in place, with a pause-budget escape hatch.
- **Correctness:** Source blocks stay unavailable while forwarding metadata is still observable; exceeding the pause budget during increment processing disables evacuation for the remainder of that pause rather than truncating it mid-object.
- **Benchmarks:** Nursery survival rate, copy volume, and pause duration with and without nursery evacuation.
- **Dependencies:** P4.2, P6.1
- **References:** `304ce69d:src/plan/lxr/rc.rs:40` (`ProcessIncs`, nursery evacuation path); paper §3.1

### P6.3 — Implement mature evacuation candidate selection

- **Status:** planned
- **Summary:** Select defragmentation candidates from sparsely populated chunks and from individually fragmented blocks scored by dead bytes, greedily until the copy budget is met.
- **Correctness:** Thresholds match the reference, including the chunk occupancy threshold, its doubling under emergency, and the half-block admission floor outside a full collection.
- **Benchmarks:** Fragmentation over time and committed-versus-live ratio against both baselines.
- **Dependencies:** P5.4, P6.1
- **References:** `mmtk-core/src/plan/lxr/mature_evac.rs:230-285`

### P6.4 — Implement the reuse-counter-tagged remembered set

- **Status:** planned
- **Summary:** Record remembered-set entries tagged with the line reuse counter captured at record time and validate that tag at replay.
- **Correctness:** An entry whose reuse counter no longer matches is discarded, as are slots inside the collection set and slots in unallocated blocks; a targeted test reuses a line during concurrent marking and confirms the stale entry is rejected.
- **Benchmarks:** Remembered-set size, stale-entry rate, and replay cost.
- **Dependencies:** P2.4, P6.3
- **References:** `304ce69d:src/plan/lxr/remset.rs:39` (`MatureEvecRemSet`)

### P6.5 — Implement mature evacuation and source block release

- **Status:** planned
- **Summary:** Feed surviving remembered-set slots into the ordinary STW closure and release defragmentation source blocks once their live content has been copied out.
- **Correctness:** Source blocks are released unconditionally after evacuation, and no slot referencing them survives unforwarded, verified by a post-evacuation heap audit. The oracle can release unconditionally only because it never compiles pinning and answers `is_pinned()` with a compile-time `false`; a .NET port must design the release path from the start to tolerate a block holding an immovable object, so P7.4 is a constraint on this step rather than a later addition.
- **Benchmarks:** Defragmentation effectiveness, evacuation ratio, and its pause cost.
- **Dependencies:** P6.4
- **References:** `304ce69d:src/plan/lxr/mature_evac.rs:19` (`EvacuateMatureObjects`)

## P7 — Managed object lifecycle

- **Status:** planned
- **Summary:** Implement the .NET lifecycle semantics the reference does not have — weak reference ordering, dependent handles, finalization, and pinning — as a deliberate superset of LXR.

### P7.1 — Implement weak reference semantics

- **Status:** planned
- **Summary:** Implement CoreCLR's handle clearing order, which the reference implementation does not model.
- **Correctness:** Short weak handles clear before f-reachability and long weak and weak-interior handles clear after; ref-counted handles consult the EE callback; surviving weak locations are relocation-pinned without becoming roots.
- **Benchmarks:** Lifecycle semantics scenario, which must pass identically on all three collectors.
- **Dependencies:** P5.4
- **References:** `src/coreclr/gc/objecthandle.cpp`; `docs/design/coreclr/botr/garbage-collection.md`

### P7.2 — Implement dependent handles to a fixed point

- **Status:** planned
- **Summary:** Resolve ephemeron dependencies to a fixed point in both the mark phase and the RC increment phase, and again after finalization promotion.
- **Correctness:** A dependent-handle chain of arbitrary depth is fully resolved in one collection, not progressively across several.
- **Benchmarks:** Dependent-handle-heavy variant of the lifecycle scenario, measuring iteration count to fixed point.
- **Dependencies:** P7.1
- **References:** `src/coreclr/gc/objecthandle.cpp`

### P7.3 — Implement the finalization lifecycle

- **Status:** planned
- **Summary:** Implement registration at allocation-complete, suppression, re-registration, f-reachability, resurrection, and normal-before-critical ordering, with deferred removal from the RC world.
- **Correctness:** Block release never outruns finalizable promotion, which is why this depends on the lazy-sweeping counters; resurrection during finalization keeps the object and its transitive closure alive.
- **Benchmarks:** Lifecycle semantics scenario including resurrection and critical finalizers.
- **Dependencies:** P3.4, P7.1
- **References:** `src/coreclr/vm/finalizerthread.cpp`; `src/coreclr/gc/gcinterface.h`

### P7.4 — Honour pinning and interior pointers

- **Status:** planned
- **Summary:** Design and implement pinning for a collector that evacuates, consuming the pinning and interior-pointer flags reported by root scanning and additionally honouring heap-resident pinned objects. **The reference provides no realization to port:** LXR declares `PinningProcessEdges = UnsupportedProcessEdges` in both work contexts and disables the `TPinningClosure` and `PinningRootsTrace` buckets, so this mechanism is designed fresh rather than ported.
- **Correctness:** Ordinary roots are never blanket-pinned, which would permanently exclude them from first promotion and mature evacuation; callback-local roots may still be forwarded and rewritten during the callback while the slot is valid. Pinning is not root-only: `GCHandleType.Pinned`, `fixed`, and the pinned object heap make objects immovable while reachable from the heap, so evacuation and defragmentation must honour a pinned bit rather than assume `is_pinned()` is false. Source-block release must tolerate a block that cannot be fully evacuated.
- **Benchmarks:** Pinning-heavy I/O scenario, comparing pinned-object counts and fragmentation against both baselines. Because the oracle never ran with pinning compiled, any pinning cost is a .NET-only delta with no reference number to match.
- **Dependencies:** P6.2, P6.5
- **References:** `src/coreclr/gc/gcinterface.h` `GC_CALL_INTERIOR` and `GC_CALL_PINNED`; the reference's unbuilt surface at `304ce69d:src/policy/sft.rs:50-55` and `304ce69d:src/policy/immix/immixspace.rs:169-179`, both `cfg(object_pinning)`, against LXR's opt-out at `304ce69d:src/plan/lxr/gc_work.rs:12`, `:21` and `304ce69d:src/plan/lxr/global.rs:707-708`

## P8 — Triggers, predictors, and tunables

- **Status:** planned
- **Summary:** Implement the feedback loop that decides when to collect and which pause to run, at the reference's exact formulas and defaults.

### P8.1 — Implement survival and mature-live predictors

- **Status:** planned
- **Summary:** Implement the survival ratio predictor with its rising and falling bias variants, and the mature live predictor fed only after FinalMark or Full pauses.
- **Correctness:** Weights and update points match the reference exactly, including which pause kinds feed which predictor.
- **Benchmarks:** Prediction error over time on the allocation churn and long-lived cache scenarios.
- **Dependencies:** P4.5
- **References:** `mmtk-core/src/plan/lxr/mod.rs:24-124`

### P8.2 — Implement collection trigger conditions

- **Status:** planned
- **Summary:** Implement the ordered trigger conditions from heap-full through the young allocation cap.
- **Correctness:** Conditions are evaluated in the reference's order, since an earlier condition firing changes which pause kind is chosen.
- **Benchmarks:** Collection frequency and heap occupancy at trigger across the matrix.
- **Dependencies:** P8.1
- **References:** `mmtk-core/src/plan/lxr/global.rs:125-179`

### P8.3 — Implement the pause selection state machine

- **Status:** planned
- **Summary:** Implement pause selection: finish an in-flight marking cycle, then emergency and user-triggered collections, then the cycle hint, otherwise a RefCount pause.
- **Correctness:** The cycle hint is computed only after the previous collection's lazy sweeping completes, behind the same condition variable the reference uses.
- **Benchmarks:** Pause kind mix compared against the paper's reported distribution.
- **Dependencies:** P5.4, P8.2
- **References:** `mmtk-core/src/plan/lxr/global.rs:738-789`

### P8.4 — Expose the tunable surface and configuration

- **Status:** planned
- **Summary:** Expose the reference's tunables through `DOTNET_` configuration at the reference defaults.
- **Correctness:** Defaults match the reference exactly — trace threshold 20, RC stop percent 15, chunk defragmentation percent 32, max survival 128 MB, buffer size 1024, defragmentation headroom 5 percent, lazy decrements on, nursery and mature evacuation on.
- **Benchmarks:** Sensitivity of each tunable on at least one scenario where it is load-bearing.
- **Dependencies:** P8.3
- **References:** `mmtk-core/src/plan/lxr/args.rs`

## P9 — Parity audit and performance convergence

- **Status:** planned
- **Summary:** Independently verify every ledger row against its declared oracle, then close the gap to the paper's reported pause, throughput, and footprint behavior.

### P9.1 — Run an independent mechanism parity audit

- **Status:** planned
- **Summary:** Re-verify every ledger row against the revision that row declares as its oracle, performed by someone other than the implementer of that row.
- **Correctness:** The audit does not trust prior status text; coupled groups are checked for oracle consistency, not only row-by-row correctness, and any unrecorded deviation reopens its step.
- **Benchmarks:** None directly; the audit gates whether later performance numbers describe LXR at all.
- **Dependencies:** P0.3, P8.4
- **References:** P0.3 ledger; both oracle revisions

### P9.2 — Converge pause times

- **Status:** planned
- **Summary:** Close the gap to the paper's pause behavior: frequent light RefCount pauses with a few milliseconds for InitialMark and FinalMark.
- **Correctness:** Pause improvements never come from deferring work indefinitely; lazy backlog is reported alongside pause figures.
- **Benchmarks:** Report p50 through p99.99 and maximum, never averages alone, across the full matrix against both baselines, but read as a characterization rather than a win condition: the paper's only comparative pause table shows LXR's pauses longer than G1's at every percentile while its query latency is far better, so application-observed latency is the acceptance signal and pause distribution is reported alongside it.
- **Dependencies:** P9.1
- **References:** paper §5.2, Figure 5; Table 1 pause versus query-latency split; `docs/design/lxr-port/P0.2-paper-targets.md`

### P9.3 — Converge throughput

- **Status:** planned
- **Summary:** Close the throughput gap against both built-in collectors, isolating the barrier's own contribution.
- **Correctness:** Throughput wins are rejected if verifier offenders, crashes, hangs, or lifecycle failures increase.
- **Benchmarks:** Full matrix against Workstation and Server GC, with a barrier-isolation measurement against the paper's reported barrier overhead, noting that the paper's throughput advantage is heap-dependent — `0.97` at 1.3x, `0.96` at 2x, and `1.01` at 6x, where LXR is slower than G1 — and that its `1.016` barrier geomean sits inside a noise floor of roughly plus or minus three percent.
- **Dependencies:** P9.1
- **References:** paper §5.1, §5.3; Table 5 heap-size sensitivity; `docs/design/lxr-port/P0.2-paper-targets.md`

### P9.4 — Converge footprint and reclamation mix

- **Status:** planned
- **Summary:** Bring working set, committed-versus-live ratio, fragmentation, and the reclamation split into line with the paper.
- **Correctness:** The reclamation split is measured, not inferred, and attributed to Young, Old-RC, and SATB paths separately.
- **Benchmarks:** Compare the measured split against the paper's reported proportions, plus footprint against both baselines.
- **Dependencies:** P9.1
- **References:** paper §5.4, Tables 5-7

### P9.5 — Reproduce the paper's ablations and sensitivity study

- **Status:** planned
- **Summary:** Run the heap-size sensitivity sweep and the mechanism ablations to show each mechanism earns its place.
- **Correctness:** Each ablation degrades in the direction and rough magnitude the paper reports; an ablation that changes nothing indicates the mechanism is not actually wired in.
- **Benchmarks:** Heap-size sweep plus ablations for nursery evacuation, mature evacuation, concurrent marking, RC width, and lazy decrements.
- **Dependencies:** P9.2, P9.3, P9.4
- **References:** paper §5.5, Figure 7

## P10 — Hardening and platform matrix

- **Status:** planned
- **Summary:** Extend beyond x64, prove long-duration reliability, add diagnostics, and decompose the work into upstreamable pull requests.

### P10.1 — Port the barrier family to the remaining architectures

- **Status:** planned
- **Summary:** Extend the slot-log barrier family to the remaining five architectures and NativeAOT, as family implementations rather than LXR-specific ports.
- **Correctness:** Each architecture passes the same barrier stress and store-path audit as x64, with volatile and SIMD state preserved per calling convention.
- **Benchmarks:** Per-architecture no-regression measurement matching the gate established for x64.
- **Runtime changes:** Adds arm64, arm, i386, loongarch64, and riscv64 implementations of the barrier family in both MASM and GAS plus NativeAOT, so a future consumer of the family inherits all six architectures rather than porting again; includes the ReadyToRun implication that precompiled images bake in barrier helper calls.
- **Dependencies:** P9.1
- **References:** `src/coreclr/vm/arm64/`, `src/coreclr/vm/i386/`, `src/coreclr/vm/loongarch64/`, `src/coreclr/vm/riscv64/`; `src/coreclr/nativeaot/Runtime/`

### P10.2 — Run long-duration reliability stress

- **Status:** planned
- **Summary:** Run multi-hour stress across the scenario matrix with verifiers enabled.
- **Correctness:** No tolerated crashes, hangs, or leaks; every failure is root-caused rather than retried.
- **Benchmarks:** Drift in pause distribution, footprint, and fragmentation over hours, which short runs cannot reveal.
- **Dependencies:** P9.1
- **References:** `src/tests/GC/`; existing GC stress infrastructure

### P10.3 — Add diagnostics and observability

- **Status:** planned
- **Summary:** Emit events and counters for the pause taxonomy and the metrics the paper reports.
- **Correctness:** Emitted counters agree with internal state under stress; the pause taxonomy is representable in existing GC event schemas or is extended deliberately.
- **Benchmarks:** Increment rate, barrier take rate, sticky count distribution, reclamation split, and lazy-decrement backlog, all exposed without measurably perturbing the workload.
- **Dependencies:** P9.1
- **References:** `src/coreclr/vm/eventtrace.cpp`; `src/coreclr/gc/gcevents.h`

### P10.4 — Decompose the work into upstream-ready pull requests

- **Status:** planned
- **Summary:** Split the work so the generic runtime capabilities land independently of LXR, each defensible on its own merits.
- **Correctness:** Each runtime capability has its own issue, motivation, and performance evidence, and would be a reasonable change to make even if the collector never merges.
- **Benchmarks:** Per-pull-request performance evidence, since core-runtime changes require measurements in the description.
- **Dependencies:** P9.5, P10.1
- **References:** `CONTRIBUTING.md`; repository pull request conventions
